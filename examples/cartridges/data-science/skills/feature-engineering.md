# feature-engineering

Derive the variables a model will actually use. Covers encoding (one-hot, target, embedding), scaling, transformation (log, Box-Cox), interaction and ratio features, time-series lags, text vectorization, and principled feature selection.

**Triggers:** `feature engineering`, `encoding`, `feature scaling`, `feature selection`, `interaction features`, `embeddings`, `tf idf`, `lag features`

**Affinity:** `ml-modeler`, `data-wrangler`
