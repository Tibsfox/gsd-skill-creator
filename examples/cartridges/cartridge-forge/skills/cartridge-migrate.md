---
name: cartridge-migrate
description: Migrate a legacy content cartridge to the unified model.
---

# cartridge-migrate

Use when the user has an old-format content cartridge (from
`src/bundles/cartridge/`) that needs to be upgraded into the unified
Cartridge + Chipset shape. Preserves id by default; stamps migrate
provenance with optional sourceCommits/buildSession.
