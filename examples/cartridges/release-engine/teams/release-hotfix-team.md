# release-hotfix-team

Ship an out-of-cadence hotfix. Engineer branches from the last tag, applies the minimal fix, publisher drives the release, verifier runs the smoke test. Skips cadence but keeps the ship gates.

**Roster:** `release-capcom`, `release-engineer`, `release-publisher`, `release-verifier`.

**Use when:** an urgent fix is required between scheduled releases
