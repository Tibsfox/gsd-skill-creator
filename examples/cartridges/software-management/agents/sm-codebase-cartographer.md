# sm-codebase-cartographer

Opus-class agent specialized in understanding existing code. Produces codebase maps, architecture extractions, and dependency graphs. Reads by intent, not by line number, and outputs artifacts that every other agent in the cartridge can consume.

**Model:** `opus`
**Tools:** `Read`, `Grep`, `Glob`, `Bash`
