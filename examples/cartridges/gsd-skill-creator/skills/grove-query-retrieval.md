# grove-query-retrieval

Query the grove arena — single-hop lookup by name, SkillView + SignatureView projections, ActivityLog reads, and multi-hop retrieval (dependency chains, cross-wing keyword intersection, subject→predicate path traversal). Covers the SkillCodebase interface and the query patterns exercised by multi-hop-retrieval.test.ts.

**Triggers:** `query grove`, `grove lookup`, `skillview`, `signatureview`, `multi-hop retrieval`, `grove dependency chain`, `find skill in grove`

**Affinity:** `grove-keeper`
