# incident-command

Drive a live production incident from detection through stabilization. The commander owns the room, not the fix — severity-setting, bridge comms, sitrep cadence, scribe assignment, and clean shift-handoff are the job. Triggers on `declare incident`, `sev1/2`, `open bridge`, `stabilize outage`. Affinity: `sysops-commander`, `sysops-runner`.
