# observability-pipeline

Care and feeding of the metrics, logs, and traces pipeline — collectors, aggregators, retention, index health, sampling strategy. Keeps the pipeline itself observable (who monitors the monitors). Triggers on `metrics pipeline`, `log ingestion`, `trace sampling`, `prometheus scrape`, `otel collector`. Affinity: `sysops-observability-engineer`, `sysops-runner`.
