# formal-methods-and-verification

Hoare logic, weakest preconditions, separation logic, invariants, model checking (finite and infinite state), and machine-checked proof assistants (Coq, Lean, Isabelle, Agda, F*). How to prove — not merely test — that a program matches its specification.

**Triggers:** `hoare logic`, `weakest precondition`, `separation logic`, `model checking`, `coq`, `lean prover`, `isabelle`, `agda`, `f star`, `machine checked proof`

**Affinity:** `proof-theorist`
