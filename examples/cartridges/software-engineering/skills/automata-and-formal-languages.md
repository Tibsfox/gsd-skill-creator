# automata-and-formal-languages

Finite automata (DFA / NFA), regular expressions and regular languages, pushdown automata and context-free grammars, the Chomsky hierarchy, pumping lemmas, closure properties, and the decidability boundary between each language class.

**Triggers:** `dfa`, `nfa`, `regular language`, `context free grammar`, `pushdown automaton`, `chomsky hierarchy`, `pumping lemma`

**Affinity:** `language-theorist`, `cs-professor`
