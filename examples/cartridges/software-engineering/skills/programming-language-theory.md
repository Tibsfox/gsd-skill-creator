# programming-language-theory

Syntax, parsing theory (LL / LR / PEG), abstract syntax trees, name resolution, scoping rules, evaluation orders, effect systems, module systems, and the fundamental design trade-offs every language maker eventually faces. Covers the compiler pipeline at the conceptual level.

**Triggers:** `parser theory`, `ll parser`, `lr parser`, `ast`, `scoping rules`, `effect system`, `language design`, `compiler pipeline`

**Affinity:** `language-theorist`
