# algorithms-and-data-structures

Design paradigms (divide and conquer, dynamic programming, greedy, backtracking, randomized, amortized), core data structures (heaps, balanced trees, hash tables, union-find, tries, graphs), correctness proofs via loop invariants, and worst / average / amortized complexity analysis.

**Triggers:** `algorithm design`, `dynamic programming`, `divide and conquer`, `greedy algorithm`, `data structure`, `loop invariant`, `amortized analysis`

**Affinity:** `algorithms-analyst`, `cs-professor`
