# dataset-curation-team

Move a dataset through its lifecycle. Curator handles the raw→curated→derived transitions, archivist reviews anything that heads toward cold, capcom approves. Writes DatasetLifecycleEvent and DedupReport records.

**Roster:** `housekeeping-capcom`, `curator`, `archivist`.

**Use when:** curating, deduping, or lifecycle-moving a dataset
