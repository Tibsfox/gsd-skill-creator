# build-and-ci

Own the build system and continuous integration pipeline. Covers reproducible builds, dependency pinning, cache strategy, CI stage decomposition, fast feedback on PRs, and keeping a green main branch as a team invariant.

**Triggers:** `build config`, `ci pipeline`, `github actions`, `reproducible build`, `dependency lock`, `cache ci`, `green main`

**Affinity:** `sd-release-engineer`, `sd-developer`
