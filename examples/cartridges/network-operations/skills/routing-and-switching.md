# routing-and-switching

Operate the L2/L3 fabric — OSPF/IS-IS interior routing, static routes, VLAN/trunk config, spanning-tree topology, LACP, and VRF separation. Adds, moves, and changes on routers and switches with rollback discipline. Triggers on `ospf`, `isis`, `vlan`, `trunk port`, `spanning tree`, `lacp`, `vrf`. Affinity: `netops-engineer`, `netops-architect`.
