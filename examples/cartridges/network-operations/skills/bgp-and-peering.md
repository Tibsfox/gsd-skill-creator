# bgp-and-peering

Own eBGP and iBGP — session health, route policy, prefix/AS-path filters, MED and local-preference tuning, communities. Drives peering with transit providers, peers, and route servers. Enforces RPKI validation and max-prefix safeties. Triggers on `bgp session`, `ebgp`, `peering`, `route map`, `rpki`, `looking glass`. Affinity: `netops-architect`, `netops-engineer`.
