# {{name}}

Scaffolded cartridge. Edit `cartridge.yaml` and the files under `chipsets/`,
`skills/`, `agents/`, and `teams/` to define real capabilities.

Slug: `{{slug}}`
Trust: `{{trust}}`

## Load + validate

```
skill-creator cartridge load ./cartridge.yaml
skill-creator cartridge validate ./cartridge.yaml
```
