/**
 * MCP Host template file generators.
 *
 * Generates a complete MCP host project with client pool, lifecycle management,
 * transport abstraction, and approval gates.
 *
 * @module mcp/templates/host-template
 */

import type { McpProjectConfig, TemplateFile } from './types.js';

/** Generate all files for an MCP host project. Stub -- implemented in Plan 299-02. */
export function generateHostFiles(_config: McpProjectConfig): TemplateFile[] {
  return [];
}
