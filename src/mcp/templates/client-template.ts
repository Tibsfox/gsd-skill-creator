/**
 * MCP Client template file generators.
 *
 * Generates a complete MCP client project with tool discovery,
 * resource subscription, and typed responses.
 *
 * @module mcp/templates/client-template
 */

import type { McpProjectConfig, TemplateFile } from './types.js';

/** Generate all files for an MCP client project. Stub -- implemented in Plan 299-02. */
export function generateClientFiles(_config: McpProjectConfig): TemplateFile[] {
  return [];
}
