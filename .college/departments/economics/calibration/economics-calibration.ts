import type { CalibrationEngine } from '../../../rosetta-core/types.js';

/**
 * Calibration stub for the Economics department.
 * Future models will assess understanding of supply/demand, opportunity cost,
 * market structures, and personal finance concepts.
 */
export function registerEconomicsModels(_engine: CalibrationEngine): void {
  // CalibrationModel registration will be added in a future phase.
  void _engine;
}
