import type { CalibrationEngine } from '../../../calibration/engine.js';

/**
 * Register astronomy calibration models with the calibration engine.
 *
 * Stub: astronomical calculation models defined in a future content phase.
 * Astronomy has quantitative calculations (orbital periods, angular distances,
 * stellar magnitudes, redshift measurements) that warrant dedicated calibration models.
 */
export function registerAstronomyModels(_engine: CalibrationEngine): void {
  // Stub: astronomical calculation models defined later
}
