# Build output
dist/

# Dependencies
node_modules/

# Planning docs (local only)
.planning/
.claude/
.cpanel.yml

# Brainstorm session data (local only)
.brainstorm/

# Discovery runtime state (local only)
.discovery-state.json

# Archive (deprecated code, hidden)
.archive/

# Generated dashboard (infra/dashboard/, not src/dashboard/)
infra/dashboard/*.html
infra/dashboard/.dashboard-manifest.json

# Tauri / Desktop
src-tauri/target/
src-tauri/gen/
desktop/dist/
desktop/node_modules/

# Citation cache (regenerable API responses)
data/citations/cache/

# Discovery engine URL snapshots (local audit trail, not committed)
scripts/python/.discovery-snapshots/

# Compiled man pages (source is .scd)
extra/man/*.1
extra/man/*.5

# Generated API docs
docs/api/

# Coverage
coverage/
.college/coverage/

# === Filesystem Zones (v1.49.4) ===

# User workspace (personal, not shared)
projects/*/
!projects/README.md
!projects/.gitkeep

# Contrib staging (ephemeral review artifacts)
contrib/downstream/staging/*/
contrib/upstream/*/
!contrib/upstream/.gitkeep
!contrib/README.md

# Local project data
/media/

# Artifacts (local only — transcripts, audio, channel scans, summaries)
artifacts/

# Python cache
__pycache__/
*.pyc
.venv/

# Local data stores (regenerable)
.grove/
.chroma/

# Runtime state (regenerable)
.cadence-state.json

# Stray VTT transcripts (should be in artifacts/)
yt-queue-*.en.vtt

# Lost and found (orphaned files from refactors)
lost-and-found/


# User-local config
.sc-config.json
apps/the-space-between-engine/package-lock.json
.env
# API directory (local only — credentials, sync scripts, schemas)
# Published site content (228 MB, regenerable, lives on disk for sync scripts)
www/
# Prometheus sweep metrics artifact — moved to .planning/prometheus/
prometheus/
# Local session scratch (checkpoints, critique logs, conversation notes)
.local/
# Token telemetry runtime data (anchored at root; src/telemetry/ is source code)
/telemetry/
# Tibsfox-specific publishing infrastructure (local-only, not for distribution)
scripts/sweep/sweep_daemon.py
scripts/sync-research-to-live.sh
scripts/python/sync-usgs.py
scripts/python/build-learn-pages.py
# Background-shell orchestrator runtime state
/.bg-shell/
# Stale per-session continuation handoffs (local scratch)
/.continue-here.md
# Compiled shader-viewer binary (source is shader-viewer.c)
scripts/nasa/shader-viewer
# Fox Companies IP — local-only site content (books + essays)
src/site/content/books/
src/site/content/essays/
release-history.local.json
.planning/release-history.db
.mathlib-verify-checkout/
# Runtime telemetry (added v1.49.586 — operational artifacts, not source)
.logs/
test-results/
