# Lessons — v1.6

3 lessons extracted. Classification source: ⚙ rule-based · 🤖 LLM tiebreaker (needs review) · 👤 human.

1. **Examples are the best documentation.**
   34 working examples teach more about the skill format than the v1.3 specification does. People learn by reading real files, not abstract schemas.
   _⚙ Status: `applied` (applied in `v1.43`) · lesson #24_

2. **Three team topology examples (leader-worker, pipeline, swarm) validate the v1.4 topology design.**
   If the topologies couldn't support real examples, the design would need revision.
---
   _⚙ Status: `applied` (applied in `v1.44`) · lesson #25_

3. **Local installation via `install.cjs` is a manual step.**
   Users have to know to run the script. Later versions should consider making examples discoverable from the CLI.
   _⚙ Status: `investigate` · lesson #26_
