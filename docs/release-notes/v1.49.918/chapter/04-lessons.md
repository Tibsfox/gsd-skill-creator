# v1.49.918 — Lessons

No new manifest lessons codified this milestone. This is forward feature work
(Memory Arena subsystem resume), not a codification ship; the discipline
manifest holds at 24 domains / 149 lessons.

## Reinforced (no new lesson ID)

- **Read the code before building (ledger-driven-work-discipline #10409).** Every
  M14–M16 slice's recon overturned the roadmap's stale framing — kernel launch,
  benchmarks, the cgroup enforcer, and the migration proof were all already
  built. Per-thread recon precedes per-thread code; it prevented re-building
  existing capability three times in one campaign.
- **Skip-guard against gitignored runtime artifacts (#10182).** M16 brought
  `grove-migration-live.test.ts`'s first block into conformance with the
  `wwwHarnessAvailable` config pattern and its already-guarded siblings —
  `describe.skipIf(!liveCorpusExists)`, proven both directions.
- **Consume, don't shelve (meta-cadence consume axis #10438).** M15 wired the
  built-but-unconsumed `CgroupEnforcer` into a real allocation chokepoint;
  M14·D3 wired the built-but-unconsumed GPU checksum kernel into an integrity
  verdict.
