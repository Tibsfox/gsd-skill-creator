[
  {
    "domain": "Mission package framing",
    "trigger": "Before authoring a mission package or W0 brief; when 4+ clusters in a CF chain accumulate",
    "summary": "Closure-verification gate for multi-cluster CF chains + re-framing review pattern + W1 brief-error catch + scope-expansion mid-mission + fact-check corrections + session-time accumulation ceiling",
    "canonical_docs": [
      "docs/MISSION-PACKAGE-DISCIPLINE.md"
    ],
    "key_lessons": ["#10172", "#10178", "#10192", "#10199", "#10207", "#10209", "#10218", "#10224", "#10226", "#10234"],
    "codified_at_milestone": "v1.49.638 + v1.49.654 (forward-cadence lessons appended)"
  },
  {
    "domain": "Test authoring",
    "trigger": "Writing or modifying vitest tests; perf-assertion authoring; flake debugging",
    "summary": "Audit-method conventions (hookTimeout, ORDER-BY tiebreaker, perf-warmup, skip-guard env-vars) + CF closure-verification templates + flake catalog + meta-test skip-guards against gitignored runtime artifacts + perf-assertion regex coverage + plain-bullet lesson format",
    "canonical_docs": [
      "docs/test-discipline/audit-method-corrections.md",
      "docs/test-discipline/cf-closure-verification-templates.md",
      "docs/test-discipline/flake-audit-2026-05-11.md"
    ],
    "key_lessons": ["#10173", "#10175", "#10180", "#10181", "#10182", "#10199", "#10204"],
    "codified_at_milestone": "v1.49.585+ + v1.49.654 (meta-test + audit lessons appended)"
  },
  {
    "domain": "Ship pipeline",
    "trigger": "Touching tools/pre-tag-gate.sh, T14 ship steps, or hook scripts",
    "summary": "Apply-to-self enforcement, CI-gate override rationale (enumerated CSV per Lesson #10185), no Co-Authored-By trailers (v1.49.621), T14 step ordering (Lesson #10197), dev/main 0-commit drift via ship-sync, cross-link-strict flag, gh-CLI background-task git-discovery quirk",
    "canonical_docs": [
      ".planning/ship-pipeline-discipline/apply-to-self-allowlist.md",
      ".planning/ship-pipeline-discipline/ci-gate-override-rationale.md",
      "docs/T14-SHIP-SEQUENCE.md"
    ],
    "key_lessons": ["#10184", "#10185", "#10191", "#10197", "#10202", "#10204", "#10220", "#10221", "#10222"],
    "codified_at_milestone": "v1.49.636+ + v1.49.654 (ship-pipeline lessons appended)"
  },
  {
    "domain": "Self-modification safety",
    "trigger": "Editing .claude/{skills,agents,hooks}/ paths; staging files into .planning/, .claude/, .archive/, artifacts/, .gsd/, .chipset/, .env, project-claude/hooks/.log/",
    "summary": "PreToolUse hook BLOCKS self-mod operations without SC_SELF_MOD=1 / SC_INSTALL_CALLER=project-claude / npm_lifecycle_event override. git-add-blocker.js BLOCKS protected-path staging without SC_FORCE_ADD=1. settings.json gsd-config-guard-protected; new hooks route via project-claude/. Mission packages never committed.",
    "canonical_docs": [
      ".claude/skills/security-hygiene/SKILL.md",
      "project-claude/hooks/self-mod-guard.js",
      "project-claude/hooks/git-add-blocker.js"
    ],
    "key_lessons": ["#10174", "#10190", "#10201"],
    "codified_at_milestone": "v1.49.585 C01 + C02; extended v1.49.653; lessons appended v1.49.654"
  },
  {
    "domain": "Keystore",
    "trigger": "Touching src-tauri/src/security/keystore.rs or wrapping Node CLI surfaces; Argon2id + age v0.10 invariants",
    "summary": "OS keyring (preferred) → age-encrypted file (fallback). Legacy plaintext keystore retired v1.49.637.",
    "canonical_docs": [
      "docs/keystore.md"
    ],
    "key_lessons": [],
    "codified_at_milestone": "v1.49.585 + v1.49.637 cluster #4 retirement"
  },
  {
    "domain": "Substrate probe",
    "trigger": "Authoring W1 substrate-axis declarations; relocating substrate-probe docs into tracked docs/ from .planning/",
    "summary": "Substrate-form declaration patterns + LOCKED/SUSTAINED state-machine + forward-shadow first-instance convention + 4-LEVEL trophic cascade closure + aquatic-substrate-pivot + auction-theory winner's-curse pattern + HARD-BLOCK forbidden-substring detection at W2 verification",
    "canonical_docs": [
      "docs/SUBSTRATE-PROBE-DISCIPLINE.md"
    ],
    "key_lessons": ["#10210", "#10219", "#10223", "#10225", "#10227", "#10228"],
    "codified_at_milestone": "v1.49.638 C3 + v1.49.654 (substrate lessons appended)"
  },
  {
    "domain": "Citation debt",
    "trigger": "Authoring retrospective V-flag emit/close/state blocks; running T14 step 2.6 (citation-debt auto-update)",
    "summary": "Formal '### V-flag emit/close/state:' markdown blocks → scan-retrospectives.mjs --write-diff → apply-diff.mjs. Closes V-flag drift between retrospectives and the ledger.",
    "canonical_docs": [
      "docs/citation-debt-syntax.md",
      "tools/citation-debt/scan-retrospectives.mjs",
      "tools/citation-debt/apply-diff.mjs"
    ],
    "key_lessons": [],
    "codified_at_milestone": "v1.49.585 C13 (ledger) + v1.49.653 L-03 (auto-update)"
  },
  {
    "domain": "STORY drift recurrence",
    "trigger": "T14 ship sequence step 2.5; observing pre-tag-gate step 12 WARN about STORY.md drift",
    "summary": "Public docs/release-notes/STORY.md auto-append gate (T14 step 2.5) closes Lesson #10197 silent-no-op via per-tag append + idempotent re-run. Pre-tag-gate step 12 surfaces accumulated drift.",
    "canonical_docs": [
      "docs/T14-SHIP-SEQUENCE.md",
      "scripts/append-story-entry.mjs",
      "tools/check-story-drift.mjs"
    ],
    "key_lessons": ["#10191", "#10197"],
    "codified_at_milestone": "v1.49.637 C5 + v1.49.653 (drift detector)"
  },
  {
    "domain": "Sub-agent dispatch",
    "trigger": "Authoring W2/W3 sub-agent dispatch prompts; spawning parallel build agents; sizing token budgets per dispatch; embedding chapter-generation in NASA build dispatches",
    "summary": "Sub-agents lack SendMessage in toolkit (spawn-task-return-result, not continuous peers); ~60-70 tool-use ceiling per dispatch; bound dispatches to ~1 component / 30-50min; commit-between-deliverables discipline; chunked Write+Edit append pattern for >32K HTML deliverables; transient API errors recover via identical-prompt retry; 64K output-token cap requires JSON+HTML split; W3.5 chapter-gen via `run-with-pg.mjs refresh --fast --quiet` belongs in the dispatch deliverable list (v709 codification)",
    "canonical_docs": [
      "docs/sub-agent-dispatch-discipline.md"
    ],
    "key_lessons": ["#10193", "#10194", "#10200", "#10214", "#10215", "#10240", "#10244"],
    "codified_at_milestone": "v1.49.654 (new domain — codified from v1.49.637-651 lesson cluster); v1.49.709 (W3.5 chapter-gen bake-in)"
  },
  {
    "domain": "Counter-cadence cadence",
    "trigger": "Planning a non-degree-advancing milestone; converting accumulated social-rule debt into deterministic gates; meta-testing newly-introduced gates at ship time",
    "summary": "Counter-cadence cleanup-mission pattern productive every ~30 forward milestones; gate-not-vigilance discipline (convert offending rule to a gate, not a re-emphasized prose rule); meta-test newly-installed gates against the milestone's own release-notes during T14; discipline-as-code in 3-cluster lifecycle; cluster-close forward-notes are load-bearing decisions; phantom-deps npm-audit fix + probe threshold gap; finer-grained ~5-1-1 alternation as the steady-state maintenance complement to 30-ship batched cleanup (#10430)",
    "canonical_docs": [
      "docs/counter-cadence-discipline.md"
    ],
    "key_lessons": ["#10168", "#10169", "#10170", "#10196", "#10203", "#10205", "#10208", "#10265", "#10266", "#10430"],
    "codified_at_milestone": "v1.49.654 (new domain — codified from v1.49.585-651 lesson cluster); finer-cadence pattern appended v1.49.805 (S7 lever)"
  },
  {
    "domain": "Ledger-driven work",
    "trigger": "Before executing any REVIEW ledger entry or planning artifact written as a sweep summary; when authoring audit rules and audit verdicts",
    "summary": "Per-file recon (~15min) precedes per-file code (verify scope, framing, prior-art). Audit rules classify by BEHAVIOR (e.g. `node:fs` import presence), not by filename pattern. Interface-conformance audits report a 3-way verdict: ALREADY CONFORMS / SHOULD CONFORM / INTENTIONALLY DIFFERENT (third verdict is load-bearing — prevents future re-flagging). REVIEW ledger fields distinguish filename-collisions (rename-scale) from class-name-collisions (merge-scale).",
    "canonical_docs": [
      "docs/ledger-driven-work-discipline.md"
    ],
    "key_lessons": ["#10409", "#10410", "#10411", "#10412", "#10413"],
    "codified_at_milestone": "v1.49.784 (new domain — codified from v1.49.780-782 lesson cluster)"
  },
  {
    "domain": "Architecture-retrofit patterns",
    "trigger": "Introducing a chokepoint, interface, or generator-template to N existing modules with high call-site multiplicity; authoring a generator that may run on partial input; adding the SECOND instance of a class-typed family",
    "summary": "Optional `ctx?` parameter pattern for chokepoint retrofit — three states (undefined→legacy permissive; defaultLoaderContext()→audit-only; restricted→enforced); zero call-site churn. Tolerant generators emit skip-the-line over `UNKNOWN`/`TODO`/placeholder sentinels — placeholders pollute output, break round-trip, and mask the schema gap. Exception: emit the default when the field has a load-bearing default value. Cross-class registries extract at the SECOND class instance (not first, not third) — the contrast between two real instances determines the registry shape; deferring to the third class ships a temporary wrong-source measure that has to be unwound.",
    "canonical_docs": [
      "docs/architecture-retrofit-patterns.md"
    ],
    "key_lessons": ["#10414", "#10416", "#10426"],
    "codified_at_milestone": "v1.49.784 (new domain — codified from v1.49.782 LoaderContext + v1.49.783 normalizer fix lesson cluster); v1.49.802 (extended with #10426 second-instance cross-class registry extraction from T1.1 arc)"
  },
  {
    "domain": "Deferred-maintenance escalation",
    "trigger": "When a handoff lists a fix as 'escalated' or 'load-bearing'; when a test in `tests/integration/` has been red across 2+ consecutive ships; when operator memory accumulates workarounds for a known broken tool",
    "summary": "Close escalated wedges within 1-2 milestones; do not defer beyond that. The cost of deferral is silent test drift + workaround documentation accumulation, not the eventual fix. Forward-cadence engine-state work can wait one milestone; an open red test in `tests/integration/` cannot. The pre-tag-gate's silent acceptance of a known-failing test is the alarm.",
    "canonical_docs": [
      "docs/deferred-maintenance-discipline.md"
    ],
    "key_lessons": ["#10415"],
    "codified_at_milestone": "v1.49.784 (new domain — codified from v1.49.783 STATE.md normalizer fix)"
  },
  {
    "domain": "Static-analysis tool authoring",
    "trigger": "Authoring or extending a CLI tool in `tools/` that scans the codebase, emits metrics, or produces a comparable report; designing its test harness; piping its output to another command",
    "summary": "Test harnesses use spawnSync (not execSync) so stderr survives exit 0; cross-module analyzers scan multiple importer roots (not just src/); metric-emitting tools commit a baseline file so future runs diff via git; CLIs flush stdout before process.exit() to avoid 64KB pipe-buffer truncation; diff-emitting observability tools document the warm-up period (first run has no prior baseline); version-stamped baseline writers refuse to overwrite existing files with different content unless --force (v794 closure of v791 trip). #10421 field-validated at v789 by adoption-baseline diff emitting exactly the predicted line.",
    "canonical_docs": [
      "docs/static-analysis-tool-discipline.md"
    ],
    "key_lessons": ["#10417", "#10418", "#10419", "#10420", "#10421", "#10424"],
    "codified_at_milestone": "v1.49.790 (new domain — codified from v1.49.785-789 adoption-telemetry build chain lesson cluster); v1.49.794 (extended with #10424 refuse-to-overwrite guard)"
  },
  {
    "domain": "Shelfware verdict patterns",
    "trigger": "Authoring a per-module shelfware verdict (WIRED / RETIRED / ALLOWLISTED) for a module flagged by the adoption-scan as test-only; selecting the wire site that satisfies a WIRED verdict",
    "summary": "Observability surface (scanner + dashboard + allowlist) and decision surface (per-module verdicts) MUST live in separate files and evolve independently. Default to the lightest wire that flips the adoption-scan classification — a CLI subcommand usually suffices and preserves HARD-preservation invariants — defer richer in-loop wires to follow-on ships only if the verdict explicitly requires production use.",
    "canonical_docs": [
      "docs/shelfware-verdict-patterns.md",
      "docs/SHELFWARE-VERDICTS.md"
    ],
    "key_lessons": ["#10422", "#10423"],
    "codified_at_milestone": "v1.49.790 (new domain — codified from v1.49.789 first shelfware verdict lesson cluster)"
  },
  {
    "domain": "Bounded-learning calibration",
    "trigger": "Designing or extending the bounded-learning calibration loop in `src/bounded-learning/`; choosing the statistical primitive for an anytime-valid threshold-update decision; adding a new calibratable threshold class",
    "summary": "Two-sided likelihood-ratio e-processes on bounded binary observations at |x|=1 are insensitive to unanimous direction (cosh(λ)·exp(−λ²/2) ≤ 1 for all λ ≥ 0); use two one-sided e-processes at Bonferroni α/2 instead. Math-check-before-test-fixture convention: compute E_n/E_{n-1} under unanimous evidence by hand before writing the trip-point assertion — if the ratio is ≤ 1 the construction is unfit. Per-class observation sources are necessary surface; the lightest-wire discipline does NOT apply to necessary surface (cross-ref Architecture-retrofit patterns #10426).",
    "canonical_docs": [
      "docs/bounded-learning-calibration-discipline.md"
    ],
    "key_lessons": ["#10425"],
    "codified_at_milestone": "v1.49.802 (new domain — codified from v1.49.795 T1.1 ship 1 e-process design lesson)"
  },
  {
    "domain": "Failure-mode contracts",
    "trigger": "Designing a new surface (function, module, CLI flag, skill prompt section, hook) where silent-vs-loud failure asymmetry is in play; adding a `try { ... } catch { /* ignore */ }` block; reviewing a long-running primitive's transient-error handling; adding a subscriber-gated observability hook on a context/options type",
    "summary": "Forensic, dashboard, and observability surfaces fail silently; load-bearing surfaces fail loudly. Test: does the user's next decision depend on this surface's output? If yes → load-bearing → propagate. If no → accessory → swallow at surface boundary. Contract documentation convention: explicit `// best-effort silent` or `// fail loudly` marker in docstring paired with at least one test assertion that exercises the failure path; surfaces without the paired test are stochastic. Subscriber-gated observability-only context-hook pattern (#10437): substrate-consumer hooks recur in a 5-element shape (optional `?` field + subscriber gate + fire-and-forget Promise + .catch(() => {}) swallow + two-argument derived-data signature). PAIR co-location (when multiple hooks live on the same surface) shares one try/catch to prevent per-hook drift.",
    "canonical_docs": [
      "docs/failure-mode-contracts.md"
    ],
    "key_lessons": ["#10427", "#10437"],
    "codified_at_milestone": "v1.49.802 (new domain — codified from v1.49.799-801 three-instance audit-log + watch-loop + /sc:status lesson cluster); v1.49.840 (extended with #10437 subscriber-gated context-hook pattern from v810+v826 onPredictions + v830+v832 fallbackProvider four-instance evidence set + PAIR co-location refinement)"
  },
  {
    "domain": "Meta-cadence",
    "trigger": "Planning a counter-cadence cleanup-mission; deciding which operational debt to spend a non-degree-advancing ship on; auditing whether the project's three operational axes (codify / consume / calibrate) are in balance",
    "summary": "Three operational axes — codify (~7-10 ships), consume (≤6 ships from substrate to first non-test caller), calibrate (~10 ships per wired threshold tick). Cadence-overdue check is prose-only and informational: each axis names its overdue trigger; the operator picks; the next counter-cadence's scope discussion NAMES the proposed axis even if a different axis wins on operator priority. The naming is the load-bearing discipline; the choice is operator-bounded.",
    "canonical_docs": [
      "docs/meta-cadence-discipline.md"
    ],
    "key_lessons": ["#10428"],
    "codified_at_milestone": "v1.49.805 (new domain — codified from S3 lever in 2026-05-26 core-functions audit retrospective)"
  },
  {
    "domain": "Substrate opt-in paths",
    "trigger": "Authoring docs that surface opt-in flags to operators; reviewing whether a new substrate ship needs a public-surface entry; deciding what a new operator's second-30-minutes path should look like",
    "summary": "Per-substrate four-field shape: what it unlocks / what it costs / opt-in mechanic / when to defer. Public surface (this doc) is the WHY-truth source; `docs/MODULE-DEFAULTS.md` is the flag-truth source. Add new entries below their axis grouping (observability / learning / execution / safety) in ship order. Retire entries only on codebase removal, not on default-flip (note inline). Cross-link to the canonical flag-truth doc; do NOT duplicate flag tables.",
    "canonical_docs": [
      "docs/SUBSTRATE-OPT-IN-PATHS.md"
    ],
    "key_lessons": ["#10429"],
    "codified_at_milestone": "v1.49.805 (new domain — codified from S4 lever in 2026-05-26 core-functions audit retrospective)"
  },
  {
    "domain": "Security chokepoints",
    "trigger": "Adding a new src/ caller that reads from disk, issues a network request, or spawns a child process; reviewing whether a new module needs to thread an existing `ctx?` parameter; migrating a KNOWN_UNWIRED entry from one of the audit-test allowlists",
    "summary": "Three sibling chokepoints — LoaderContext (filesystem, v782), EgressContext (network egress, v806), ProcessContext (child-process spawn, v806) — share a common shape (optional `ctx?` param, allow-list pattern, audit sink, `*ContextDenied` error) but specialize their targets. Each chokepoint's audit-test (`src/security/*-context-audit.test.ts`) is the runtime enforcement: greps src/ for the surface's signature, accepts either `ensure*Allowed(` calls or `Role: NOT a <surface> caller` docstring exemption, grandfathered existing call sites tracked in KNOWN_UNWIRED. Security denials are load-bearing per #10427 — hoist `ensure*Allowed(` OUTSIDE try/catch blocks that swallow errors. Internal-helper pattern (#10433): when a file already wraps the side-effecting op in an internal helper, thread `ctx?` through the helper for 1 LOC × N callsites; files without a helper take N LOC × N callsites.",
    "canonical_docs": [
      "docs/security-chokepoints.md",
      "src/security/loader-context.ts",
      "src/security/egress-context.ts",
      "src/security/process-context.ts"
    ],
    "key_lessons": ["#10414", "#10426", "#10427", "#10433"],
    "codified_at_milestone": "v1.49.806 (new domain — codified from S6 lever in 2026-05-26 core-functions audit retrospective; extends #10414 LoaderContext pattern to network egress + child-process spawn surfaces); v1.49.824 (extended with #10433 internal-helper pattern for ctx? threading from v809 + v820 case studies)"
  },
  {
    "domain": "Two-layer closure for procedure-rooted drift",
    "trigger": "Designing a closure for a drift class that originates from a multi-step operator procedure OR from a tool blindly overwriting destination files that may contain hand-authored content; reviewing whether a one-layer fix (detector OR source-eliminator alone) is structurally complete; planning a counter-cadence ship targeting an operational-debt class",
    "summary": "Procedure-rooted drift classes (where an operator-discretion step is forgettable/skippable) require BOTH a source eliminator (deterministic tool replacing the discretion step + post-condition check) AND a detector gate (structural check catching bypass). Either layer alone is incomplete: source-without-detector lets future operators work around the tool; detector-without-source leaves the human-error window open. Case study: STATE.md v805→v806 drift closed in v807 (detector: pre-tag-gate step 0.5) + v813 (source: atomic-writer `tools/state-md-set-shipped.mjs`). Generalizes to file-overwrite drift (#10436): v836 publish.mjs `shouldPublishToDestination` is the destination-side preservation layer paired with chapter.mjs's source-side preservation (from v585 C04). The manual recovery procedure operators worked around for 251 ships (`git checkout HEAD -- <chapters>`) was the alarm bell for a missing destination-side detector. Apply at every counter-cadence ship targeting a known procedure-rooted or file-overwrite drift class.",
    "canonical_docs": [
      "docs/two-layer-closure-discipline.md",
      "tools/state-md-set-shipped.mjs",
      "tools/pre-tag-gate.sh",
      "tools/release-history/publish.mjs",
      "tools/release-history/chapter.mjs",
      "docs/T14-SHIP-SEQUENCE.md"
    ],
    "key_lessons": ["#10431", "#10436"],
    "codified_at_milestone": "v1.49.814 (new domain — codified from v807+v813 case study; extends #10414 gate-not-vigilance to procedure-rooted drift specifically); v1.49.840 (extended with #10436 file-overwrite drift sub-class from v813 STATE.md + v836 publish.mjs two-instance evidence)"
  },
  {
    "domain": "KNOWN_UNWIRED allowlists as migration-debt ledger",
    "trigger": "Introducing a new audit-test that enforces a cross-cutting invariant on src/ files with grandfathered call sites; reviewing how to grandfather existing call sites without permanently exempting them; planning chip-down ships against an existing allowlist; consolidating allowlist entries when N siblings are all wired",
    "summary": "When a chokepoint or cross-cutting audit-test is introduced into a mature codebase, it MUST grandfather existing call sites via a named allowlist in the audit-test itself. The allowlist is debt, not exemption — should asymptote toward zero as chip ships consume it. Per-ship release notes record `<surface> KNOWN_UNWIRED N → N-K`. Established pattern at v806 (initial 16 egress + 38 process allowlist entries) + v809+v811+v812 (3 chip ships demonstrating the migration cadence). Forward observation: the v806 audit-test enforcement is unidirectional (catches missing wires but not stale allowlist entries); inverse check is a future enhancement. Generalization beyond chokepoints (#10434): the same ratchet-ledger shape works for any cross-cutting observability+enforcement surface where the current state has N enumerated out-of-conformance entries and each is independently chip-able — v821 discipline-coverage ceiling generalizes the chokepoint KNOWN_UNWIRED pattern to per-discipline UNCODIFIED counts.",
    "canonical_docs": [
      "docs/known-unwired-ledger-discipline.md",
      "src/security/egress-context-audit.test.ts",
      "src/security/process-context-audit.test.ts",
      "src/security/loader-context-audit.test.ts"
    ],
    "key_lessons": ["#10432", "#10434"],
    "codified_at_milestone": "v1.49.814 (new domain — codified from v806 introduction + v809+v811+v812 chip-ship demonstration; companion to Security chokepoints discipline); v1.49.824 (extended with #10434 ratchet-ledger generalization from v821 discipline-coverage ceiling case study)"
  },
  {
    "domain": "Cross-rootdir wire pattern",
    "trigger": "Authoring a wire between src/ and .college/ (or any future rootdir pair) where the two rootdirs cannot import each other directly; declaring duck-typed interfaces in one rootdir + satisfying structurally in the other; verifying the wire works end-to-end at the application boundary",
    "summary": "Cross-rootdir wires follow a 4-step pattern: (1) declare the interface in the CONSUMER rootdir with minimal primitive field types; (2) implement the satisfier in the PROVIDER rootdir via duck typing (no `implements` keyword; no cross-import); (3) bridge via an optional context field that's no-op when unset; (4) verify with a `tests/integration/*.integration.test.ts` test that has visibility into BOTH rootdirs. Two subordinate patterns: local-interface redeclaration discipline (when the satisfier RETURNS a typed value, the provider rootdir must locally redeclare the result type byte-equivalently — drift between the two declarations is silent), and `Pick<T, K>` structural-handle narrowing (when constructor opts accept concrete classes, type them as `Pick<>` so tests can pass thin mocks without a cast while real instances still flow through subtyping).",
    "canonical_docs": [
      "docs/cross-rootdir-wire-discipline.md",
      "src/predictive-skill-loader/fallback.ts",
      ".college/integration/rosetta-concept-fallback.ts",
      "tests/integration/copper-rosetta-fallback-wire.integration.test.ts",
      ".college/integration/observation-bridge.ts",
      "tests/integration/college-observation-bridge-wire.integration.test.ts"
    ],
    "key_lessons": ["#10435"],
    "codified_at_milestone": "v1.49.833 (new domain — codified from 5-instance evidence set v823 SkillActivationObserver declaration + v829 application-boundary verification + v830 ConceptFallbackProvider framework + v831 RosettaConceptFallback implementation + v832 selector 2nd-caller wire + tests/integration/ verification)"
  }
]
