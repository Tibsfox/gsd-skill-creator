---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .college/rosetta-core/types.ts
  - .college/rosetta-core/types.test.ts
autonomous: true
requirements:
  - CORE-05

must_haves:
  truths:
    - "All shared types compile with npx tsc --noEmit and zero errors"
    - "RosettaConcept, PanelExpression, CalibrationDelta, CollegeDepartment, SafetyBoundary, PanelId, and CalibrationModel interfaces are exported and importable"
    - "PanelId type includes all 9 panel identifiers plus 'natural'"
    - "SafetyBoundary type enforces 'absolute' | 'warning' distinction"
  artifacts:
    - path: ".college/rosetta-core/types.ts"
      provides: "All shared type definitions for Rosetta Core, Calibration, College, and Safety"
      exports:
        - "RosettaConcept"
        - "PanelExpression"
        - "CalibrationDelta"
        - "CollegeDepartment"
        - "SafetyBoundary"
        - "PanelId"
        - "CalibrationModel"
        - "ConceptRelationship"
        - "CalibrationProfile"
        - "ComplexPosition"
        - "DepartmentWing"
        - "TrySession"
        - "TokenBudgetConfig"
      min_lines: 80
    - path: ".college/rosetta-core/types.test.ts"
      provides: "Type compilation smoke tests"
      min_lines: 30
  key_links:
    - from: ".college/rosetta-core/types.ts"
      to: "all downstream .college/ modules"
      via: "import { RosettaConcept, PanelExpression, ... } from '../rosetta-core/types'"
      pattern: "export (interface|type)"
---

<objective>
Create all shared type definitions for the Rosetta Core, Calibration Engine, College Structure, and Safety systems. These types form the contract that every downstream component in Waves 1-4 builds against.

Purpose: Establish the canonical type definitions so all parallel tracks (Rosetta Core, Calibration Engine, Panels, College) can begin implementation without coordination overhead.
Output: `.college/rosetta-core/types.ts` with all shared interfaces and types exported, plus a smoke test proving compilation.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/staging/cooking-with-claude/01-milestone-spec.md

Note: The milestone spec (01-milestone-spec.md) contains the exact type definitions in the "Shared Types" section. Use those as the canonical source. The tsconfig.json uses ES2022 target, NodeNext module resolution, strict mode. The `.college/` directory does not exist yet -- create it.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared type definitions</name>
  <files>.college/rosetta-core/types.ts</files>
  <behavior>
    - All interfaces from milestone spec compile: RosettaConcept, PanelExpression, CalibrationDelta, CollegeDepartment, CalibrationModel, SafetyBoundary
    - PanelId type includes all 10 panel identifiers: 'python' | 'cpp' | 'java' | 'lisp' | 'pascal' | 'fortran' | 'perl' | 'algol' | 'unison' | 'natural'
    - PanelId includes 'vhdl' from milestone spec for future extensibility
    - Supporting types exist: ConceptRelationship, CalibrationProfile, ComplexPosition, DepartmentWing, TrySession, TokenBudgetConfig
    - SafetyBoundary.type is 'absolute' | 'warning' (absolute = never override, warning = flag only)
    - CalibrationDelta.confidence is typed as number (0-1 range, documented in JSDoc)
    - CalibrationDelta.adjustment is Record<string, number>
    - RosettaConcept.panels uses Map<PanelId, PanelExpression>
    - CollegeDepartment includes wings, concepts, calibrationModels, trySessions, tokenBudget fields
  </behavior>
  <action>
Create `.college/rosetta-core/types.ts` with all shared type definitions. Use the milestone spec's "Shared Types" section as the canonical source, with these additions:

1. **PanelId type** -- extend to include all 9 initial panels plus 'natural' and 'vhdl':
   ```typescript
   export type PanelId = 'python' | 'cpp' | 'java' | 'lisp' | 'pascal' | 'fortran' | 'perl' | 'algol' | 'unison' | 'vhdl' | 'natural';
   ```

2. **Core interfaces** (from milestone spec verbatim):
   - `RosettaConcept` -- canonical concept with id, name, domain, description, panels (Map), relationships, calibration, complexPlanePosition
   - `PanelExpression` -- panel-specific expression with panelId, code, explanation, examples, pedagogicalNotes
   - `CalibrationDelta` -- observed/expected/adjustment/confidence/domainModel/timestamp
   - `CollegeDepartment` -- id, name, wings, concepts, calibrationModels, trySessions, tokenBudget
   - `CalibrationModel` -- domain, parameters, science, safetyBoundaries
   - `SafetyBoundary` -- parameter, limit, type ('absolute'|'warning'), reason

3. **Supporting types** (not in milestone spec, but required by interfaces):
   - `ConceptRelationship` -- type ('dependency'|'analogy'|'cross-reference'), targetId, description
   - `CalibrationProfile` -- domain, deltas (CalibrationDelta[]), confidenceScore, lastUpdated
   - `ComplexPosition` -- real (number), imaginary (number), magnitude (number), angle (number)
   - `DepartmentWing` -- id, name, description, concepts (string[] of concept IDs)
   - `TrySession` -- id, name, description, entryPoint, estimatedDuration
   - `TokenBudgetConfig` -- summaryLimit, activeLimit, deepLimit (all numbers in tokens)

4. **JSDoc comments** on every interface and every field. Include the purpose and constraints (e.g., "confidence: 0-1 range" on CalibrationDelta).

5. **Export everything** -- every interface, type, and type alias must be exported.

Do NOT include any implementation code, classes, or functions. This file is pure type definitions.
  </action>
  <verify>
    <automated>cd /path/to/projectTibsfox-dev/gsd-skill-creator && npx tsc --noEmit .college/rosetta-core/types.ts --strict --target ES2022 --module NodeNext --moduleResolution NodeNext</automated>
  </verify>
  <done>All shared types compile with zero errors. Every interface from the milestone spec is present and exported. PanelId covers all 9+2 panel identifiers. Supporting types fill gaps in the milestone spec's interface references.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create type compilation smoke test</name>
  <files>.college/rosetta-core/types.test.ts</files>
  <behavior>
    - Test imports all exported types successfully
    - Test creates a valid RosettaConcept object (type-checks)
    - Test creates a valid CalibrationDelta object (type-checks)
    - Test creates a valid CollegeDepartment object (type-checks)
    - Test creates a valid SafetyBoundary with type 'absolute' (type-checks)
    - Test verifies PanelId union accepts 'python', rejects invalid string (compile-time check via satisfies)
  </behavior>
  <action>
Create `.college/rosetta-core/types.test.ts` using Vitest. This is a compilation smoke test -- it verifies that the type definitions are correct and usable, not runtime behavior.

```typescript
import { describe, it, expect } from 'vitest';
import type {
  RosettaConcept, PanelExpression, CalibrationDelta,
  CollegeDepartment, SafetyBoundary, PanelId, CalibrationModel,
  ConceptRelationship, CalibrationProfile, ComplexPosition,
  DepartmentWing, TrySession, TokenBudgetConfig
} from './types.js';
```

Tests to write:
1. **"shared types are importable"** -- import all types, assert they are defined (typeof checks)
2. **"RosettaConcept can be constructed"** -- create a literal object satisfying the interface, verify key fields
3. **"CalibrationDelta enforces structure"** -- create a literal with all required fields
4. **"SafetyBoundary distinguishes absolute from warning"** -- create one of each type, verify the type field
5. **"CollegeDepartment includes all required fields"** -- construct a minimal valid department
6. **"PanelId accepts valid panel identifiers"** -- test 'python', 'lisp', 'natural' as valid PanelId values

Each test creates a typed object literal and uses `expect` assertions on key properties to verify the types compile and the structure is correct.

Use `satisfies` operator where possible for compile-time type verification:
```typescript
const concept = { ... } satisfies RosettaConcept;
```
  </action>
  <verify>
    <automated>cd /path/to/projectTibsfox-dev/gsd-skill-creator && npx vitest run .college/rosetta-core/types.test.ts --reporter=verbose</automated>
  </verify>
  <done>All smoke tests pass. Every exported type is importable and constructible with valid data. Types enforce the expected structure at compile time.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit .college/rosetta-core/types.ts` exits with code 0
- `npx vitest run .college/rosetta-core/types.test.ts` shows all tests passing
- All 13 type exports are present in types.ts (grep for `export`)
- No implementation code (no functions, no classes) in types.ts
</verification>

<success_criteria>
1. All shared types from the milestone spec compile with zero errors under strict TypeScript
2. Every type is exported and importable by downstream modules
3. Smoke tests prove types are constructible with valid data
4. PanelId includes all 11 panel identifiers (9 initial + vhdl + natural)
5. JSDoc comments document every interface and field
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
