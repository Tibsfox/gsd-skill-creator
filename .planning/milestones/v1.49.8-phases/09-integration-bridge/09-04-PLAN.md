---
phase: 09-integration-bridge
plan: 04
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - .college/integration/index.ts
  - .college/integration/integration.test.ts
autonomous: true
requirements:
  - INTG-01
  - INTG-02
  - INTG-03

must_haves:
  truths:
    - "Exploring a cooking concept triggers pattern detection via ObservationBridge"
    - "Panel loading at every tier stays within the 2-5% token budget ceiling"
    - "Chipset adapter routes a panel request to the correct specialist engine"
    - "All three adapters work together in a single integration flow"
    - "Barrel export provides clean API for downstream consumers"
  artifacts:
    - path: ".college/integration/index.ts"
      provides: "Barrel export for all integration bridge modules"
      exports: ["ObservationBridge", "TokenBudgetAdapter", "ChipsetAdapter"]
    - path: ".college/integration/integration.test.ts"
      provides: "Integration tests proving all three adapters work together"
      min_lines: 60
  key_links:
    - from: ".college/integration/index.ts"
      to: ".college/integration/observation-bridge.ts"
      via: "re-exports ObservationBridge and types"
      pattern: "export.*ObservationBridge"
    - from: ".college/integration/index.ts"
      to: ".college/integration/token-budget-adapter.ts"
      via: "re-exports TokenBudgetAdapter and types"
      pattern: "export.*TokenBudgetAdapter"
    - from: ".college/integration/index.ts"
      to: ".college/integration/chipset-adapter.ts"
      via: "re-exports ChipsetAdapter and types"
      pattern: "export.*ChipsetAdapter"
    - from: ".college/integration/integration.test.ts"
      to: ".college/integration/observation-bridge.ts"
      via: "uses ObservationBridge in integration flow"
      pattern: "ObservationBridge"
---

<objective>
Create the barrel export for the integration bridge and write integration tests proving all three adapters (ObservationBridge, TokenBudgetAdapter, ChipsetAdapter) work together end-to-end.

Purpose: Verify the three Phase 9 success criteria hold when the adapters interact: (1) exploration triggers pattern detection, (2) loading respects token budgets, (3) chipset routing works correctly. The barrel export provides a clean import path for downstream consumers.

Output: Barrel index.ts and integration test file proving the full bridge works.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-integration-bridge/09-01-SUMMARY.md
@.planning/phases/09-integration-bridge/09-02-SUMMARY.md
@.planning/phases/09-integration-bridge/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Barrel export for integration bridge</name>
  <files>.college/integration/index.ts</files>
  <action>
    Replace the stub at `.college/integration/index.ts` (it may not exist as a proper barrel yet) with a barrel export that re-exports all public APIs from the three adapter modules:

    ```typescript
    // Observation Bridge
    export { ObservationBridge } from './observation-bridge.js';
    export type { CollegeObservationEvent, ObservationBridgeConfig } from './observation-bridge.js';

    // Token Budget Adapter
    export { TokenBudgetAdapter } from './token-budget-adapter.js';
    export type { BudgetEnforcementResult, ContextWindowConfig } from './token-budget-adapter.js';

    // Chipset Adapter
    export { ChipsetAdapter } from './chipset-adapter.js';
    export type { PanelChipsetMapping, ChipsetAdapterConfig, PanelRouteResult, EngineResolver } from './chipset-adapter.js';
    ```

    Adjust the exact type names based on what was created in plans 01-03. Read each adapter file first to get the actual exported names.
  </action>
  <verify>
    <automated>cd /path/to/projectTibsfox-dev/gsd-skill-creator && npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <done>Barrel export compiles without errors and re-exports all three adapters</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for all three adapters</name>
  <files>.college/integration/integration.test.ts</files>
  <action>
    Create integration tests at `.college/integration/integration.test.ts` that verify the three Phase 9 success criteria working together:

    **Test 1: Exploration triggers observation event (INTG-01)**
    - Create an ObservationBridge with a test config
    - Create a mock ExplorationResult for a cooking concept (e.g., `{path: 'culinary-arts/food-science/maillard-reaction', concept: {id: 'maillard-reaction', ...}, departmentId: 'culinary-arts', ...}`)
    - Call `bridge.onExploration(result)`
    - Assert an event was emitted with correct conceptId
    - Call `bridge.flush()` and convert to SessionObservation
    - Assert the SessionObservation has topFiles containing the concept path and activeSkills containing 'college'

    **Test 2: Budget enforcement across tiers (INTG-02)**
    - Create a TokenBudgetAdapter wrapping a mock CollegeLoader
    - Mock CollegeLoader.loadSummary() returning tokenCost of 3000 (within 2% of 200K = 4000)
    - Assert loadSummary result has truncated=false
    - Mock CollegeLoader.loadWing() returning tokenCost of 8000 (exceeds 3% of 200K = 6000)
    - Assert loadWing result has truncated=true and tokenCost <= 6000
    - Mock CollegeLoader.loadDeep() returning tokenCost of 12000 (exceeds 5% of 200K = 10000)
    - Assert loadDeep result has truncated=true and tokenCost <= 10000

    **Test 3: Chipset routing for panels (INTG-03)**
    - Create a ChipsetAdapter with a mock EngineResolver
    - Route 'python' panel -> assert domain is 'context'
    - Route 'lisp' panel -> assert domain is 'output'
    - Route 'unison' panel -> assert domain is 'io'
    - Route 'natural' panel -> assert domain is 'glue'

    **Test 4: End-to-end flow**
    - Simulate: user explores a concept -> ObservationBridge emits event -> TokenBudgetAdapter confirms budget -> ChipsetAdapter routes the panel
    - Create all three adapters
    - Call observation bridge with a mock exploration result
    - Assert event emitted
    - Call token budget adapter to load the concept's wing
    - Assert within budget
    - Call chipset adapter to route the concept's panel
    - Assert correct engine

    Import from `./index.js` barrel to verify the barrel export works.
  </action>
  <verify>
    <automated>cd /path/to/projectTibsfox-dev/gsd-skill-creator && npx vitest run .college/integration/integration.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Integration tests pass proving all three success criteria: exploration triggers pattern detection, loading respects budgets, chipset routes correctly</done>
</task>

</tasks>

<verification>
- `npx vitest run .college/integration/` passes all tests (unit + integration)
- `npx tsc --noEmit` reports no type errors
- Barrel export provides clean imports for ObservationBridge, TokenBudgetAdapter, ChipsetAdapter
- All three Phase 9 success criteria verified in integration test
</verification>

<success_criteria>
- Barrel index.ts compiles and re-exports all three adapters
- Integration test verifies: exploration triggers observation, loading respects budgets, chipset routes correctly
- All tests pass with zero failures
- End-to-end flow demonstrates the three adapters working together
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-bridge/09-04-SUMMARY.md`
</output>
