---
phase: 06-heritage-panels
plan: 06
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
  - 06-05
files_modified:
  - .college/panels/heritage-panels.integration.test.ts
autonomous: true
requirements:
  - PANEL-04
  - PANEL-05
  - PANEL-06
  - PANEL-07
  - PANEL-08
  - PANEL-09

must_haves:
  truths:
    - "All 6 heritage/frontier panels register in PanelRegistry without conflict"
    - "All 6 panels can translate the same concept (exponential-decay) and each produces distinct, non-empty, panel-specific output"
    - "Each panel's pedagogical annotations are non-empty and contain panel-specific teaching content"
    - "Token cost estimates are within reasonable bounds (< 5000 chars per panel at active depth)"
    - "Cross-panel concept rendering produces different code but equivalent mathematical expressions"
    - "PAN-12 (all panels have pedagogical notes) passes for all 6 panels"
    - "PAN-13 (token cost within 20% of actual) passes for all 6 panels"
  artifacts:
    - path: ".college/panels/heritage-panels.integration.test.ts"
      provides: "Integration tests for all 6 heritage/frontier panels working together"
      min_lines: 150
  key_links:
    - from: ".college/panels/heritage-panels.integration.test.ts"
      to: ".college/panels/lisp-panel.ts"
      via: "import { LispPanel }"
      pattern: "import.*LispPanel"
    - from: ".college/panels/heritage-panels.integration.test.ts"
      to: ".college/panels/pascal-panel.ts"
      via: "import { PascalPanel }"
      pattern: "import.*PascalPanel"
    - from: ".college/panels/heritage-panels.integration.test.ts"
      to: ".college/panels/fortran-panel.ts"
      via: "import { FortranPanel }"
      pattern: "import.*FortranPanel"
    - from: ".college/panels/heritage-panels.integration.test.ts"
      to: ".college/panels/perl-panel.ts"
      via: "import { PerlPanel }"
      pattern: "import.*PerlPanel"
    - from: ".college/panels/heritage-panels.integration.test.ts"
      to: ".college/panels/algol-panel.ts"
      via: "import { AlgolPanel }"
      pattern: "import.*AlgolPanel"
    - from: ".college/panels/heritage-panels.integration.test.ts"
      to: ".college/panels/unison-panel.ts"
      via: "import { UnisonPanel }"
      pattern: "import.*UnisonPanel"
    - from: ".college/panels/heritage-panels.integration.test.ts"
      to: ".college/panels/panel-interface.ts"
      via: "import { PanelRegistry }"
      pattern: "import.*PanelRegistry"
---

<objective>
Create integration tests verifying all 6 heritage/frontier panels work together — registering in PanelRegistry, translating the same concept across all panels, and maintaining pedagogical annotation quality.

Purpose: This is the verification gate for Phase 6. It proves that all six panels (Lisp, Pascal, Fortran, Perl, ALGOL, Unison) implement the PanelInterface contract correctly, register without conflict, produce distinct panel-specific output for the same concept, and include the pedagogical annotations that make each panel distinctive. Covers PAN-12 (all panels have pedagogical notes), PAN-13 (token cost estimates), and PAN-14 (cross-reference links valid).

Output: Integration test file covering cross-panel verification.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-heritage-panels/06-01-SUMMARY.md
@.planning/phases/06-heritage-panels/06-02-SUMMARY.md
@.planning/phases/06-heritage-panels/06-03-SUMMARY.md
@.planning/phases/06-heritage-panels/06-04-SUMMARY.md
@.planning/phases/06-heritage-panels/06-05-SUMMARY.md
@.college/panels/panel-interface.ts
@.college/rosetta-core/types.ts

<interfaces>
<!-- Key types the executor needs for integration tests -->

From .college/panels/panel-interface.ts:
```typescript
export abstract class PanelInterface {
  abstract readonly panelId: PanelId;
  abstract readonly name: string;
  abstract readonly description: string;
  abstract translate(concept: RosettaConcept): PanelExpression;
  abstract getCapabilities(): PanelCapabilities;
  abstract formatExpression(expression: PanelExpression): string;
}

export class PanelRegistry {
  register(panel: PanelInterface): void;
  get(id: PanelId): PanelInterface | undefined;
  getAll(): PanelInterface[];
  has(id: PanelId): boolean;
}
```

Panel exports expected from plans 01-05:
```typescript
// From .college/panels/lisp-panel.ts
export class LispPanel extends PanelInterface { ... }

// From .college/panels/pascal-panel.ts
export class PascalPanel extends PanelInterface { ... }

// From .college/panels/fortran-panel.ts
export class FortranPanel extends PanelInterface { ... }

// From .college/panels/perl-panel.ts
export class PerlPanel extends PanelInterface { ... }

// From .college/panels/algol-panel.ts
export class AlgolPanel extends PanelInterface { ... }

// From .college/panels/unison-panel.ts
export class UnisonPanel extends PanelInterface { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Heritage Panels Integration Tests</name>
  <files>.college/panels/heritage-panels.integration.test.ts</files>
  <behavior>
    - Test 1 (Registry): All 6 panels register in a fresh PanelRegistry without error. Registry has() returns true for all 6 IDs: 'lisp', 'pascal', 'fortran', 'perl', 'algol', 'unison'.
    - Test 2 (Cross-panel translation): All 6 panels translate the same exponential-decay RosettaConcept. Each returns a non-empty PanelExpression with non-empty code. No two panels produce identical code.
    - Test 3 (PAN-12 pedagogical notes): Every panel's translate() output has non-empty pedagogicalNotes. Each panel's notes mention its distinctive pedagogical focus (Lisp: homoiconicity, Pascal: Wirth, Fortran: scientific computing, Perl: regex/closures/POD, ALGOL: BNF/block structure, Unison: content-addressed).
    - Test 4 (PAN-13 token cost): For each panel, the formatExpression() output length divided by 4 (chars-per-token estimate) is within 20% of a reasonable bound. No panel exceeds 5000 tokens for active-depth output.
    - Test 5 (PAN-14 cross-references valid): Panels that include cross-reference annotations (ALGOL->Pascal, ALGOL->C++, Perl->Lisp, Unison->Concept Registry) reference valid panel IDs or concept domains.
    - Test 6 (capabilities consistency): Every panel's getCapabilities() returns hasPedagogicalNotes=true and hasCodeGeneration=true. Each panel has at least one supportedDomain.
    - Test 7 (panelId uniqueness): All 6 panels have distinct panelId values — no duplicates.
    - Test 8 (formatExpression non-empty): formatExpression() for each panel's translate() output returns a non-empty string.
    - Test 9 (distinctive features unique): getDistinctiveFeature() (if present) returns panel-specific content — no two panels return identical distinctive features for the same concept.
  </behavior>
  <action>
    Create `.college/panels/heritage-panels.integration.test.ts`:

    ```typescript
    import { describe, it, expect, beforeAll } from 'vitest';
    import { PanelRegistry } from './panel-interface.js';
    import { LispPanel } from './lisp-panel.js';
    import { PascalPanel } from './pascal-panel.js';
    import { FortranPanel } from './fortran-panel.js';
    import { PerlPanel } from './perl-panel.js';
    import { AlgolPanel } from './algol-panel.js';
    import { UnisonPanel } from './unison-panel.js';
    import type { RosettaConcept, PanelId } from '../rosetta-core/types.js';
    ```

    Create shared test fixtures:
    - `exponentialDecayConcept`: A RosettaConcept with id 'exponential-decay', domain 'mathematics', description about Newton's cooling law
    - `allPanels`: Array of instantiated panels [LispPanel, PascalPanel, FortranPanel, PerlPanel, AlgolPanel, UnisonPanel]
    - `panelIds`: Expected IDs ['lisp', 'pascal', 'fortran', 'perl', 'algol', 'unison']

    Test groups:
    1. **Registry Integration**: Register all 6, verify has(), getAll() length, no duplicate errors
    2. **Cross-Panel Translation**: translate() the same concept on all 6, verify distinct non-empty output
    3. **Pedagogical Annotation Verification**: Each panel's notes contain its distinctive keyword
    4. **Token Cost Bounds**: formatExpression output within reasonable size
    5. **Cross-Reference Validity**: Panel annotations reference valid targets
    6. **Capability Consistency**: All panels report correct capabilities

    The test concept for exponential-decay:
    ```typescript
    const testConcept: RosettaConcept = {
      id: 'exponential-decay',
      name: 'Exponential Decay',
      domain: 'mathematics',
      description: 'T(t) = T_ambient + (T_initial - T_ambient) * e^(-kt) — Newton\'s law of cooling',
      panels: new Map(),
      relationships: [
        { type: 'cross-reference', targetId: 'newtons-cooling-law', description: 'Applied form in cooking/thermodynamics' },
        { type: 'analogy', targetId: 'radioactive-decay', description: 'Same mathematical structure in physics' },
      ],
    };
    ```
  </action>
  <verify>
    <automated>npx vitest run .college/panels/heritage-panels.integration.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    All 9 integration test groups pass:
    - 6 panels register without conflict
    - Same concept translates to 6 distinct outputs
    - All pedagogical notes contain panel-specific keywords
    - Token costs within bounds
    - Cross-references valid
    - Capabilities consistent
    - PAN-12, PAN-13, PAN-14 test criteria covered
  </done>
</task>

<task type="auto">
  <name>Task 2: Full test suite verification and TypeScript compilation check</name>
  <files></files>
  <action>
    Run the complete panel test suite to verify no regressions:

    1. Run `npx tsc --noEmit` to verify all 6 panel files + integration test compile without TypeScript errors
    2. Run `npx vitest run .college/panels/ --reporter=verbose` to run ALL panel tests (existing panel-interface tests + all 6 new panel tests + integration tests)
    3. Verify that the existing panel-interface.test.ts still passes (no regression)
    4. Count total passing tests across all panel test files

    If any test fails, diagnose and fix before completing.
  </action>
  <verify>
    <automated>npx vitest run .college/panels/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All panel TypeScript files compile without errors (npx tsc --noEmit succeeds)
    - All panel tests pass: existing panel-interface tests + 6 new panel tests + integration tests
    - Zero regressions in existing tests
    - Total test count across all panel files is reported
  </done>
</task>

</tasks>

<verification>
```bash
# Full verification sequence
npx tsc --noEmit && npx vitest run .college/panels/ --reporter=verbose
```

Verify:
1. TypeScript compilation succeeds for all panel files
2. All individual panel tests pass (plans 01-05)
3. Integration tests pass (this plan)
4. Existing panel-interface.test.ts passes (no regression)
5. Test count covers PAN-04 through PAN-14 from the test plan
</verification>

<success_criteria>
- All 6 heritage/frontier panels register in PanelRegistry without conflict
- Same concept (exponential-decay) renders distinctly across all 6 panels
- Every panel has non-empty pedagogical notes with panel-specific teaching content
- Token costs within bounds (< 5000 tokens at active depth)
- Cross-reference annotations reference valid targets
- All TypeScript files compile without errors
- Zero regressions in existing panel-interface tests
- Phase 6 success criteria are demonstrably met:
  1. Lisp panel: concept IS manipulable data structure (homoiconicity)
  2. Pascal panel: Wirth's principles encoded in annotations
  3. Fortran panel: authentic scientific computing idioms
  4. Perl panel: regex + closures + POD in single example
  5. ALGOL panel: BNF + three-syntax; Unison panel: content-addressed + hash identity
</success_criteria>

<output>
After completion, create `.planning/phases/06-heritage-panels/06-06-SUMMARY.md`
</output>
