---
phase: 06-heritage-panels
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - .college/panels/lisp-panel.ts
  - .college/panels/lisp-panel.test.ts
autonomous: true
requirements:
  - PANEL-04

must_haves:
  truths:
    - "Lisp panel implements PanelInterface with panelId 'lisp' and registers without error"
    - "Lisp panel translate() returns a PanelExpression where the code IS a manipulable S-expression data structure — not a string describing Lisp, but actual nested list notation"
    - "Lisp panel demonstrates homoiconicity: the concept definition can be quoted, car'd, cdr'd, and consed — it IS data"
    - "Lisp panel includes macro system annotation showing concept composition via defmacro"
    - "Lisp panel pedagogicalNotes teach code-as-data and the Rosetta principle in its purest form"
    - "Lisp panel getCapabilities() reports functional paradigm with hasPedagogicalNotes true"
    - "Lisp panel formatExpression() produces formatted S-expression output with pedagogical annotations"
  artifacts:
    - path: ".college/panels/lisp-panel.ts"
      provides: "LispPanel class extending PanelInterface"
      exports:
        - "LispPanel"
      min_lines: 120
    - path: ".college/panels/lisp-panel.test.ts"
      provides: "Tests for homoiconicity, S-expression structure, macro annotations, PanelInterface contract"
      min_lines: 100
  key_links:
    - from: ".college/panels/lisp-panel.ts"
      to: ".college/panels/panel-interface.ts"
      via: "extends PanelInterface"
      pattern: "extends PanelInterface"
    - from: ".college/panels/lisp-panel.ts"
      to: ".college/rosetta-core/types.ts"
      via: "import { RosettaConcept, PanelExpression, PanelId }"
      pattern: "import.*from.*types\\.js"
---

<objective>
Implement the Lisp panel — the panel where concept definitions ARE manipulable data structures, demonstrating homoiconicity as the purest form of the Rosetta principle.

Purpose: PANEL-04 requires that the Lisp panel doesn't just generate Lisp code — it produces concept definitions that are themselves S-expression data structures. A concept expressed through the Lisp panel can be quoted, decomposed with car/cdr, and composed with cons/append. This is code-as-data made tangible.

Output: `LispPanel` class implementing `PanelInterface` + comprehensive tests proving homoiconicity.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/staging/cooking-with-claude/03-04-panel-calibration-specs.md
@.college/panels/panel-interface.ts
@.college/rosetta-core/types.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From .college/panels/panel-interface.ts:
```typescript
export abstract class PanelInterface {
  abstract readonly panelId: PanelId;
  abstract readonly name: string;
  abstract readonly description: string;
  abstract translate(concept: RosettaConcept): PanelExpression;
  abstract getCapabilities(): PanelCapabilities;
  abstract formatExpression(expression: PanelExpression): string;
}

export interface PanelCapabilities {
  supportedDomains: string[];
  mathLibraries: string[];
  hasCodeGeneration: boolean;
  hasPedagogicalNotes: boolean;
  expressionFormats: string[];
}
```

From .college/rosetta-core/types.ts:
```typescript
export type PanelId = 'lisp' | 'pascal' | 'fortran' | 'perl' | 'algol' | 'unison' | ...;

export interface PanelExpression {
  panelId: PanelId;
  code?: string;
  explanation?: string;
  examples?: string[];
  pedagogicalNotes?: string;
}

export interface RosettaConcept {
  id: string;
  name: string;
  domain: string;
  description: string;
  panels: Map<PanelId, PanelExpression>;
  relationships: ConceptRelationship[];
  calibration?: CalibrationProfile;
  complexPlanePosition?: ComplexPosition;
}
```
</interfaces>
</context>

<tasks>

<feature>
  <name>Lisp Panel: Homoiconic Concept Expression</name>
  <files>.college/panels/lisp-panel.ts, .college/panels/lisp-panel.test.ts</files>
  <behavior>
    - Test 1 (PAN-09 / homoiconicity): translate() for exponential-decay concept returns PanelExpression where `code` contains a `(defun ...)` or `(define ...)` S-expression that IS a quoted list — the definition itself is data. The code string must contain nested parentheses forming valid S-expression structure, not just a textual description.
    - Test 2 (macro composition): translate() includes in examples a `(defmacro ...)` showing how the concept can be composed — e.g., `(defmacro with-decay (params &body body) ...)` demonstrating concept composition via macro expansion.
    - Test 3 (S-expression structure): The `code` field contains S-expressions that demonstrate car/cdr decomposability — e.g., `(quote (concept "exponential-decay" (lambda (t) ...)))` where the concept definition itself is a quoted list that can be inspected.
    - Test 4 (pedagogicalNotes): pedagogicalNotes contains teaching content about homoiconicity, code-as-data, and the connection to the Rosetta principle. Must mention "homoiconicity" and "data structure".
    - Test 5 (PanelInterface contract): panelId is 'lisp', getCapabilities() returns functional paradigm, supportedDomains includes 'mathematics', hasPedagogicalNotes is true, hasCodeGeneration is true.
    - Test 6 (formatExpression): formatExpression() produces indented S-expression output with ;; comment annotations explaining the pedagogical significance.
    - Test 7 (PAN-04): For exponential decay concept, the code includes `(exp ...)` producing the correct mathematical form `T(t) = T_ambient + (T_initial - T_ambient) * e^(-k*t)`.
    - Test 8 (token cost): Token cost estimate for the expression is within reasonable bounds (code + explanation + notes < 5000 chars for active depth).
    - Test 9 (distinctive feature): getDistinctiveFeature() for any concept returns a string mentioning "homoiconicity" or "code is data" or "manipulable data structure".
  </behavior>
  <implementation>
    Create `LispPanel` class extending `PanelInterface`:

    ```typescript
    export class LispPanel extends PanelInterface {
      readonly panelId: PanelId = 'lisp';
      readonly name = 'Lisp Panel';
      readonly description = 'Code as data (homoiconicity). In Lisp, a concept definition IS a data structure you can inspect, transform, and compose. This is the Rosetta principle in its purest form.';
    ```

    **translate() implementation:**
    - Build the concept as an S-expression data structure using `(define-concept ...)` form
    - Wrap mathematical expressions in `(lambda ...)` forms
    - For exponential decay: `(define-concept "exponential-decay" :domain "mathematics" :formula (lambda (t t-initial t-ambient k) (+ t-ambient (* (- t-initial t-ambient) (exp (- (* k t))))))`
    - Include `(defmacro ...)` in examples showing concept composition
    - Include a quoted form `(quote ...)` demonstrating the definition IS inspectable data
    - Set pedagogicalNotes explaining homoiconicity: the concept definition you just read IS a list. You can `(car concept)` to get the head, `(cdr concept)` to get the rest. You can `(eval concept)` to execute it. Code IS data IS curriculum.

    **getCapabilities():**
    - supportedDomains: ['mathematics', 'computer-science', 'formal-languages']
    - mathLibraries: ['common-lisp-math', 'scheme-math']
    - hasCodeGeneration: true
    - hasPedagogicalNotes: true
    - expressionFormats: ['code', 'explanation', 'example']

    **formatExpression():**
    - Indent S-expressions with 2-space nesting
    - Add ;; pedagogical comment annotations
    - Include the code, then explanation, then examples

    **Helper: getDistinctiveFeature(concept):**
    - Always returns a string about homoiconicity — that the concept definition IS a manipulable list, not just code that runs

    The critical insight: the `code` field in PanelExpression must contain S-expressions where the CONCEPT DEFINITION ITSELF is a data structure. Not just Lisp code that computes — but a quoted list that represents the concept and can be programmatically manipulated. This is what makes the Lisp panel unique.
  </implementation>
</feature>

</tasks>

<verification>
```bash
npx vitest run .college/panels/lisp-panel.test.ts --reporter=verbose
```

Verify:
1. All tests pass (homoiconicity, macro composition, S-expression structure, pedagogical notes, contract)
2. The `code` field contains valid S-expression syntax with nested parens
3. PAN-09 (homoiconicity) and PAN-04 (exponential decay accuracy) criteria are met
4. Token cost of generated expressions stays reasonable (< 5000 chars at active depth)
</verification>

<success_criteria>
- LispPanel extends PanelInterface and compiles without errors
- translate() returns PanelExpression where code IS an S-expression data structure (not just Lisp code)
- The concept definition is demonstrably homoiconic: it includes quoted forms showing the definition as inspectable data
- Macro example shows concept composition via defmacro
- pedagogicalNotes teach homoiconicity and the code-as-data principle
- Exponential decay renders with correct mathematical form using (exp ...)
- All test IDs PAN-04 and PAN-09 from the test plan are covered
</success_criteria>

<output>
After completion, create `.planning/phases/06-heritage-panels/06-01-SUMMARY.md`
</output>
