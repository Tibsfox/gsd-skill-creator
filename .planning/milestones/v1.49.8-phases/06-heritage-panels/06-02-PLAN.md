---
phase: 06-heritage-panels
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - .college/panels/pascal-panel.ts
  - .college/panels/pascal-panel.test.ts
  - .college/panels/fortran-panel.ts
  - .college/panels/fortran-panel.test.ts
autonomous: true
requirements:
  - PANEL-05
  - PANEL-06

must_haves:
  truths:
    - "Pascal panel annotations encode Wirth's principles — a reader learns structured programming by exploring the code"
    - "Pascal panel produces code with explicit begin/end, typed variables, procedure decomposition, and pedagogical annotations linking each construct to Wirth's design philosophy"
    - "Fortran panel expresses array operations and numerical methods using authentic scientific computing idioms"
    - "Fortran panel uses REAL precision, DO loops, array notation, and scientific notation that would be recognizable to a Fortran programmer"
    - "Both panels implement PanelInterface and compile without errors"
    - "Both panels produce mathematically correct exponential decay expressions"
  artifacts:
    - path: ".college/panels/pascal-panel.ts"
      provides: "PascalPanel class extending PanelInterface"
      exports:
        - "PascalPanel"
      min_lines: 100
    - path: ".college/panels/pascal-panel.test.ts"
      provides: "Tests for Wirth's principles, structured programming annotations, PanelInterface contract"
      min_lines: 80
    - path: ".college/panels/fortran-panel.ts"
      provides: "FortranPanel class extending PanelInterface"
      exports:
        - "FortranPanel"
      min_lines: 100
    - path: ".college/panels/fortran-panel.test.ts"
      provides: "Tests for scientific computing idioms, array operations, REAL precision, PanelInterface contract"
      min_lines: 80
  key_links:
    - from: ".college/panels/pascal-panel.ts"
      to: ".college/panels/panel-interface.ts"
      via: "extends PanelInterface"
      pattern: "extends PanelInterface"
    - from: ".college/panels/fortran-panel.ts"
      to: ".college/panels/panel-interface.ts"
      via: "extends PanelInterface"
      pattern: "extends PanelInterface"
    - from: ".college/panels/pascal-panel.ts"
      to: ".college/rosetta-core/types.ts"
      via: "import types"
      pattern: "import.*from.*types\\.js"
    - from: ".college/panels/fortran-panel.ts"
      to: ".college/rosetta-core/types.ts"
      via: "import types"
      pattern: "import.*from.*types\\.js"
---

<objective>
Implement the Pascal and Fortran heritage panels — Pascal encoding Wirth's structured programming principles in every annotation, and Fortran expressing scientific computing heritage through authentic array operations and numerical methods.

Purpose: Pascal teaches structured thinking by making every construct explicit and principled. Fortran teaches scientific computing heritage — mathematical expressions that look like their textbook form, connecting to sixty years of computational science. Together they represent the pedagogical and scientific branches of the heritage panel family.

Output: `PascalPanel` and `FortranPanel` classes implementing `PanelInterface` + comprehensive tests for both.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/staging/cooking-with-claude/03-04-panel-calibration-specs.md
@.college/panels/panel-interface.ts
@.college/rosetta-core/types.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From .college/panels/panel-interface.ts:
```typescript
export abstract class PanelInterface {
  abstract readonly panelId: PanelId;
  abstract readonly name: string;
  abstract readonly description: string;
  abstract translate(concept: RosettaConcept): PanelExpression;
  abstract getCapabilities(): PanelCapabilities;
  abstract formatExpression(expression: PanelExpression): string;
}

export interface PanelCapabilities {
  supportedDomains: string[];
  mathLibraries: string[];
  hasCodeGeneration: boolean;
  hasPedagogicalNotes: boolean;
  expressionFormats: string[];
}
```

From .college/rosetta-core/types.ts:
```typescript
export interface PanelExpression {
  panelId: PanelId;
  code?: string;
  explanation?: string;
  examples?: string[];
  pedagogicalNotes?: string;
}

export interface RosettaConcept {
  id: string;
  name: string;
  domain: string;
  description: string;
  panels: Map<PanelId, PanelExpression>;
  relationships: ConceptRelationship[];
}
```
</interfaces>
</context>

<tasks>

<feature>
  <name>Pascal Panel: Wirth's Structured Programming Principles</name>
  <files>.college/panels/pascal-panel.ts, .college/panels/pascal-panel.test.ts</files>
  <behavior>
    - Test 1 (PAN-10 / structured programming): translate() for exponential-decay produces Pascal code with proper `program`, `var` declarations with explicit types, `begin...end` blocks, and `function` decomposition.
    - Test 2 (Wirth's principles in annotations): pedagogicalNotes contains teaching about at least 3 of Wirth's principles: simplicity, explicit typing, procedure decomposition, data abstraction, stepwise refinement. Must mention "Wirth" by name.
    - Test 3 (begin/end structure): The `code` field contains `begin` and `end` keywords with proper Pascal structure — not braces, not indentation-based.
    - Test 4 (typed variables): The code declares variables with explicit Pascal types: `var T_initial, T_ambient, k: Real;` — strong typing is visible and pedagogically annotated.
    - Test 5 (PAN-05 / exponential decay accuracy): Pascal code uses `Exp()` function for exponential decay with correct mathematical form.
    - Test 6 (PanelInterface contract): panelId is 'pascal', getCapabilities() returns procedural paradigm, supportedDomains includes 'mathematics', hasPedagogicalNotes is true.
    - Test 7 (distinctive feature): getDistinctiveFeature() returns a string mentioning "structured programming" or "Wirth" or "discipline" or "clarity".
    - Test 8 (formatExpression): formatExpression() produces indented Pascal with { comment } annotations explaining Wirth's design choices.
  </behavior>
  <implementation>
    Create `PascalPanel` class extending `PanelInterface`:

    ```typescript
    export class PascalPanel extends PanelInterface {
      readonly panelId: PanelId = 'pascal';
      readonly name = 'Pascal Panel';
      readonly description = 'Structured thinking. Wirth designed Pascal to make the learner understand. Every construct has explicit boundaries. Every variable has a declared type. The discipline produces clarity.';
    ```

    **translate() implementation:**
    - Generate Pascal code with `program ConceptName;` header
    - Declare all variables with explicit types in `var` section
    - Use `function` for mathematical expressions with typed parameters and return type
    - Wrap in `begin...end.` blocks
    - For exponential decay:
      ```pascal
      program ExponentialDecay;
      { Wirth's Principle: Every program has a name. Every name has meaning. }
      var
        T_initial, T_ambient, k, t, result: Real;
        { Wirth's Principle: Every variable is declared with its type.
          The declaration IS the documentation. }

      function CoolingCurve(t, T_init, T_amb, k: Real): Real;
      { Wirth's Principle: Functions decompose complex operations into
        named, typed, self-documenting units. }
      begin
        CoolingCurve := T_amb + (T_init - T_amb) * Exp(-k * t)
      end;

      begin
        { ... }
      end.
      ```
    - Set pedagogicalNotes explaining Wirth's design philosophy: Pascal was designed to teach. Every construct — begin/end, typed declarations, procedure decomposition — exists to make the programmer's intent explicit and verifiable.

    **getCapabilities():**
    - supportedDomains: ['mathematics', 'computer-science', 'education']
    - mathLibraries: ['system-math'] (Pascal's built-in Exp, Sin, Cos, Ln, Sqrt)
    - hasCodeGeneration: true
    - hasPedagogicalNotes: true
    - expressionFormats: ['code', 'explanation', 'example']
  </implementation>
</feature>

<feature>
  <name>Fortran Panel: Scientific Computing Heritage</name>
  <files>.college/panels/fortran-panel.ts, .college/panels/fortran-panel.test.ts</files>
  <behavior>
    - Test 1 (PAN-11 / scientific notation): translate() for exponential-decay produces Fortran code with `REAL` declarations and correct `EXP()` function using scientific computing idioms.
    - Test 2 (array operations): For array-oriented concepts, Fortran code uses array notation (`DIMENSION`, array slicing, implied DO loops) rather than element-by-element iteration.
    - Test 3 (REAL precision): Code declares precision explicitly with `REAL(KIND=8)` or `DOUBLE PRECISION` — authentic Fortran idiom for scientific computing.
    - Test 4 (DO loops): Code uses `DO` loops (not `for`/`while`) with authentic Fortran syntax.
    - Test 5 (FORmula TRANslation): pedagogicalNotes explain that Fortran was designed for FORmula TRANslation — mathematical expressions in Fortran look almost exactly like their textbook form. Must mention the name origin.
    - Test 6 (PAN-06 / exponential decay): Fortran code uses `EXP()` intrinsic with correct mathematical form for Newton's cooling law.
    - Test 7 (PanelInterface contract): panelId is 'fortran', getCapabilities() returns imperative paradigm with scientific domains, hasPedagogicalNotes is true.
    - Test 8 (distinctive feature): getDistinctiveFeature() returns a string mentioning "scientific computing" or "formula translation" or "numerical methods".
    - Test 9 (formatExpression): formatExpression() produces Fortran with `!` comment annotations and proper indentation.
  </behavior>
  <implementation>
    Create `FortranPanel` class extending `PanelInterface`:

    ```typescript
    export class FortranPanel extends PanelInterface {
      readonly panelId: PanelId = 'fortran';
      readonly name = 'Fortran Panel';
      readonly description = 'Scientific computing heritage. Fortran was designed for FORmula TRANslation — mathematical expressions in Fortran look almost exactly like their textbook form. It connects you to sixty years of computational science.';
    ```

    **translate() implementation:**
    - Generate modern Fortran (free-form, post-Fortran 90) code
    - Use `PROGRAM`, `IMPLICIT NONE`, `REAL(KIND=8)` declarations
    - Use intrinsic math functions: `EXP()`, `SIN()`, `COS()`, `SQRT()`
    - For exponential decay:
      ```fortran
      PROGRAM ExponentialDecay
      ! FORmula TRANslation: the equation IS the code
      IMPLICIT NONE
      REAL(KIND=8) :: T_initial, T_ambient, k, t, T_result
      ! Scientific computing: explicit precision declaration
      ! KIND=8 gives double precision (~15 significant digits)

      REAL(KIND=8) FUNCTION cooling_curve(t, T_init, T_amb, k)
        REAL(KIND=8), INTENT(IN) :: t, T_init, T_amb, k
        ! Newton's cooling law — the formula translates directly
        cooling_curve = T_amb + (T_init - T_amb) * EXP(-k * t)
      END FUNCTION

      END PROGRAM ExponentialDecay
      ```
    - For array concepts, use array operations: `T_values = T_ambient + (T_initial - T_ambient) * EXP(-k * times)`
    - Set pedagogicalNotes explaining Fortran's heritage: designed in 1957 by John Backus at IBM, Fortran is the oldest high-level language still in active use. Its design principle — mathematical formulas should translate directly into code — means Fortran code reads like a textbook equation.

    **getCapabilities():**
    - supportedDomains: ['mathematics', 'physics', 'engineering', 'scientific-computing']
    - mathLibraries: ['intrinsic-math', 'lapack', 'blas']
    - hasCodeGeneration: true
    - hasPedagogicalNotes: true
    - expressionFormats: ['code', 'explanation', 'example']
  </implementation>
</feature>

</tasks>

<verification>
```bash
npx vitest run .college/panels/pascal-panel.test.ts .college/panels/fortran-panel.test.ts --reporter=verbose
```

Verify:
1. All tests pass for both panels
2. Pascal code contains begin/end, typed var declarations, and Wirth annotations
3. Fortran code contains REAL precision, EXP() intrinsics, and scientific idioms
4. PAN-05 (Pascal Exp()), PAN-06 (Fortran EXP()), PAN-10, PAN-11 criteria met
5. Both panels compile via `npx tsc --noEmit` without errors
</verification>

<success_criteria>
- PascalPanel and FortranPanel both extend PanelInterface and compile without errors
- Pascal code encodes Wirth's principles in annotations — a reader learns structured programming by exploring the output
- Pascal uses begin/end blocks, explicit typed declarations, procedure decomposition
- Fortran uses authentic scientific computing idioms: REAL precision, array operations, intrinsic math functions
- Fortran expressions look like their textbook mathematical form (FORmula TRANslation principle)
- Both panels produce correct exponential decay expressions (Exp/EXP)
- All test IDs PAN-05, PAN-06, PAN-10, PAN-11 from the test plan are covered
</success_criteria>

<output>
After completion, create `.planning/phases/06-heritage-panels/06-02-SUMMARY.md`
</output>
