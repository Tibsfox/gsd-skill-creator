---
phase: 10-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - .college/calibration/models/cooking.test.ts
  - .college/calibration/models/mathematics.test.ts
  - .college/integration/observation-bridge.test.ts
  - .college/integration/token-budget-adapter.test.ts
  - .college/integration/chipset-adapter.test.ts
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "Vitest coverage configuration produces a report scoped to .college/ source files"
    - "Coverage gap analysis identifies untested source files and uncovered branches"
    - "Tests exist for calibration models (cooking, mathematics)"
    - "Tests exist for integration modules (observation-bridge, token-budget-adapter, chipset-adapter)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Coverage configuration for .college/ source files"
      contains: "coverage"
    - path: ".college/calibration/models/cooking.test.ts"
      provides: "Tests for cooking calibration models"
      contains: "describe"
    - path: ".college/calibration/models/mathematics.test.ts"
      provides: "Tests for mathematics calibration model"
      contains: "describe"
    - path: ".college/integration/observation-bridge.test.ts"
      provides: "Tests for observation bridge integration module"
      contains: "describe"
    - path: ".college/integration/token-budget-adapter.test.ts"
      provides: "Tests for token budget adapter"
      contains: "describe"
    - path: ".college/integration/chipset-adapter.test.ts"
      provides: "Tests for chipset adapter"
      contains: "describe"
  key_links:
    - from: "vitest.config.ts"
      to: ".college/"
      via: "coverage include glob for .college/ source files"
      pattern: "coverage.*include.*college"
---

<objective>
Configure Vitest coverage reporting for `.college/` source files and fill test coverage gaps for untested modules (calibration models, integration adapters) to lay the foundation for 85%+ coverage.

Purpose: TEST-01 requires 85%+ coverage across all new code. Before writing specialized tests (safety-critical, panel correctness, e2e), we need coverage tooling configured and the most obvious gaps filled. The calibration/models/ directory has stub implementations with pre-written failing tests. The integration/ directory has three stub modules with zero tests.

Output: Updated vitest.config.ts with coverage settings, and 5 new/updated test files covering previously untested modules.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@vitest.config.ts
@.college/calibration/models/cooking.ts
@.college/calibration/models/mathematics.ts
@.college/integration/observation-bridge.ts
@.college/integration/token-budget-adapter.ts
@.college/integration/chipset-adapter.ts

<interfaces>
<!-- From .college/rosetta-core/types.ts -->
```typescript
export interface CalibrationModel {
  domain: string;
  parameters: string[];
  science: string;
  safetyBoundaries: SafetyBoundary[];
}

export interface SafetyBoundary {
  parameter: string;
  limit: number;
  type: 'absolute' | 'warning';
  reason: string;
}

export interface TokenBudgetConfig {
  summaryLimit: number;
  activeLimit: number;
  deepLimit: number;
  panelCeiling: number;
}
```

<!-- From .college/calibration/engine.ts -->
```typescript
export interface DomainCalibrationModel {
  domain: string;
  parameters: string[];
  science: string;
  safetyBoundaries: SafetyBoundary[];
  computeAdjustment(delta: ComparisonDelta): Record<string, number>;
  confidence(delta: ComparisonDelta): number;
}

export interface ComparisonDelta {
  category: string;
  magnitude: number;
  direction?: 'increase' | 'decrease';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Vitest coverage for .college/ source files</name>
  <files>vitest.config.ts</files>
  <action>
    Update vitest.config.ts to add coverage configuration. The project already has @vitest/coverage-v8 in devDependencies.

    Add a `coverage` block inside the `test` object:
    ```typescript
    coverage: {
      provider: 'v8',
      include: ['.college/**/*.ts'],
      exclude: [
        '.college/**/*.test.ts',
        '.college/**/*.integration.test.ts',
        '.college/departments/test-department/**',
      ],
      reporter: ['text', 'text-summary', 'json-summary'],
      reportsDirectory: '.college/coverage',
      thresholds: {
        statements: 85,
        branches: 85,
        functions: 85,
        lines: 85,
      },
    },
    ```

    Add `.college/coverage` to .gitignore if not already there.

    Run `npx vitest run .college/ --coverage` to generate the initial coverage report and identify which files are below 85%. Save the output -- it informs Task 2 about specific uncovered branches.

    NOTE: Do NOT set `thresholds.perFile` to true yet -- that would fail on stub files. The thresholds are aggregate targets.
  </action>
  <verify>
    <automated>cd /path/to/projectTibsfox-dev/gsd-skill-creator && npx vitest run .college/ --coverage 2>&1 | grep -E "^(All files|%.college|Statements|Branches|Functions|Lines|ERROR)" | head -20</automated>
  </verify>
  <done>vitest.config.ts has coverage configuration. Running `npx vitest run .college/ --coverage` produces a coverage report for .college/ source files. Coverage report identifies files below 85%.</done>
</task>

<task type="auto">
  <name>Task 2: Fill coverage gaps for calibration models and integration modules</name>
  <files>
    .college/calibration/models/cooking.test.ts
    .college/calibration/models/mathematics.test.ts
    .college/integration/observation-bridge.test.ts
    .college/integration/token-budget-adapter.test.ts
    .college/integration/chipset-adapter.test.ts
  </files>
  <action>
    **Important context:** The calibration/models/cooking.ts and calibration/models/mathematics.ts are currently stubs (they export nothing). The integration/ modules are also stubs. These stubs will be implemented in Phases 7 and 9 respectively. However, cooking.test.ts already exists with 18 tests that all fail because the module is a stub.

    The goal here is to create test files that either:
    (a) Test what IS exported (if anything), or
    (b) Verify the stub state and add placeholder tests marked with `it.skip()` or `it.todo()` for future implementation, OR
    (c) If the prior phases (7, 8, 9) have already been executed and the modules are implemented, write real tests.

    **For .college/calibration/models/cooking.test.ts:**
    This file already exists with 18 tests. Read it first. If the cooking.ts module now exports real models (temperatureModel, timingModel, seasoningModel, textureModel), the existing tests should pass. If cooking.ts is still a stub, the tests will fail -- that is expected and Phase 7 will fix it. Do NOT modify this test file unless the tests need adjustment to match actual exports.

    **For .college/calibration/models/mathematics.test.ts:**
    Create a test file that tests the mathematics calibration model. Import from `./mathematics.js`. If the module exports a math calibration model, test its domain, parameters, science description, computeAdjustment, confidence, and safetyBoundaries. If the module is a stub (exports nothing), create the test file with `describe` and `it.todo()` tests for the expected interface:
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('Mathematics calibration model', () => {
      it.todo('exports a mathCalibrationModel with domain "mathematics"');
      it.todo('has parameters for abstraction_level, notation_preference, proof_depth');
      it.todo('computeAdjustment returns bounded adjustments');
      it.todo('confidence returns value between 0 and 1');
      it.todo('has no absolute safety boundaries (math has no safety-critical constraints)');
    });
    ```

    **For integration module tests:**
    The three integration modules (observation-bridge.ts, token-budget-adapter.ts, chipset-adapter.ts) are all stubs marked "TODO: Implement in Phase 9". Create test files that:
    1. Verify the module can be imported without error
    2. Add `it.todo()` tests for the expected interface based on the module's doc comments
    3. If Phase 9 has been executed and the modules are implemented, write real tests instead

    For observation-bridge.test.ts:
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('ObservationBridge', () => {
      it('module imports without error', async () => {
        const mod = await import('./observation-bridge.js');
        expect(mod).toBeDefined();
      });
      it.todo('routes concept exploration events to pattern detection');
      it.todo('triggers skill suggestion for repeated concept access');
    });
    ```

    For token-budget-adapter.test.ts:
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('TokenBudgetAdapter', () => {
      it('module imports without error', async () => {
        const mod = await import('./token-budget-adapter.js');
        expect(mod).toBeDefined();
      });
      it.todo('enforces summaryLimit on summary tier loading');
      it.todo('enforces activeLimit on active tier loading');
      it.todo('enforces panelCeiling (5% of context) on panel loading');
      it.todo('SC-10: panel loading never exceeds 5% of context window');
    });
    ```

    For chipset-adapter.test.ts:
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('ChipsetAdapter', () => {
      it('module imports without error', async () => {
        const mod = await import('./chipset-adapter.js');
        expect(mod).toBeDefined();
      });
      it.todo('loads chipset configuration from config file');
      it.todo('routes panel requests to correct specialist agents');
    });
    ```

    The key is that these test files exist and import correctly -- they contribute to coverage of the import paths even for stub modules, and `it.todo()` tests document the expected interface for Phase 9 executors.
  </action>
  <verify>
    <automated>cd /path/to/projectTibsfox-dev/gsd-skill-creator && npx vitest run .college/calibration/models/mathematics.test.ts .college/integration/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Test files exist for all previously untested modules. Import-level tests pass. Todo tests document expected interfaces for stub modules. Running `npx vitest run .college/ --coverage` shows improved coverage numbers.</done>
</task>

</tasks>

<verification>
```bash
cd /path/to/projectTibsfox-dev/gsd-skill-creator && npx vitest run .college/ --coverage 2>&1 | tail -30
```

Coverage report generates. All new test files pass (import tests). Coverage percentage is tracked.

```bash
cd /path/to/projectTibsfox-dev/gsd-skill-creator && npx tsc --noEmit
```
TypeScript compilation passes.
</verification>

<success_criteria>
- vitest.config.ts has coverage configuration scoped to .college/ source files
- Coverage report generates when running `npx vitest run .college/ --coverage`
- Test files exist for calibration/models/mathematics.ts and all three integration/ modules
- All new test files pass (import-level tests succeed even for stubs)
- Coverage gaps are identified and documented
</success_criteria>

<output>
After completion, create `.planning/phases/10-test-suite/10-01-SUMMARY.md`
</output>
