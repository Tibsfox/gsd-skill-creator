---
phase: 10-test-suite
plan: 04
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
  - 10-03
files_modified:
  - .planning/phases/10-test-suite/FOXFOOD-REPORT.md
  - .planning/phases/10-test-suite/COVERAGE-REPORT.md
autonomous: false
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05

must_haves:
  truths:
    - "Coverage report shows 85%+ across .college/ source files"
    - "All 14 safety-critical tests pass with zero failures"
    - "All panel correctness tests pass"
    - "Integration round-trip test passes"
    - "End-to-end flat cookies test passes"
    - "Foxfood report documents using the system to verify itself"
  artifacts:
    - path: ".planning/phases/10-test-suite/COVERAGE-REPORT.md"
      provides: "Coverage report summary with per-directory breakdown"
      contains: "Coverage Report"
      min_lines: 30
    - path: ".planning/phases/10-test-suite/FOXFOOD-REPORT.md"
      provides: "Foxfood report documenting self-verification"
      contains: "Foxfood Report"
      min_lines: 50
  key_links:
    - from: ".planning/phases/10-test-suite/COVERAGE-REPORT.md"
      to: ".college/"
      via: "vitest --coverage output parsed and formatted"
      pattern: "coverage"
---

<objective>
Generate the final coverage report, run the complete test suite to verify all requirements pass, and write the foxfood report documenting how the system was used to build and verify itself.

Purpose: This is the milestone wrap-up. TEST-01 needs the 85%+ coverage verified. All 5 TEST requirements need final confirmation. The foxfood report is the deliverable documenting the self-referential nature of using GSD + skill-creator to build skill-creator's Cooking with Claude feature.

Output: Coverage report, foxfood report, and human verification that all tests pass.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/10-test-suite/10-01-SUMMARY.md
@.planning/phases/10-test-suite/10-02-SUMMARY.md
@.planning/phases/10-test-suite/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate final coverage report and verify 85%+ threshold</name>
  <files>
    .planning/phases/10-test-suite/COVERAGE-REPORT.md
  </files>
  <action>
    Run the complete .college/ test suite with coverage:
    ```bash
    cd /path/to/projectTibsfox-dev/gsd-skill-creator && npx vitest run .college/ --coverage 2>&1
    ```

    Capture the output. Parse the coverage summary to extract:
    - Overall statement coverage %
    - Overall branch coverage %
    - Overall function coverage %
    - Overall line coverage %
    - Per-directory breakdown (calibration/, college/, departments/, integration/, panels/, rosetta-core/, safety/, tests/)
    - Any files below 85%

    Create `.planning/phases/10-test-suite/COVERAGE-REPORT.md` with:

    ```markdown
    # Coverage Report -- Phase 10: Test Suite and Verification

    **Generated:** [date]
    **Target:** 85%+ across all .college/ source files
    **Result:** [PASS/FAIL]

    ## Summary

    | Metric | Coverage | Target | Status |
    |--------|----------|--------|--------|
    | Statements | XX% | 85% | [PASS/FAIL] |
    | Branches | XX% | 85% | [PASS/FAIL] |
    | Functions | XX% | 85% | [PASS/FAIL] |
    | Lines | XX% | 85% | [PASS/FAIL] |

    ## Per-Directory Breakdown

    | Directory | Statements | Branches | Functions | Lines |
    |-----------|-----------|----------|-----------|-------|
    | calibration/ | XX% | XX% | XX% | XX% |
    | college/ | XX% | XX% | XX% | XX% |
    | ... | ... | ... | ... | ... |

    ## Files Below 85%

    [List any files below threshold and why (stub files, integration stubs, etc.)]

    ## Test Suite Summary

    - Total test files: XX
    - Total tests: XX
    - Passing: XX
    - Failing: XX
    - Skipped: XX (with reasons)

    ## Safety-Critical Tests (SC-01 through SC-14)

    | Test ID | Description | Status |
    |---------|-------------|--------|
    | SC-01 | Poultry temp floor 165F | [PASS/SKIP] |
    | ... | ... | ... |
    | SC-14 | DAG enforcement | PASS |
    ```

    If overall coverage is below 85%, identify the specific files/directories pulling it down and document what would be needed to reach 85% (these would be gap closure items). Note that stub files (integration modules, cooking calibration model if Phase 7/9 not yet executed) may pull down coverage -- document this clearly.

    Also run:
    ```bash
    npx vitest run .college/tests/safety-critical.test.ts --reporter=verbose
    ```
    to verify all 14 SC tests individually.

    ```bash
    npx vitest run .college/tests/panel-correctness.test.ts --reporter=verbose
    ```
    to verify all PAN tests.

    ```bash
    npx vitest run .college/tests/flat-cookies-e2e.test.ts --reporter=verbose
    ```
    to verify the e2e test.

    Document all results in the coverage report.
  </action>
  <verify>
    <automated>cd /path/to/projectTibsfox-dev/gsd-skill-creator && npx vitest run .college/ --coverage 2>&1 | grep -E "(Statements|Branches|Functions|Lines|Test Files|Tests)" | head -10</automated>
  </verify>
  <done>COVERAGE-REPORT.md exists with per-directory breakdown, overall percentages documented, all test suite results captured, and any gaps identified.</done>
</task>

<task type="auto">
  <name>Task 2: Write foxfood report</name>
  <files>
    .planning/phases/10-test-suite/FOXFOOD-REPORT.md
  </files>
  <action>
    Create the foxfood report documenting how the Cooking with Claude system was used to build and verify itself. This is the self-referential capstone of the milestone.

    The foxfood report should cover:

    ```markdown
    # Foxfood Report: Cooking with Claude v1.49.8

    **Date:** [date]
    **Milestone:** v1.49.8 Cooking With Claude
    **Author:** Claude (via GSD workflow)

    ## What is Foxfooding?

    Foxfooding (our term for dogfooding) means using the system to build and verify itself. The Cooking with Claude milestone is uniquely suited to this: the GSD workflow that managed the build IS the skill-creator system, and the Calibration Engine pattern (Observe -> Compare -> Adjust -> Record) mirrors the iterative development process itself.

    ## How the System Built Itself

    ### GSD Workflow as Rosetta Core
    The GSD planning workflow translated the same high-level requirements (the "concepts") into different execution contexts (the "panels"):
    - ROADMAP.md = Concept Registry (canonical concept definitions)
    - PLAN.md files = Panel Expressions (concept rendered for specific execution context)
    - SUMMARY.md files = Calibration Deltas (observed outcome vs expected)

    ### Development as Calibration
    Each phase followed the Calibration Engine pattern:
    - **Observe:** Execute a plan, see what happened
    - **Compare:** Check against success criteria (expected vs actual)
    - **Adjust:** Fix gaps, update approach for next plan
    - **Record:** Write SUMMARY.md documenting what was learned

    ### College Structure as Project Organization
    The `.planning/` directory IS a College department:
    - Phases = Wings (grouped by topic)
    - Plans = Concepts (specific knowledge units)
    - SUMMARYs = Progressive disclosure (summary tier of what was built)

    ## Metrics

    ### Build Process
    | Metric | Value |
    |--------|-------|
    | Total phases | 10 |
    | Total plans | [count] |
    | Total execution time | [from STATE.md] |
    | Average plan duration | [from STATE.md] |
    | Parallel execution waves | 5 |

    ### Code Produced
    | Metric | Value |
    |--------|-------|
    | Source files in .college/ | [count] |
    | Test files in .college/ | [count] |
    | Total tests | [count] |
    | Safety-critical tests | 14 |
    | Coverage | [from coverage report] |

    ### Architecture Validation
    | Pillar | Proven By |
    |--------|-----------|
    | Rosetta Core (translation) | 9 panels translating 7+ math concepts across domains |
    | College Structure (knowledge) | 3 departments, 10+ wings, progressive disclosure working |
    | Calibration Engine (feedback) | Full cycle with bounded adjustment, delta persistence, profile synthesis |

    ## Self-Verification Results

    ### Safety-Critical Tests
    [SC-01 through SC-14 results from coverage report]

    ### Panel Correctness
    [PAN-01 through PAN-14 results]

    ### End-to-End
    The "why are my cookies flat?" scenario proves:
    1. **Concept routing works** -- query reaches baking science concepts
    2. **Diagnosis is science-grounded** -- butter ratio, chill time identified
    3. **Calibration records learning** -- delta persisted for future sessions

    ## Lessons Learned

    ### What Worked
    - Wave-based parallel execution reduced total time
    - TDD for core engines (Rosetta, Calibration) caught interface mismatches early
    - Progressive disclosure kept context windows manageable
    - Safety boundaries as non-negotiable absolutes simplified testing

    ### What Could Improve
    - Stub files in later phases reduced coverage metrics
    - Some integration tests depend on Phase 8/9 completion
    - Panel correctness could include runtime execution (not just pattern matching)

    ## Conclusion

    The Cooking with Claude milestone demonstrates that skill-creator's architecture -- Rosetta Core for translation, College for knowledge organization, Calibration Engine for feedback -- is both buildable and self-consistent. The system that manages skills can be managed BY skills. The foxfood is the feast.
    ```

    Fill in actual metrics by reading:
    - `.planning/STATE.md` for build process metrics
    - The COVERAGE-REPORT.md just created for test metrics
    - Count actual files: `find .college -name "*.ts" -not -name "*.test.ts" | wc -l` for source files
    - Count test files: `find .college -name "*.test.ts" -o -name "*.integration.test.ts" | wc -l`
    - Count total tests from the last vitest run output
  </action>
  <verify>
    <automated>test -f /path/to/projectTibsfox-dev/gsd-skill-creator/.planning/phases/10-test-suite/FOXFOOD-REPORT.md && wc -l /path/to/projectTibsfox-dev/gsd-skill-creator/.planning/phases/10-test-suite/FOXFOOD-REPORT.md | awk '{print ($1 >= 50) ? "PASS: " $1 " lines" : "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>FOXFOOD-REPORT.md exists with 50+ lines documenting the self-verification process, actual metrics from the build, and the connection between the system architecture and the build process.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete test suite and reports</name>
  <files>.planning/phases/10-test-suite/COVERAGE-REPORT.md, .planning/phases/10-test-suite/FOXFOOD-REPORT.md</files>
  <action>
    Present the coverage report and foxfood report for human review. The human should verify:
    1. Review coverage report: `cat .planning/phases/10-test-suite/COVERAGE-REPORT.md` -- verify overall coverage meets 85%+ target (or gaps documented with reasons)
    2. Run safety-critical tests: `npx vitest run .college/tests/safety-critical.test.ts --reporter=verbose` -- all 14 SC tests must pass or be cleanly skipped (Phase 8 dependency)
    3. Run panel correctness: `npx vitest run .college/tests/panel-correctness.test.ts --reporter=verbose` -- all PAN tests pass
    4. Run e2e test: `npx vitest run .college/tests/flat-cookies-e2e.test.ts --reporter=verbose` -- flat cookies scenario passes
    5. Run full suite: `npx vitest run .college/ --reporter=verbose` -- zero unexpected failures
    6. Review foxfood report: `cat .planning/phases/10-test-suite/FOXFOOD-REPORT.md` -- contains actual metrics, not placeholders
  </action>
  <verify>
    <automated>cd /path/to/projectTibsfox-dev/gsd-skill-creator && npx vitest run .college/ --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>Human approves coverage report, foxfood report, and overall test suite results. All TEST requirements (TEST-01 through TEST-05) verified. Phase 10 complete.</done>
</task>

</tasks>

<verification>
```bash
cd /path/to/projectTibsfox-dev/gsd-skill-creator && npx vitest run .college/ --coverage --reporter=verbose 2>&1 | tail -50
```

All tests pass. Coverage report generated. Zero safety-critical failures.

```bash
test -f .planning/phases/10-test-suite/COVERAGE-REPORT.md && test -f .planning/phases/10-test-suite/FOXFOOD-REPORT.md && echo "Both reports exist" || echo "Missing reports"
```
</verification>

<success_criteria>
- COVERAGE-REPORT.md documents actual coverage percentages with per-directory breakdown
- Coverage is 85%+ overall OR gaps are documented with clear reasons (stub files from unexecuted phases)
- All 14 SC tests accounted for (pass or cleanly skipped)
- All PAN tests pass
- Integration round-trip passes
- E2E flat cookies passes
- FOXFOOD-REPORT.md contains actual metrics, not placeholders
- Human approves the final verification checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/10-test-suite/10-04-SUMMARY.md`
</output>
