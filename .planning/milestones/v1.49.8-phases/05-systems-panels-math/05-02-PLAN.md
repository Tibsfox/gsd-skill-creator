---
phase: 05-systems-panels-math
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - .college/panels/cpp-panel.ts
  - .college/panels/cpp-panel.test.ts
autonomous: true
requirements:
  - PANEL-02

must_haves:
  truths:
    - "C++ panel implements PanelInterface with panelId 'cpp' and registers without error"
    - "C++ panel translate() returns PanelExpression with code using #include <cmath>, std::exp(), and double precision types"
    - "C++ panel code forces thinking about types, memory, and hardware -- the pedagogical focus is performance and precision"
    - "C++ panel includes pedagogicalNotes teaching what the computer actually does with your math"
    - "C++ panel getCapabilities() reports supportedDomains including 'mathematics' with mathLibraries including 'cmath', 'algorithm'"
    - "C++ panel formatExpression() produces formatted C++ code with // comment annotations"
    - "C++ panel getDistinctiveFeature() mentions performance, precision, or hardware"
  artifacts:
    - path: ".college/panels/cpp-panel.ts"
      provides: "CppPanel class extending PanelInterface"
      exports:
        - "CppPanel"
      min_lines: 150
    - path: ".college/panels/cpp-panel.test.ts"
      provides: "Tests for cmath bindings, type precision, expression templates, pedagogical annotations, PanelInterface contract"
      min_lines: 100
  key_links:
    - from: ".college/panels/cpp-panel.ts"
      to: ".college/panels/panel-interface.ts"
      via: "extends PanelInterface"
      pattern: "extends PanelInterface"
    - from: ".college/panels/cpp-panel.ts"
      to: ".college/rosetta-core/types.ts"
      via: "import { RosettaConcept, PanelExpression, PanelId }"
      pattern: "import.*from.*types\\.js"
---

<objective>
Implement the C++ panel -- the performance and precision panel where code forces thinking about types, memory, and hardware, revealing what the computer actually does with your math.

Purpose: PANEL-02 requires a C++ panel with cmath bindings (std::exp, std::sin), expression templates, and pedagogical annotations explaining how C++ reveals the hardware reality behind mathematical abstractions.

Output: `CppPanel` class implementing `PanelInterface` + comprehensive TDD tests.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/staging/cooking-with-claude/03-04-panel-calibration-specs.md
@.planning/staging/cooking-with-claude/08-math-department-blueprint.md
@.college/panels/panel-interface.ts
@.college/panels/fortran-panel.ts
@.college/rosetta-core/types.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From .college/panels/panel-interface.ts:
```typescript
export abstract class PanelInterface {
  abstract readonly panelId: PanelId;
  abstract readonly name: string;
  abstract readonly description: string;
  abstract translate(concept: RosettaConcept): PanelExpression;
  abstract getCapabilities(): PanelCapabilities;
  abstract formatExpression(expression: PanelExpression): string;
}

export interface PanelCapabilities {
  supportedDomains: string[];
  mathLibraries: string[];
  hasCodeGeneration: boolean;
  hasPedagogicalNotes: boolean;
  expressionFormats: string[];
}

export class PanelRegistry {
  register(panel: PanelInterface): void;
  get(id: PanelId): PanelInterface | undefined;
  has(id: PanelId): boolean;
}
```

From .college/rosetta-core/types.ts:
```typescript
export type PanelId = 'python' | 'cpp' | 'java' | ...;

export interface PanelExpression {
  panelId: PanelId;
  code?: string;
  explanation?: string;
  examples?: string[];
  pedagogicalNotes?: string;
}

export interface RosettaConcept {
  id: string; name: string; domain: string; description: string;
  panels: Map<PanelId, PanelExpression>;
  relationships: ConceptRelationship[];
  complexPlanePosition?: ComplexPosition;
}
```
</interfaces>
</context>

<tasks>

<feature>
  <name>C++ Panel: Performance and Precision</name>
  <files>.college/panels/cpp-panel.ts, .college/panels/cpp-panel.test.ts</files>
  <behavior>
    - Test 1 (PAN-02 / SC-13 / exponential decay): translate() for exponential-decay concept returns PanelExpression where `code` uses `#include <cmath>` and `std::exp()` with correct Newton's cooling law form. Code must contain `double` type declarations enforcing precision.
    - Test 2 (PAN-08 / trig functions): translate() for a trig-functions concept returns code using `std::sin()`, `std::cos()`, `std::tan()` with `<cmath>` include.
    - Test 3 (type system enforcement): Code uses `double` (not `float`) for mathematical precision. Pedagogical annotations explain why `double` vs `float` matters for accumulated error.
    - Test 4 (template/constexpr): Examples include template or constexpr usage showing compile-time computation capability.
    - Test 5 (performance pedagogical focus): pedagogicalNotes explain that C++ forces thinking about types, memory, and hardware. Must mention "precision" or "performance" or "hardware".
    - Test 6 (PanelInterface contract): panelId is 'cpp', getCapabilities() returns supportedDomains including 'mathematics', mathLibraries including 'cmath', hasPedagogicalNotes true, hasCodeGeneration true.
    - Test 7 (formatExpression): formatExpression() produces C++ code with // comment annotations.
    - Test 8 (getDistinctiveFeature): getDistinctiveFeature() returns a string mentioning "performance" or "precision" or "hardware".
    - Test 9 (token cost): Token cost within bounds (< 5000 chars).
    - Test 10 (PanelRegistry): Registers without error.
  </behavior>
  <implementation>
    Create `CppPanel` class extending `PanelInterface`. Follow the exact same structural pattern as the heritage panels:

    ```typescript
    export class CppPanel extends PanelInterface {
      readonly panelId: PanelId = 'cpp';
      readonly name = 'C++ Panel';
      readonly description = 'Performance and precision. C++ forces you to think about types, memory, and hardware -- revealing what the computer actually does with your math.';
    ```

    **translate() implementation:**
    - For exponential-decay: Build C++ code with `#include <cmath>`, `#include <iostream>`, define `double cooling_curve(double t, double T_initial, double T_ambient, double k)` returning `T_ambient + (T_initial - T_ambient) * std::exp(-k * t)`. Include main() with example computation.
    - For trig concepts: Use `std::sin()`, `std::cos()`, `std::tan()` with double precision.
    - Generic fallback: Use `<cmath>` with concept-appropriate functions.
    - Include template example: `template<typename T> T cooling_curve(T t, T T_init, T T_amb, T k)`.
    - Include constexpr example for compile-time math.
    - pedagogicalNotes: "C++ makes you think about what the hardware does... double vs float matters... accumulated floating-point error..."

    **getCapabilities():**
    - supportedDomains: ['mathematics', 'systems-programming', 'scientific-computing', 'performance']
    - mathLibraries: ['cmath', 'algorithm', 'numeric', 'complex']
    - hasCodeGeneration: true, hasPedagogicalNotes: true
    - expressionFormats: ['code', 'explanation', 'example']

    **formatExpression():**
    - C++ style with // comment annotations
    - Header: `// -- C++ Panel: Performance and Precision --`
    - Code section, then explanation as comments, then examples

    **getDistinctiveFeature():**
    - Return string about performance and precision

    Reference the math-department-blueprint.md for exact C++ expressions:
    - Exponential: `N0 * std::exp(-k*t)`
    - Trig: `std::sin(theta), std::cos(theta)`
    - Complex: `std::complex<double>(3, 4)`
    - Ratios: `double ratio = a/b;`
    - Logarithmic: `std::log10(x)`
  </implementation>
</feature>

</tasks>

<verification>
```bash
npx vitest run .college/panels/cpp-panel.test.ts --reporter=verbose
```

Verify:
1. All tests pass (std::exp correctness, cmath includes, double precision, template usage, pedagogical notes, contract)
2. C++ code uses `#include <cmath>` and `std::exp()` with correct cooling law form
3. PAN-02, PAN-08, SC-13 test criteria are met
4. Token cost within bounds (< 5000 chars at active depth)
</verification>

<success_criteria>
- CppPanel extends PanelInterface and compiles without errors
- translate() returns PanelExpression with C++ code using std::exp() and double precision
- translate() returns PanelExpression with std::sin/cos/tan for trig concepts
- Template/constexpr examples demonstrate compile-time capability
- pedagogicalNotes teach performance, precision, and hardware awareness
- All test IDs PAN-02, PAN-08, SC-13 from the test plan are covered
</success_criteria>

<output>
After completion, create `.planning/phases/05-systems-panels-math/05-02-SUMMARY.md`
</output>
