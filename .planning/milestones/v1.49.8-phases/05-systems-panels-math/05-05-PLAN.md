---
phase: 05-systems-panels-math
plan: 05
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
files_modified:
  - .college/panels/systems-panels.integration.test.ts
autonomous: true
requirements:
  - MATH-02

must_haves:
  truths:
    - "All 3 systems panels (Python, C++, Java) register in PanelRegistry without conflict"
    - "All 3 systems panels translate the same math concept and each produces distinct, non-empty, panel-specific output"
    - "Python panel uses math.exp() or numpy for exponential decay -- correct Python math library bindings"
    - "C++ panel uses std::exp() with double precision and #include <cmath> -- correct C++ cmath bindings"
    - "Java panel uses Math.exp() in class structure with typed signatures -- correct Java Math class bindings"
    - "Math concepts from the Mathematics Department render correctly in all three systems panels"
    - "Each panel's pedagogical annotations are non-empty and contain panel-specific teaching content"
    - "Token cost estimates are within reasonable bounds (< 5000 chars per panel at active depth)"
    - "All 9 active panels (3 systems + 6 heritage) can be registered simultaneously without conflict"
  artifacts:
    - path: ".college/panels/systems-panels.integration.test.ts"
      provides: "Integration tests for 3 systems panels + cross-panel math concept rendering"
      min_lines: 200
  key_links:
    - from: ".college/panels/systems-panels.integration.test.ts"
      to: ".college/panels/python-panel.ts"
      via: "import { PythonPanel }"
      pattern: "import.*PythonPanel"
    - from: ".college/panels/systems-panels.integration.test.ts"
      to: ".college/panels/cpp-panel.ts"
      via: "import { CppPanel }"
      pattern: "import.*CppPanel"
    - from: ".college/panels/systems-panels.integration.test.ts"
      to: ".college/panels/java-panel.ts"
      via: "import { JavaPanel }"
      pattern: "import.*JavaPanel"
    - from: ".college/panels/systems-panels.integration.test.ts"
      to: ".college/departments/mathematics/concepts/index.ts"
      via: "import { exponentialDecay, trigFunctions, ... }"
      pattern: "import.*from.*mathematics/concepts"
    - from: ".college/panels/systems-panels.integration.test.ts"
      to: ".college/panels/panel-interface.ts"
      via: "import { PanelRegistry }"
      pattern: "import.*PanelRegistry"
---

<objective>
Create integration tests proving all 3 systems panels render Mathematics Department concepts correctly, and that all 9 panels (3 systems + 6 heritage) coexist in PanelRegistry without conflict.

Purpose: MATH-02 requires that math concepts render correctly in all active panels. This plan proves that each Mathematics Department concept can be translated through all three systems panels (Python, C++, Java) with correct library bindings, and verifies cross-panel coexistence with the 6 heritage panels from Phase 6. This is the verification gate for Phase 5.

Output: Integration test file covering systems panel + math concept cross-rendering.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-systems-panels-math/05-01-SUMMARY.md
@.planning/phases/05-systems-panels-math/05-02-SUMMARY.md
@.planning/phases/05-systems-panels-math/05-03-SUMMARY.md
@.planning/phases/05-systems-panels-math/05-04-SUMMARY.md
@.college/panels/panel-interface.ts
@.college/panels/heritage-panels.integration.test.ts
@.college/rosetta-core/types.ts

<interfaces>
<!-- Key types the executor needs for integration tests -->

From .college/panels/panel-interface.ts:
```typescript
export abstract class PanelInterface {
  abstract readonly panelId: PanelId;
  abstract readonly name: string;
  abstract readonly description: string;
  abstract translate(concept: RosettaConcept): PanelExpression;
  abstract getCapabilities(): PanelCapabilities;
  abstract formatExpression(expression: PanelExpression): string;
}

export class PanelRegistry {
  register(panel: PanelInterface): void;
  get(id: PanelId): PanelInterface | undefined;
  getAll(): PanelInterface[];
  has(id: PanelId): boolean;
}
```

Panel exports expected from plans 01-03:
```typescript
// From .college/panels/python-panel.ts
export class PythonPanel extends PanelInterface { ... }

// From .college/panels/cpp-panel.ts
export class CppPanel extends PanelInterface { ... }

// From .college/panels/java-panel.ts
export class JavaPanel extends PanelInterface { ... }
```

Heritage panel exports (from Phase 6):
```typescript
export class LispPanel extends PanelInterface { ... }
export class PascalPanel extends PanelInterface { ... }
export class FortranPanel extends PanelInterface { ... }
export class PerlPanel extends PanelInterface { ... }
export class AlgolPanel extends PanelInterface { ... }
export class UnisonPanel extends PanelInterface { ... }
```

Math concept exports (from plan 04):
```typescript
export { exponentialDecay } from './exponential-decay.js';
export { trigFunctions } from './trig-functions.js';
export { complexNumbers } from './complex-numbers.js';
export { eulerFormula } from './euler-formula.js';
export { ratiosProportions } from './ratios-proportions.js';
export { logarithmicScales } from './logarithmic-scales.js';
export { fractalGeometry } from './fractal-geometry.js';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Systems Panels + Math Concepts Integration Tests</name>
  <files>.college/panels/systems-panels.integration.test.ts</files>
  <behavior>
    - Test 1 (Registry): All 3 systems panels (python, cpp, java) register in a fresh PanelRegistry without error. Registry has() returns true for all 3 IDs.
    - Test 2 (All 9 panels coexist): All 9 panels (3 systems + 6 heritage) register in PanelRegistry without conflict. Registry getAll() returns length 9.
    - Test 3 (Cross-panel translation -- exponential decay): All 3 systems panels translate the exponentialDecay concept. Each returns non-empty PanelExpression with non-empty code. No two panels produce identical code.
    - Test 4 (Python math library bindings): Python panel's code for exponential decay contains `math.exp` or `numpy`. Python panel's code for trig functions contains `math.sin` or `math.cos`.
    - Test 5 (C++ cmath bindings): C++ panel's code for exponential decay contains `std::exp` and `#include <cmath>` or `cmath`. C++ panel's code uses `double` precision types.
    - Test 6 (Java Math class bindings): Java panel's code for exponential decay contains `Math.exp`. Java panel's code contains `public class` or `public static`.
    - Test 7 (MATH-02 multi-concept rendering): Each of the 7 math concepts renders through all 3 systems panels without throwing. Each rendering produces non-empty code.
    - Test 8 (Pedagogical annotation verification): All 3 panels have non-empty pedagogicalNotes. Python mentions readability/notation, C++ mentions precision/performance/hardware, Java mentions type safety/platform/portable.
    - Test 9 (Token cost bounds): No panel exceeds 5000 tokens (chars/4) for any math concept. All formatted expressions are between 100-20000 chars.
    - Test 10 (panelId uniqueness): All 3 systems panels have distinct panelId values.
    - Test 11 (capabilities): All 3 panels report hasPedagogicalNotes=true, hasCodeGeneration=true, supportedDomains includes 'mathematics'.
    - Test 12 (distinctive features): getDistinctiveFeature() returns panel-specific content -- no two panels return identical features.
  </behavior>
  <action>
    Create `.college/panels/systems-panels.integration.test.ts` following the same pattern as `heritage-panels.integration.test.ts`:

    ```typescript
    import { describe, it, expect, beforeAll } from 'vitest';
    import { PanelInterface, PanelRegistry } from './panel-interface.js';
    import { PythonPanel } from './python-panel.js';
    import { CppPanel } from './cpp-panel.js';
    import { JavaPanel } from './java-panel.js';
    // Heritage panels for 9-panel coexistence test
    import { LispPanel } from './lisp-panel.js';
    import { PascalPanel } from './pascal-panel.js';
    import { FortranPanel } from './fortran-panel.js';
    import { PerlPanel } from './perl-panel.js';
    import { AlgolPanel } from './algol-panel.js';
    import { UnisonPanel } from './unison-panel.js';
    // Math concepts
    import { exponentialDecay, trigFunctions, complexNumbers, eulerFormula,
             ratiosProportions, logarithmicScales, fractalGeometry
    } from '../departments/mathematics/concepts/index.js';
    import type { RosettaConcept, PanelId, PanelExpression } from '../rosetta-core/types.js';
    ```

    Test groups:
    1. **Registry Integration**: Register all 3 systems panels, verify has() for python/cpp/java. Then test all 9 panels coexisting.
    2. **Cross-Panel Translation (exponential decay)**: Translate exponentialDecay concept on all 3, verify distinct non-empty code.
    3. **Library Binding Verification**: Python uses math.exp/numpy, C++ uses std::exp/cmath, Java uses Math.exp.
    4. **MATH-02 Multi-Concept Rendering**: Loop over all 7 math concepts, translate through all 3 panels, verify no throws and non-empty code.
    5. **Pedagogical Annotation Verification**: Each panel's notes contain its distinctive keyword.
    6. **Token Cost Bounds**: formatExpression output within size limits.
    7. **Capability Consistency**: All panels report correct capabilities.
    8. **Distinctive Features**: getDistinctiveFeature() returns unique content per panel.

    Use the actual math concept exports from plan 04 as test data -- real concepts, not mocks. This proves MATH-02 with real data.
  </action>
  <verify>
    <automated>npx vitest run .college/panels/systems-panels.integration.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    All integration tests pass:
    - 3 systems panels register without conflict; all 9 panels coexist
    - exponentialDecay renders distinctly across Python, C++, Java
    - Library bindings verified: math.exp (Python), std::exp (C++), Math.exp (Java)
    - All 7 math concepts render through all 3 panels without error
    - Pedagogical notes contain panel-specific keywords
    - Token costs within bounds
    - MATH-02 requirement proven with real concept data
  </done>
</task>

<task type="auto">
  <name>Task 2: Full test suite verification and TypeScript compilation check</name>
  <files></files>
  <action>
    Run the complete panel and math test suite to verify no regressions:

    1. Run `npx tsc --noEmit` to verify all panel files + concept files + integration tests compile without TypeScript errors
    2. Run `npx vitest run .college/panels/ .college/departments/mathematics/ --reporter=verbose` to run ALL tests (existing heritage panel tests + 3 new systems panel tests + math concept tests + both integration tests)
    3. Verify that the existing heritage-panels.integration.test.ts still passes (no regression)
    4. Verify that the existing panel-interface.test.ts still passes (no regression)
    5. Count total passing tests across all files

    If any test fails, diagnose and fix before completing.
  </action>
  <verify>
    <automated>npx vitest run .college/panels/ .college/departments/mathematics/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All TypeScript files compile without errors (npx tsc --noEmit succeeds)
    - All panel tests pass: existing heritage panel tests + 3 new systems panel tests + both integration tests
    - All math concept tests pass
    - Zero regressions in existing tests
    - Total test count across all files is reported
    - Phase 5 success criteria demonstrably met:
      1. Python, C++, Java panels implement PanelInterface and compile
      2. Math Department has 7 concepts with correct data
      3. Math concepts render correctly in all 3 systems panels
      4. Concepts positioned on Complex Plane with valid values
  </done>
</task>

</tasks>

<verification>
```bash
# Full verification sequence
npx tsc --noEmit && npx vitest run .college/panels/ .college/departments/mathematics/ --reporter=verbose
```

Verify:
1. TypeScript compilation succeeds for all files
2. All individual panel tests pass (systems + heritage)
3. All math concept tests pass
4. Systems panels integration tests pass
5. Heritage panels integration tests still pass (no regression)
6. MATH-02 proven: all 7 math concepts render in all 3 systems panels
</verification>

<success_criteria>
- All 3 systems panels register in PanelRegistry without conflict
- All 9 panels (3 systems + 6 heritage) coexist without conflict
- Exponential decay renders distinctly across Python, C++, Java with correct library bindings
- All 7 math concepts render through all 3 systems panels without error (MATH-02)
- Pedagogical annotations are non-empty and panel-specific
- Token costs within bounds
- Zero regressions in existing tests
- Phase 5 success criteria are demonstrably met
</success_criteria>

<output>
After completion, create `.planning/phases/05-systems-panels-math/05-05-SUMMARY.md`
</output>
