---
phase: 05-systems-panels-math
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - .college/panels/python-panel.ts
  - .college/panels/python-panel.test.ts
autonomous: true
requirements:
  - PANEL-01

must_haves:
  truths:
    - "Python panel implements PanelInterface with panelId 'python' and registers without error"
    - "Python panel translate() returns PanelExpression with code using math.exp(), numpy, and Python math library bindings"
    - "Python panel code reads like mathematical notation -- the pedagogical focus is readability"
    - "Python panel includes pedagogicalNotes teaching why Python is the natural first panel for concept exploration"
    - "Python panel getCapabilities() reports supportedDomains including 'mathematics' with mathLibraries including 'math', 'numpy'"
    - "Python panel formatExpression() produces formatted Python code with # comment annotations"
    - "Python panel getDistinctiveFeature() mentions readability or mathematical notation"
  artifacts:
    - path: ".college/panels/python-panel.ts"
      provides: "PythonPanel class extending PanelInterface"
      exports:
        - "PythonPanel"
      min_lines: 150
    - path: ".college/panels/python-panel.test.ts"
      provides: "Tests for math library bindings, expression templates, pedagogical annotations, PanelInterface contract"
      min_lines: 100
  key_links:
    - from: ".college/panels/python-panel.ts"
      to: ".college/panels/panel-interface.ts"
      via: "extends PanelInterface"
      pattern: "extends PanelInterface"
    - from: ".college/panels/python-panel.ts"
      to: ".college/rosetta-core/types.ts"
      via: "import { RosettaConcept, PanelExpression, PanelId }"
      pattern: "import.*from.*types\\.js"
---

<objective>
Implement the Python panel -- the readability-first panel where code reads almost like mathematical notation, making it the natural first panel for concept exploration.

Purpose: PANEL-01 requires a Python panel with math library bindings (math.exp, numpy), expression templates, and pedagogical annotations explaining why Python's readability makes it the ideal entry point for mathematical concepts.

Output: `PythonPanel` class implementing `PanelInterface` + comprehensive TDD tests.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/staging/cooking-with-claude/03-04-panel-calibration-specs.md
@.planning/staging/cooking-with-claude/08-math-department-blueprint.md
@.college/panels/panel-interface.ts
@.college/panels/lisp-panel.ts
@.college/rosetta-core/types.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From .college/panels/panel-interface.ts:
```typescript
export abstract class PanelInterface {
  abstract readonly panelId: PanelId;
  abstract readonly name: string;
  abstract readonly description: string;
  abstract translate(concept: RosettaConcept): PanelExpression;
  abstract getCapabilities(): PanelCapabilities;
  abstract formatExpression(expression: PanelExpression): string;
}

export interface PanelCapabilities {
  supportedDomains: string[];
  mathLibraries: string[];
  hasCodeGeneration: boolean;
  hasPedagogicalNotes: boolean;
  expressionFormats: string[];
}

export class PanelRegistry {
  register(panel: PanelInterface): void;
  get(id: PanelId): PanelInterface | undefined;
  has(id: PanelId): boolean;
}
```

From .college/rosetta-core/types.ts:
```typescript
export type PanelId = 'python' | 'cpp' | 'java' | 'lisp' | 'pascal' | 'fortran' | 'perl' | 'algol' | 'unison' | 'vhdl' | 'natural';

export interface PanelExpression {
  panelId: PanelId;
  code?: string;
  explanation?: string;
  examples?: string[];
  pedagogicalNotes?: string;
}

export interface RosettaConcept {
  id: string;
  name: string;
  domain: string;
  description: string;
  panels: Map<PanelId, PanelExpression>;
  relationships: ConceptRelationship[];
  calibration?: CalibrationProfile;
  complexPlanePosition?: ComplexPosition;
}
```
</interfaces>
</context>

<tasks>

<feature>
  <name>Python Panel: Readability as Mathematical Notation</name>
  <files>.college/panels/python-panel.ts, .college/panels/python-panel.test.ts</files>
  <behavior>
    - Test 1 (PAN-01 / SC-12 / exponential decay): translate() for exponential-decay concept returns PanelExpression where `code` uses `math.exp()` with correct Newton's cooling law form: `T_ambient + (T_initial - T_ambient) * math.exp(-k * t)`. Code must contain `import math` and use `math.exp`.
    - Test 2 (PAN-07 / trig functions): translate() for a trig-functions concept returns code using `math.sin()`, `math.cos()`, `math.tan()` with correct mathematical form.
    - Test 3 (numpy integration): For array-oriented concepts, translate() output or examples include numpy usage (`import numpy as np`, array operations).
    - Test 4 (readability pedagogical focus): pedagogicalNotes explain that Python code reads like mathematical notation -- the code IS readable math. Must mention "readability" or "mathematical notation".
    - Test 5 (PanelInterface contract): panelId is 'python', getCapabilities() returns supportedDomains including 'mathematics', mathLibraries including 'math' and 'numpy', hasPedagogicalNotes true, hasCodeGeneration true.
    - Test 6 (formatExpression): formatExpression() produces Python code with # comment annotations explaining the pedagogical significance.
    - Test 7 (getDistinctiveFeature): getDistinctiveFeature() returns a string mentioning "readability" or "mathematical notation" or "natural first panel".
    - Test 8 (token cost): Token cost of generated expression within bounds (code + explanation + notes < 5000 chars).
    - Test 9 (PanelRegistry): Registers in PanelRegistry without error.
  </behavior>
  <implementation>
    Create `PythonPanel` class extending `PanelInterface`. Follow the exact same structural pattern as the heritage panels (LispPanel, FortranPanel, etc.):

    ```typescript
    export class PythonPanel extends PanelInterface {
      readonly panelId: PanelId = 'python';
      readonly name = 'Python Panel';
      readonly description = 'Readability and rapid prototyping. Python code reads almost like mathematical notation, making it the natural first panel for concept exploration.';
    ```

    **translate() implementation:**
    - For exponential-decay: Build Python code with `import math`, define `def cooling_curve(t, T_initial, T_ambient, k):` returning `T_ambient + (T_initial - T_ambient) * math.exp(-k * t)`. Include computation example with print statements.
    - For trig concepts: Use `math.sin()`, `math.cos()`, `math.tan()` with unit circle values.
    - Generic fallback: Use `math` module with concept-appropriate functions.
    - Include numpy in examples: `import numpy as np`, `np.linspace()`, vectorized operations.
    - Include explanation comparing code to mathematical notation.
    - Include pedagogicalNotes about readability: "Python code reads almost exactly like the mathematical notation..."

    **getCapabilities():**
    - supportedDomains: ['mathematics', 'data-science', 'scientific-computing', 'education']
    - mathLibraries: ['math', 'numpy', 'scipy', 'matplotlib']
    - hasCodeGeneration: true, hasPedagogicalNotes: true
    - expressionFormats: ['code', 'explanation', 'example']

    **formatExpression():**
    - Python-style with # comment annotations
    - Header: `# -- Python Panel: Readability as Mathematical Notation --`
    - Code section, then explanation as comments, then examples

    **getDistinctiveFeature():**
    - Return string about readability -- Python code reads like math notation

    Reference the math-department-blueprint.md for exact Python expressions:
    - Exponential: `N0 * math.exp(-k*t)`
    - Trig: `math.sin(theta), math.cos(theta)`
    - Complex: `z = complex(3, 4)`, `cmath.exp(1j * theta)`
    - Ratios: `ratio = a/b`
    - Logarithmic: `math.log10(x)`
  </implementation>
</feature>

</tasks>

<verification>
```bash
npx vitest run .college/panels/python-panel.test.ts --reporter=verbose
```

Verify:
1. All tests pass (math.exp correctness, trig functions, numpy integration, readability pedagogical notes, contract)
2. Python code uses `import math` and `math.exp()` with correct cooling law form
3. PAN-01, PAN-07, SC-12 test criteria are met
4. Token cost within bounds (< 5000 chars at active depth)
</verification>

<success_criteria>
- PythonPanel extends PanelInterface and compiles without errors
- translate() returns PanelExpression with Python code using math.exp() for exponential decay
- translate() returns PanelExpression with math.sin/cos/tan for trig concepts
- numpy integration demonstrated in examples
- pedagogicalNotes teach readability as mathematical notation
- All test IDs PAN-01, PAN-07, SC-12 from the test plan are covered
</success_criteria>

<output>
After completion, create `.planning/phases/05-systems-panels-math/05-01-SUMMARY.md`
</output>
