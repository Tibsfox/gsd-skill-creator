# Batch 05: Infrastructure & Cloud Ops
# Extracted from 3 vision documents
# Generated by Phase 223, Plan 05

extraction_metadata:
  batch: 5
  plan: "223-05"
  documents_processed: 3
  extraction_date: "2026-02-19"

documents:
  - id: "local-cloud-provisioning"
    file: "gsd-local-cloud-provisioning-vision.md"
    checkpoint_count: 25
    checkpoints:
      # --- Template Engine ---
      - id: "lcp-001"
        section: "Implementation Phases > Phase 1: Template Engine"
        claim: "Template rendering system reads .yaml.template files, substitutes values from local/*.yaml, and writes to local/output/"
        verification: "unit-test"
        suggested_tier: "T2"
        status: "pending"
        notes: "Core template engine capability -- render-templates.sh produces correct output from templates + sample values"

      - id: "lcp-002"
        section: "Phase 2: Network Architecture > Templates Generated"
        claim: "Template variable substitution supports {{ variable }} syntax with dot-notation paths (e.g., network.management_subnet)"
        verification: "unit-test"
        suggested_tier: "T2"
        status: "pending"
        notes: ""

      - id: "lcp-003"
        section: "Phase 5: Service Configuration Templates"
        claim: "Template engine supports loop constructs ({% for %}) and conditional sections for list-based configs like DNS records and firewall rules"
        verification: "unit-test"
        suggested_tier: "T2"
        status: "pending"
        notes: "dnsmasq template uses {% for server in dns.upstream %} and {% for vm in vms %}"

      - id: "lcp-004"
        section: "Phase 5: Service Configuration Templates"
        claim: "Template engine supports filter expressions (e.g., {{ network.management_subnet | network_address }}.1)"
        verification: "unit-test"
        suggested_tier: "T2"
        status: "pending"
        notes: "Used in vmnet-management.yaml.template for gateway address derivation"

      # --- Hardware Interview ---
      - id: "lcp-005"
        section: "Implementation Phases > Phase 2: Hardware Interview"
        claim: "Interactive inventory collects CPU, RAM, storage, GPU, and network information from the user"
        verification: "manual-test"
        suggested_tier: "T2"
        status: "pending"
        notes: "Smart defaults based on detected system"

      - id: "lcp-006"
        section: "Phase 1: Hardware Inventory"
        claim: "Hardware interview writes sanitized profile to git (inventory/hardware-profile.yaml) and actual values to local (local/hardware-values.yaml)"
        verification: "code-review"
        suggested_tier: "T2"
        status: "pending"
        notes: "Sanitized means no identifying details (e.g., 192.168.x.0/24 not 192.168.1.0/24)"

      - id: "lcp-007"
        section: "Phase 1: Hardware Inventory"
        claim: "Hardware profile records CPU (cores, threads, host reserve), memory (total, reserves, available), storage (allocated, type, provisioning), GPU (vendor, vram, passthrough status), and network (sanitized subnets)"
        verification: "code-review"
        suggested_tier: "T2"
        status: "pending"
        notes: ""

      - id: "lcp-008"
        section: "Phase 1: Hardware Inventory > The Interview"
        claim: "Hardware interview adapts resource allocation suggestions based on available RAM (64GB vs 32GB produces different VM sizing)"
        verification: "unit-test"
        suggested_tier: "T2"
        status: "pending"
        notes: "64GB system gets 2GB+6GB VMs; 32GB system gets 1GB+4GB VMs"

      # --- Network Templates ---
      - id: "lcp-009"
        section: "Phase 2: Network Architecture"
        claim: "Network templates define two isolated virtual networks: management (host-only, static IPs, no DHCP) and NAT (outbound internet)"
        verification: "code-review"
        suggested_tier: "T2"
        status: "pending"
        notes: "Management: 10.10.1.0/24, NAT: 192.168.72.0/24"

      - id: "lcp-010"
        section: "Phase 2: Network Architecture > Templates Generated"
        claim: "DNS records template generates per-VM hostname entries from VM definitions"
        verification: "unit-test"
        suggested_tier: "T2"
        status: "pending"
        notes: "dns-records.yaml.template iterates VM list"

      - id: "lcp-011"
        section: "Phase 3: VM Definitions"
        claim: "VM definition templates include resource allocation (CPU, RAM, disk), network assignments (management + NAT), service list, and firewall zone rules"
        verification: "build-check"
        suggested_tier: "T2"
        status: "pending"
        notes: "infra-01.yaml.template and service-01.yaml.template"

      # --- VM Provisioning (Stretch Scope Note) ---
      - id: "lcp-012"
        section: "Phase 3: VM Definitions"
        claim: "MVP architecture defines exactly 2 VMs: infra-01 (DNS, NTP, monitoring) and service-01 (user workloads)"
        verification: "code-review"
        suggested_tier: "T2"
        status: "pending"
        notes: "4-VM environment (infra-01, gsd-dev-01, web-01, backend-01) is Phase 230 stretch goal -- MVP is 2 VMs only"

      - id: "lcp-013"
        section: "Phase 3: VM Definitions"
        claim: "VM resource allocation validates that total allocation does not exceed available hardware from the hardware profile"
        verification: "unit-test"
        suggested_tier: "T2"
        status: "pending"
        notes: ""

      # --- Provisioning Scripts ---
      - id: "lcp-014"
        section: "Phase 4: Base Image Provisioning Scripts"
        claim: "build-base-image.sh is idempotent -- safe to run multiple times without side effects"
        verification: "integration-test"
        suggested_tier: "T2"
        status: "pending"
        notes: "Uses set -euo pipefail, conditional service enable, firewall zone creation with 2>/dev/null || true"

      - id: "lcp-015"
        section: "Phase 4: Base Image Provisioning Scripts"
        claim: "Base image script installs essential packages, hardens SSH (no root login, max 3 auth tries), configures firewall zones (management + public), enables Cockpit, enforces SELinux, and enables chronyd"
        verification: "code-review"
        suggested_tier: "T2"
        status: "pending"
        notes: ""

      - id: "lcp-016"
        section: "Phase 4: Base Image Provisioning Scripts"
        claim: "configure-clone.sh sets per-VM identity: regenerates machine-id, regenerates SSH host keys, sets hostname, configures management network static IP, assigns firewall zone, and restricts SSH listen address"
        verification: "code-review"
        suggested_tier: "T2"
        status: "pending"
        notes: ""

      - id: "lcp-017"
        section: "Phase 6: Provisioning Orchestration"
        claim: "Master provisioning script (provision.sh) reads templates + local values and produces final configuration files in local/output/ -- it generates for review, does not auto-apply"
        verification: "integration-test"
        suggested_tier: "T2"
        status: "pending"
        notes: "Two modes: --render-only and --apply"

      # --- Template/Local Split (Privacy Architecture) ---
      - id: "lcp-018"
        section: "The Privacy Architecture > Two-Layer Configuration"
        claim: "Git repository contains zero private information -- all IPs, passwords, hardware specifics, and network details are in local/ which is gitignored"
        verification: "code-review"
        suggested_tier: "T1"
        status: "pending"
        notes: "Foundational privacy architecture -- the first commit is the .gitignore"

      - id: "lcp-019"
        section: "The Privacy Architecture > The .gitignore Contract"
        claim: "Repository ships with a strict .gitignore committed first (before any other file) that excludes local/, secrets/, *.local.*, SSH keys, VMware files, and generated output"
        verification: "code-review"
        suggested_tier: "T1"
        status: "pending"
        notes: ".gitignore is marked as protected -- GSD warns before modification"

      - id: "lcp-020"
        section: "The Privacy Architecture > How skill-creator Uses This Safely"
        claim: "Templates contain only structure, documentation, defaults, validation rules, and dependency declarations -- no user-specific configuration data"
        verification: "code-review"
        suggested_tier: "T1"
        status: "pending"
        notes: ""

      - id: "lcp-021"
        section: "The Skill-Creator Contract > Scenario 3"
        claim: "The local/ directory can be deleted and regenerated from templates + a fresh hardware interview without losing any infrastructure architecture decisions"
        verification: "integration-test"
        suggested_tier: "T2"
        status: "pending"
        notes: "Success criteria #5 from the vision document"

      # --- GSD Workflow Integration ---
      - id: "lcp-022"
        section: "Implementation Phases > Phase 5: GSD Workflow Integration"
        claim: "gsd new-project --vision local-cloud triggers the entire provisioning flow: hardware interview during research, template generation during execute-phase, verification during verify"
        verification: "integration-test"
        suggested_tier: "T1"
        status: "pending"
        notes: "Wires provisioning into GSD lifecycle"

      - id: "lcp-023"
        section: "The GSD Workflow > Starting the Project"
        claim: "GSD creates the project as a private GitHub repository by default using gh repo create --private --clone"
        verification: "manual-test"
        suggested_tier: "T2"
        status: "pending"
        notes: "Privacy-first default; never offers to make it public"

      - id: "lcp-024"
        section: "Scaling Beyond the MVP"
        claim: "Expansion projects (Minecraft, Ceph, OpenStack, reverse proxy) read the existing infrastructure repo's hardware profile and VM definitions to check resource availability"
        verification: "code-review"
        suggested_tier: "T2"
        status: "pending"
        notes: "Each expansion is a new GSD milestone: gsd new-project --vision <expansion> --infra gsd-local-cloud"

      - id: "lcp-025"
        section: "Success Criteria"
        claim: "A second user can clone the template repository and produce a working infrastructure for their different hardware by running the hardware interview and rendering templates"
        verification: "integration-test"
        suggested_tier: "T1"
        status: "pending"
        notes: "The shareability/reproducibility guarantee of the template/local architecture"

  - id: "cloud-ops-curriculum"
    file: "gsd-cloud-ops-curriculum-vision.md"
    scope_note: "Structure verification only per REQUIREMENTS.md Out of Scope. Full Cloud Ops content is future-scoped. Only structural and entry-point claims are extracted."
    checkpoint_count: 8
    checkpoints:
      # --- Curriculum Structure ---
      - id: "cop-001"
        section: "The Entry Point Decision"
        claim: "Entry point selection logic exists that lets users choose between three paths: Containers (Podman/Docker), Desktop VMs (VMware/VirtualBox/KVM), or OpenStack"
        verification: "code-review"
        suggested_tier: "T2"
        status: "pending"
        notes: "Structure only -- not verifying content of each path"

      - id: "cop-002"
        section: "The Curriculum Arc"
        claim: "Curriculum is structured as 6 sequential modules: The Machine, The Box, The Network, The Services, The Cloud, The Operator"
        verification: "code-review"
        suggested_tier: "T3"
        status: "pending"
        notes: "Verify module definitions exist; content quality is future scope"

      - id: "cop-003"
        section: "The GSD Integration"
        claim: "Each module is a GSD milestone consumable via gsd new-project --vision cloud-ops/<module-name>"
        verification: "code-review"
        suggested_tier: "T2"
        status: "pending"
        notes: "Verify vision documents exist for each module, or the pattern is documented"

      - id: "cop-004"
        section: "The GSD Integration"
        claim: "Each module produces configuration templates committed to the user's private gsd-local-cloud repo, git history documenting decisions, and working infrastructure the next module builds on"
        verification: "code-review"
        suggested_tier: "T2"
        status: "pending"
        notes: "Structural pattern -- templates + history + building-on-previous"

      - id: "cop-005"
        section: "The Convergence"
        claim: "Concept mapping table exists showing how container, VM, and OpenStack paths teach the same concepts (process isolation, networking, storage, boot image, identity, orchestration, dashboard, template sharing)"
        verification: "code-review"
        suggested_tier: "T3"
        status: "pending"
        notes: "Documentation/structural check"

      - id: "cop-006"
        section: "The Container Path in Detail > Podman on CentOS Stream 9"
        claim: "Container path uses Podman (daemonless, ships with CentOS) and produces the same git history structure as the VM path"
        verification: "code-review"
        suggested_tier: "T3"
        status: "pending"
        notes: "Structural parity between paths"

      - id: "cop-007"
        section: "Implementation Phases"
        claim: "Implementation phases follow a 6-phase structure: Curriculum Structure, Container Path, Desktop VM Path, OpenStack Path, Operations Module, Cross-Cloud Translation"
        verification: "code-review"
        suggested_tier: "T3"
        status: "pending"
        notes: "Verify phase structure is defined; full implementation is future"

      - id: "cop-008"
        section: "The Entry Point Decision"
        claim: "Curriculum adapts to hardware -- never suggests something the user's machine can't handle, shows what becomes possible with more resources"
        verification: "code-review"
        suggested_tier: "T2"
        status: "pending"
        notes: "Minimum requirements listed per path: containers 4GB/10GB, VMs 32GB/100GB, OpenStack 64GB recommended"

  - id: "wetty-tmux"
    file: "wetty-tmux-guide.md"
    conflict_note: "GSD-OS desktop vision specifies 'direct PTY, no Wetty dependency'. This guide describes a legacy/fallback approach. Claims extracted as-is; potential conflict noted for synthesis agent."
    checkpoint_count: 12
    checkpoints:
      # --- tmux Session Management ---
      - id: "wtm-001"
        section: "Basic Usage > Option 2: Direct Command Mode"
        claim: "tmux sessions can be auto-attached or created via the pattern: tmux attach -t <name> || tmux new -s <name>"
        verification: "manual-test"
        suggested_tier: "T2"
        status: "pending"
        notes: ""

      - id: "wtm-002"
        section: "tmux Integration Patterns > Dedicated Development Session"
        claim: "A dev-session.sh script creates a tmux session with predefined layout: editor, server, git, and test windows"
        verification: "manual-test"
        suggested_tier: "T3"
        status: "pending"
        notes: "Creates session if not exists, attaches if exists"

      - id: "wtm-003"
        section: "tmux Integration Patterns > Multiple Named Sessions"
        claim: "Multiple Wetty instances can serve different tmux sessions on different base paths (e.g., /dev, /logs)"
        verification: "manual-test"
        suggested_tier: "T3"
        status: "pending"
        notes: ""

      - id: "wtm-004"
        section: "tmux Integration Patterns > Read-Only Session Sharing"
        claim: "tmux sessions support read-only sharing via tmux attach -t <session> -r flag"
        verification: "manual-test"
        suggested_tier: "T3"
        status: "pending"
        notes: "Observer mode -- grouped sessions share windows but independent view"

      # --- Wetty Integration ---
      - id: "wtm-005"
        section: "Why Wetty"
        claim: "Wetty provides browser-based terminal access with full xterm-compatible rendering that handles tmux without issues"
        verification: "manual-test"
        suggested_tier: "T3"
        status: "pending"
        notes: "CONFLICT: GSD-OS desktop vision says direct PTY, no Wetty dependency. Wetty is fallback/legacy mode."

      - id: "wtm-006"
        section: "Basic Usage > Option 1: SSH Mode"
        claim: "Wetty SSH mode connects to localhost via SSH providing authenticated browser terminal access"
        verification: "manual-test"
        suggested_tier: "T3"
        status: "pending"
        notes: "Default mode -- user logs in with system credentials"

      - id: "wtm-007"
        section: "Basic Usage > Option 2: Direct Command Mode"
        claim: "Wetty direct command mode (--command flag) skips SSH and launches straight into a specified command (e.g., tmux)"
        verification: "manual-test"
        suggested_tier: "T3"
        status: "pending"
        notes: "Simpler for local-only use but no authentication layer"

      - id: "wtm-008"
        section: "Configuration > Mounting at a Subpath"
        claim: "Wetty can be mounted at any URL subpath via --base flag, enabling co-hosting alongside other services (e.g., /terminal, /docs)"
        verification: "manual-test"
        suggested_tier: "T3"
        status: "pending"
        notes: ""

      - id: "wtm-009"
        section: "Embedding in a Dashboard"
        claim: "Wetty supports iframe embedding via --allow-iframe flag for integration into local dev dashboards"
        verification: "manual-test"
        suggested_tier: "T3"
        status: "pending"
        notes: "CONFLICT with GSD-OS direct PTY approach -- iframe embedding is the browser-based pattern"

      # --- Session Persistence ---
      - id: "wtm-010"
        section: "Running as a Service > systemd"
        claim: "Wetty can run as a systemd service with Restart=on-failure for persistent availability"
        verification: "code-review"
        suggested_tier: "T3"
        status: "pending"
        notes: "wetty.service unit file provided"

      - id: "wtm-011"
        section: "Troubleshooting > Session disconnects after idle period"
        claim: "tmux sessions persist across browser disconnections -- reconnecting re-attaches to the existing session"
        verification: "manual-test"
        suggested_tier: "T2"
        status: "pending"
        notes: "Core persistence guarantee -- tmux sessions survive regardless of Wetty/browser state"

      - id: "wtm-012"
        section: "Reverse Proxy with nginx"
        claim: "Wetty behind nginx reverse proxy requires WebSocket upgrade headers (Upgrade, Connection) for proper real-time operation; without them falls back to polling"
        verification: "code-review"
        suggested_tier: "T3"
        status: "pending"
        notes: "nginx proxy_http_version 1.1 + proxy_set_header Upgrade required"
