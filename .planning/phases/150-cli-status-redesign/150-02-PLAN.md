---
phase: 150-cli-status-redesign
plan: 02
type: tdd
wave: 2
depends_on: ["150-01"]
files_modified:
  - src/cli/commands/status.ts
  - src/cli/commands/status.test.ts
autonomous: true

must_haves:
  truths:
    - "Running status shows an Installed Skills section with proportional percentages and mini bars"
    - "Running status shows a Loading Projection section with progress bar, loaded/deferred counts, and color coding"
    - "Over-budget scenarios display count-based summaries with no negative headroom"
    - "Skills sorted by size descending in both sections"
  artifacts:
    - path: "src/cli/commands/status.ts"
      provides: "Redesigned status command using status-display rendering functions"
    - path: "src/cli/commands/status.test.ts"
      provides: "Updated tests for redesigned status output"
  key_links:
    - from: "src/cli/commands/status.ts"
      to: "src/cli/commands/status-display.ts"
      via: "import renderInstalledSection, renderProjectionSection"
      pattern: "import.*status-display"
    - from: "src/cli/commands/status.ts"
      to: "src/validation/budget-validation.ts"
      via: "checkCumulative with optional profile for loading projection"
      pattern: "checkCumulative.*profile"
---

<objective>
Wire the pure rendering functions from `status-display.ts` into the actual `status.ts` CLI command, replacing the existing monolithic budget/per-skill display with the new two-section layout.

Purpose: Connect the tested rendering logic to the CLI command so `skill-creator status` actually shows the redesigned Installed Skills and Loading Projection sections.

Output: Updated `status.ts` that calls `renderInstalledSection()` and `renderProjectionSection()` for formatted output. Updated `status.test.ts` validating the new output structure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-cli-status-redesign/150-01-SUMMARY.md

@src/cli/commands/status.ts
@src/cli/commands/status.test.ts
@src/cli/commands/status-display.ts
@src/validation/budget-validation.ts
@src/validation/loading-projection.ts
@src/application/budget-profiles.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — Write failing tests for redesigned status command output</name>
  <files>src/cli/commands/status.test.ts</files>
  <action>
READ `src/cli/commands/status.test.ts` first — it already exists with tests for the old layout. Update it to test the new two-section layout.

**Keep existing infrastructure:** mocks for BudgetValidator, picocolors, fs/promises, DisclosureBudget, BudgetHistory, index.js, SkillStore. Keep the console.log capture pattern.

**Update mockCheckCumulative** to return results with `installedTotal` and `loadableTotal` fields (matching the extended CumulativeBudgetResult from Phase 149).

**Add/update these test cases:**

1. **"should show Installed Skills section header"** — output contains "Installed Skills"
2. **"should show skill percentages relative to total installed"** — for a skill that is 3800 of 8000 total installed, output should contain "47.5%" (not percentage of budget)
3. **"should show Loading Projection section header"** — output contains "Loading Projection"
4. **"should show loaded/deferred counts when projection is available"** — mock result with projection having 2 loaded and 1 deferred, check output contains "2 loaded" and "1 deferred"
5. **"should show count-based summary for over-budget"** — mock with 3 loaded out of 14 total skills, check output contains "3 of 14 skills fit"
6. **"should not show negative headroom values"** — mock where loadedTotal > budgetLimit, verify no negative numbers in output
7. **"should show color-coded budget percentage"** — verify color function is applied (since picocolors is mocked as identity, check the percentage text appears)
8. **"should pass profile to checkCumulative when getBudgetProfile returns a profile"** — verify checkCumulative is called with a profile argument (import and mock getBudgetProfile)

**Add mock for status-display.ts** — mock `renderInstalledSection` and `renderProjectionSection` to return identifiable strings so we can verify status.ts calls them and outputs their results.

**Add mock for budget-profiles.ts** — mock `getBudgetProfile` to return a test profile (used by status command to pass to checkCumulative).

Keep existing help and extends-line tests that still apply. Remove tests that test old layout details that no longer exist (like the old "Budget" header, old headroom line, old per-skill percentage against budget).

Commit: `test(150-02): add failing tests for redesigned status command wiring`
  </action>
  <verify>Run `npx vitest run src/cli/commands/status.test.ts` — new/updated tests MUST FAIL (status.ts still has old layout).</verify>
  <done>Test file updated with tests for new two-section layout. New tests fail, validating that current status.ts needs updating.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — Wire rendering functions into status command</name>
  <files>src/cli/commands/status.ts</files>
  <action>
READ `src/cli/commands/status.ts` first — it already exists.

**Modify the formatted output section** (the non-JSON branch) to use the new rendering functions:

1. **Import** `renderInstalledSection` and `renderProjectionSection` from `./status-display.js`
2. **Import** `getBudgetProfile` from `../../application/budget-profiles.js` (or accept profile name from CLI args)
3. **Determine active profile:** Try `getBudgetProfile('gsd-executor')` as default profile (the most common agent). This is the profile passed to `checkCumulative` for loading projection.
4. **Call** `validator.checkCumulative(skillsDir, profile)` — pass the profile so CumulativeBudgetResult includes projection data.
5. **Replace** the old "Section 2: Budget overview" and "Section 3: Per-skill breakdown" and "Section 4: Headroom" with:
   - `console.log(renderInstalledSection(result))` — installed skills section
   - `console.log('')` — blank separator
   - `console.log(renderProjectionSection(result))` — loading projection section
6. **Keep** Section 1 (active skills display from applicator) and Section 5 (trend) and the history append.
7. **Remove** the old `colorBySeverity` function from status.ts (replaced by `budgetColorCode` in status-display.ts).
8. **Remove** the `hasDisclosureDirs` function and disclosure breakdown logic from status.ts — the new installed section is simpler (percentage + mini bar). Disclosure details can be accessed via a separate command.

**Updated structure of formatted output:**
```
[Active skills display]          ← kept
[Flagged skills if any]          ← kept

[Installed Skills section]       ← NEW: from renderInstalledSection()

[Loading Projection section]     ← NEW: from renderProjectionSection()

[Trend line]                     ← kept
```

Commit: `feat(150-02): wire status display functions into CLI status command`
  </action>
  <verify>
1. `npx vitest run src/cli/commands/status.test.ts` — ALL tests pass
2. `npx vitest run src/cli/commands/status-display.test.ts` — still passes (no regression)
3. `npx tsc --noEmit` — no type errors
  </verify>
  <done>Status command uses renderInstalledSection and renderProjectionSection. Two-section layout visible. All tests pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/cli/commands/status.test.ts` — all tests pass
2. `npx vitest run src/cli/commands/status-display.test.ts` — no regressions
3. `npx tsc --noEmit` — clean compilation
4. status.ts imports and calls renderInstalledSection and renderProjectionSection
5. status.ts passes a BudgetProfile to checkCumulative for projection data
6. Old monolithic budget display code removed from status.ts
</verification>

<success_criteria>
- `skill-creator status` shows "Installed Skills" section with proportional percentages
- `skill-creator status` shows "Loading Projection" section with loaded/deferred breakdown
- Over-budget displays count-based summary, no negative headroom
- Color coding applied to budget percentage
- TDD RED-GREEN cycle with 2 atomic commits
</success_criteria>

<output>
After completion, create `.planning/phases/150-cli-status-redesign/150-02-SUMMARY.md`
</output>
