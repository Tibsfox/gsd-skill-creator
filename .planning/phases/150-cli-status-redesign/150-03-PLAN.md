---
phase: 150-cli-status-redesign
plan: 03
type: tdd
wave: 3
depends_on: ["150-02"]
files_modified:
  - src/cli/commands/status.ts
  - src/cli/commands/status.test.ts
autonomous: true

must_haves:
  truths:
    - "Running status --json returns structured data with both installed inventory and loading projection"
    - "JSON output includes installed array with name, charCount, percentOfInstalled for each skill"
    - "JSON output includes projection object with loaded/deferred arrays when profile is available"
    - "JSON output handles null projection gracefully when no profile matches"
  artifacts:
    - path: "src/cli/commands/status.ts"
      provides: "Updated JSON output mode using buildStatusJson"
    - path: "src/cli/commands/status.test.ts"
      provides: "Tests for JSON output with installed and projection data"
  key_links:
    - from: "src/cli/commands/status.ts"
      to: "src/cli/commands/status-display.ts"
      via: "import buildStatusJson"
      pattern: "import.*buildStatusJson.*status-display"
---

<objective>
Wire the `buildStatusJson()` function into the status command's `--json` mode, replacing the old JSON output with the new structured format containing both installed inventory and loading projection data.

Purpose: Machine-readable output that downstream tools and scripts can parse to inspect both what is installed and what would load for a given profile.

Output: Updated `--json` mode in `status.ts` using `buildStatusJson()`. Tests validating JSON structure matches BC-08.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-cli-status-redesign/150-01-SUMMARY.md
@.planning/phases/150-cli-status-redesign/150-02-SUMMARY.md

@src/cli/commands/status.ts
@src/cli/commands/status.test.ts
@src/cli/commands/status-display.ts
@src/cli/commands/status-display.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — Write failing tests for JSON output mode with installed and projection data</name>
  <files>src/cli/commands/status.test.ts</files>
  <action>
READ `src/cli/commands/status.test.ts` first — it already exists from plan 150-02.

**Add/update JSON output tests:**

1. **"should produce JSON with installed section containing skills with percentOfInstalled"** — run with `['--json']`, parse output, verify `parsed.installed` is an array where each entry has `name`, `charCount`, `percentOfInstalled` fields. Verify percentOfInstalled uses total installed as denominator (e.g., skill of 3800 out of 8000 total = 47.5).

2. **"should produce JSON with projection section when profile provides projection"** — mock checkCumulative to return a result with projection data (loaded/deferred arrays). Parse JSON output, verify `parsed.projection` has `profileName`, `budgetLimit`, `loadedTotal`, `deferredTotal`, `loaded` array, `deferred` array. Each loaded/deferred entry has `name`, `charCount`, `tier`, `oversized`.

3. **"should produce JSON with null projection when no profile available"** — mock checkCumulative to return result without projection (projection: undefined). Parse JSON, verify `parsed.projection` is `null`.

4. **"should include budget and totalInstalled in JSON output"** — verify `parsed.budget` and `parsed.totalInstalled` are present and numeric.

5. **"should include trend data in JSON output"** — mock BudgetHistory.getTrend to return trend data, verify `parsed.trend` has `charDelta`, `skillDelta`, `periodSnapshots`.

6. **"should sort installed skills by size descending in JSON"** — verify first skill in `parsed.installed` has the largest charCount.

Update the existing `"should produce valid JSON with --json flag"` test to check for the new structure (installed + projection) instead of the old structure (budget + totalChars + headroom + skills).

Commit: `test(150-03): add failing tests for JSON output with installed and projection data`
  </action>
  <verify>Run `npx vitest run src/cli/commands/status.test.ts` — new JSON tests MUST FAIL (status.ts still uses old JSON format from 150-02 wiring).</verify>
  <done>JSON output tests written for BC-08 requirements. Tests fail against current implementation.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — Wire buildStatusJson into status command JSON mode</name>
  <files>src/cli/commands/status.ts</files>
  <action>
READ `src/cli/commands/status.ts` first — it already exists from plan 150-02.

**Update the JSON output branch** (the `if (jsonMode)` block):

1. **Import** `buildStatusJson` from `./status-display.js` (add to existing import from 150-02)
2. **Replace** the old JSON construction with:
   ```ts
   const jsonData = buildStatusJson(result);
   ```
3. **Add trend data** to the JSON output — extend the object with a `trend` field:
   ```ts
   const jsonOutput = {
     ...jsonData,
     trend: trend ? {
       charDelta: trend.charDelta,
       skillDelta: trend.skillDelta,
       periodSnapshots: trend.periodSnapshots,
     } : null,
   };
   ```
4. **Output** `JSON.stringify(jsonOutput, null, 2)`
5. **Keep** the history append call after JSON output

The old JSON output had: `budget, totalChars, headroom, usagePercent, skills, trend`.
The new JSON output has: `budget, totalInstalled, installed, projection, trend`.

This is a BREAKING CHANGE to the JSON output format. This is intentional per BC-08.

Commit: `feat(150-03): wire buildStatusJson into CLI status JSON mode`
  </action>
  <verify>
1. `npx vitest run src/cli/commands/status.test.ts` — ALL tests pass
2. `npx vitest run src/cli/commands/status-display.test.ts` — no regressions
3. `npx tsc --noEmit` — no type errors
  </verify>
  <done>JSON mode outputs structured data with installed inventory and loading projection. All tests pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/cli/commands/status.test.ts` — all tests pass
2. `npx vitest run src/cli/commands/status-display.test.ts` — no regressions
3. `npx tsc --noEmit` — clean compilation
4. `--json` output contains `installed` array with percentOfInstalled
5. `--json` output contains `projection` object (or null when no profile)
6. `--json` output includes `trend` data
</verification>

<success_criteria>
- `skill-creator status --json` returns structured data with installed and projection sections (BC-08)
- JSON installed array has percentOfInstalled using total installed as denominator
- JSON projection has loaded/deferred arrays with tier and oversized flags
- Null projection handled gracefully
- TDD RED-GREEN cycle with 2 atomic commits
</success_criteria>

<output>
After completion, create `.planning/phases/150-cli-status-redesign/150-03-SUMMARY.md`
</output>
