---
phase: 320-operations-manual
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations-manual/neutron-procedures.md
  - docs/operations-manual/cinder-procedures.md
  - docs/operations-manual/swift-procedures.md
autonomous: true
requirements: [DOCS-03, DOCS-04, DOCS-09]

must_haves:
  truths:
    - "Neutron procedures file contains health check, config verification, backup/restore, upgrade, troubleshooting, security audit, network topology changes, floating IP management, and security group audit procedures"
    - "Cinder procedures file contains health check, config verification, backup/restore, upgrade, troubleshooting, security audit, volume migration, backend failover, and snapshot management procedures"
    - "Swift procedures file contains health check, config verification, backup/restore, upgrade, troubleshooting, security audit, ring rebalancing, replication monitoring, and container quota management procedures"
    - "Every procedure has VERIFICATION section with specific commands"
    - "All OpenStack documentation references are page-specific URLs"
  artifacts:
    - path: "docs/operations-manual/neutron-procedures.md"
      provides: "All OPS-NEUTRON procedures for networking service operations"
      contains: "OPS-NEUTRON-001"
    - path: "docs/operations-manual/cinder-procedures.md"
      provides: "All OPS-CINDER procedures for block storage service operations"
      contains: "OPS-CINDER-001"
    - path: "docs/operations-manual/swift-procedures.md"
      provides: "All OPS-SWIFT procedures for object storage service operations"
      contains: "OPS-SWIFT-001"
  key_links:
    - from: "docs/operations-manual/neutron-procedures.md"
      to: "skills/openstack/neutron/SKILL.md"
      via: "procedure content derived from skill troubleshoot/operate sections"
      pattern: "NPR 7123.1"
    - from: "docs/operations-manual/cinder-procedures.md"
      to: "skills/openstack/cinder/SKILL.md"
      via: "procedure content derived from skill troubleshoot/operate sections"
      pattern: "NPR 7123.1"
    - from: "docs/operations-manual/swift-procedures.md"
      to: "skills/openstack/swift/SKILL.md"
      via: "procedure content derived from skill troubleshoot/operate sections"
      pattern: "NPR 7123.1"
---

<objective>
Create operations manual procedure files for Neutron (networking), Cinder (block storage), and Swift (object storage) -- the networking and storage services. Each file contains per-service procedures in NASA format covering daily health checks, configuration verification, backup/restore, upgrades, troubleshooting common failures, security audits, and service-specific operations.

Purpose: Networking and storage are the most operationally complex day-2 domains. Operators need verified procedures for SDN management, volume lifecycle, and object storage ring operations.
Output: 3 procedure files in docs/operations-manual/ with OPS-{SERVICE}-{NNN} formatted procedures
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@skills/methodology/ops-manual-writer/SKILL.md
@skills/openstack/neutron/SKILL.md
@skills/openstack/cinder/SKILL.md
@skills/openstack/swift/SKILL.md
@docs/filesystem-contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Neutron networking service procedures</name>
  <files>docs/operations-manual/neutron-procedures.md</files>
  <action>
Create the Neutron operations manual procedure file following the exact format from ops-manual-writer SKILL.md. The file header should include a title, description, and table of contents.

Write these procedures using the OPS-NEUTRON-NNN format with all required sections (PURPOSE, PRECONDITIONS, SAFETY CONSIDERATIONS, PROCEDURE with numbered steps with Expected result and If unexpected branches, VERIFICATION, ROLLBACK, REFERENCES):

**Required standard categories:**
- OPS-NEUTRON-001: Service Health Check (daily) -- verify neutron containers (neutron_server, neutron_dhcp_agent, neutron_l3_agent, neutron_metadata_agent, neutron_openvswitch_agent or neutron_ovn_controller), check agent list status, verify network connectivity, check logs for errors
- OPS-NEUTRON-002: Configuration Verification -- confirm neutron settings in globals.yml (network_interface, neutron_external_interface, neutron_plugin_agent), verify OVS/OVN bridge configuration, check ML2 plugin settings, validate DHCP agent settings
- OPS-NEUTRON-003: Backup and Restore -- backup neutron database, backup OVS/OVN configuration, backup network definitions, restore procedure with connectivity verification
- OPS-NEUTRON-004: Minor Upgrade -- pre-upgrade network state capture, kolla-ansible upgrade --tags neutron, post-upgrade agent status and connectivity verification
- OPS-NEUTRON-005: Major Upgrade -- extended pre-checks including OVS/OVN version compatibility, database migration, upgrade execution, full connectivity verification
- OPS-NEUTRON-006: Troubleshooting Common Failures -- network unreachable (OVS bridge issues), DHCP agent failures, floating IP not working, security group rules not applied, metadata agent unreachable
- OPS-NEUTRON-007: Security Audit -- security group rule review, network isolation verification, RBAC network policy check, port security settings, anti-spoofing verification

**Service-specific additions:**
- OPS-NEUTRON-008: Network Topology Changes -- add/modify/remove networks and subnets, router management, verify connectivity after changes
- OPS-NEUTRON-009: Floating IP Management -- allocate/assign/release floating IPs, troubleshoot NAT issues, verify external connectivity
- OPS-NEUTRON-010: Security Group Audit -- list all security groups and rules, identify overly permissive rules, verify default security group settings, document exceptions

All commands must use `openstack` CLI, `ovs-vsctl`, or `docker exec`. Include OVS bridge verification commands. Cross-reference OPS-KEYSTONE for auth and OPS-NOVA for compute-network integration.

Use imperative mood. Include exact commands. Provide expected output examples. Neutron is the most operationally complex service -- ensure troubleshooting procedures include packet tracing guidance.
  </action>
  <verify>
Confirm file exists at docs/operations-manual/neutron-procedures.md. Verify it contains all 10 OPS-NEUTRON procedures (001-010). Verify each procedure has all required sections. Verify OVS/OVN-specific commands are present. Verify cross-references to OPS-KEYSTONE and OPS-NOVA.
  </verify>
  <done>neutron-procedures.md contains 10 complete procedures (OPS-NEUTRON-001 through OPS-NEUTRON-010) in NASA format with OVS/OVN verification commands, cross-references to Keystone and Nova, specific OpenStack doc URLs, and NASA cross-references</done>
</task>

<task type="auto">
  <name>Task 2: Create Cinder block storage service procedures</name>
  <files>docs/operations-manual/cinder-procedures.md</files>
  <action>
Create the Cinder operations manual procedure file following the exact format from ops-manual-writer SKILL.md. The file header should include a title, description, and table of contents.

Write these procedures using the OPS-CINDER-NNN format with all required sections:

**Required standard categories:**
- OPS-CINDER-001: Service Health Check (daily) -- verify cinder containers (cinder_api, cinder_scheduler, cinder_volume), check volume service list, verify backend connectivity (LVM), check logs
- OPS-CINDER-002: Configuration Verification -- confirm cinder settings in globals.yml (cinder backend, LVM configuration), verify iSCSI target service, check volume type definitions, validate backend capacity
- OPS-CINDER-003: Backup and Restore -- backup cinder database, backup volume metadata, LVM snapshot backup procedure, restore procedure with volume attachment verification
- OPS-CINDER-004: Minor Upgrade -- pre-upgrade volume state check, kolla-ansible upgrade --tags cinder, post-upgrade volume service and attachment verification
- OPS-CINDER-005: Major Upgrade -- extended pre-checks, database migration, upgrade execution, volume accessibility verification
- OPS-CINDER-006: Troubleshooting Common Failures -- volume create fails (no valid backend), volume attach fails (iSCSI target issues), volume stuck in creating/deleting/error state, backend capacity exhausted, snapshot failures
- OPS-CINDER-007: Security Audit -- volume encryption settings, backup encryption verification, volume type access restrictions, API policy review

**Service-specific additions:**
- OPS-CINDER-008: Volume Migration -- migrate volumes between backends, online migration for attached volumes, verify data integrity after migration
- OPS-CINDER-009: Backend Failover -- detect backend failure, initiate failover, verify volume accessibility, failback procedure
- OPS-CINDER-010: Snapshot Management -- create/list/delete snapshots, create volumes from snapshots, manage snapshot chains, purge orphaned snapshots

All commands must use `openstack` CLI, `lvs`/`vgs` for LVM verification, or `docker exec`. Cross-reference OPS-KEYSTONE for auth and OPS-NOVA for volume attachment context.

Use imperative mood. Include exact commands. Include LVM-specific verification commands since single-node uses LVM backend.
  </action>
  <verify>
Confirm file exists at docs/operations-manual/cinder-procedures.md. Verify it contains all 10 OPS-CINDER procedures (001-010). Verify LVM backend commands are present (lvs, vgs). Verify cross-references to OPS-KEYSTONE and OPS-NOVA.
  </verify>
  <done>cinder-procedures.md contains 10 complete procedures (OPS-CINDER-001 through OPS-CINDER-010) in NASA format with LVM backend verification commands, cross-references to Keystone and Nova, specific OpenStack doc URLs, and NASA cross-references</done>
</task>

<task type="auto">
  <name>Task 3: Create Swift object storage service procedures</name>
  <files>docs/operations-manual/swift-procedures.md</files>
  <action>
Create the Swift operations manual procedure file following the exact format from ops-manual-writer SKILL.md. The file header should include a title, description, and table of contents.

Write these procedures using the OPS-SWIFT-NNN format with all required sections:

**Required standard categories:**
- OPS-SWIFT-001: Service Health Check (daily) -- verify swift containers (swift_proxy_server, swift_account_server, swift_container_server, swift_object_server), check ring file integrity, verify replication status, check logs
- OPS-SWIFT-002: Configuration Verification -- confirm swift settings in globals.yml, verify ring files (account.ring.gz, container.ring.gz, object.ring.gz), check proxy pipeline configuration, validate storage policy
- OPS-SWIFT-003: Backup and Restore -- backup swift ring files, backup proxy configuration, backup account database, restore procedure with ring integrity verification
- OPS-SWIFT-004: Minor Upgrade -- pre-upgrade ring state capture, kolla-ansible upgrade --tags swift, post-upgrade replication and accessibility verification
- OPS-SWIFT-005: Major Upgrade -- extended pre-checks, ring compatibility verification, upgrade execution, full replication status check
- OPS-SWIFT-006: Troubleshooting Common Failures -- 503 proxy errors, replication lag, ring inconsistency, container sync failures, object not found after upload
- OPS-SWIFT-007: Security Audit -- container ACL review, tempurl key management, account quota verification, access log analysis

**Service-specific additions:**
- OPS-SWIFT-008: Ring Rebalancing -- add/remove devices, rebalance ring, distribute ring files, monitor rebalancing progress, verify data placement
- OPS-SWIFT-009: Replication Monitoring -- check replication status for all three rings, identify replication lag, troubleshoot replication failures, verify consistency
- OPS-SWIFT-010: Container Quota Management -- set/modify/remove container quotas, account quotas, verify quota enforcement, handle quota exceeded errors

All commands must use `openstack` CLI, `swift-ring-builder`, or `docker exec`. Cross-reference OPS-KEYSTONE for auth. Swift ring operations are unique -- include ring builder command syntax.

Use imperative mood. Include exact commands. Include swift-ring-builder syntax for ring operations.
  </action>
  <verify>
Confirm file exists at docs/operations-manual/swift-procedures.md. Verify it contains all 10 OPS-SWIFT procedures (001-010). Verify swift-ring-builder commands are present. Verify cross-references to OPS-KEYSTONE.
  </verify>
  <done>swift-procedures.md contains 10 complete procedures (OPS-SWIFT-001 through OPS-SWIFT-010) in NASA format with swift-ring-builder commands, cross-references to Keystone, specific OpenStack doc URLs, and NASA cross-references</done>
</task>

</tasks>

<verification>
1. All 3 procedure files exist in docs/operations-manual/
2. Each file uses OPS-{SERVICE}-{NNN} procedure ID format
3. Every procedure contains all 7 required sections
4. Imperative mood used throughout
5. Exact commands in every procedure step with expected results
6. Service-specific tools included (ovs-vsctl for Neutron, lvs/vgs for Cinder, swift-ring-builder for Swift)
7. Cross-references use OPS-{SERVICE}-{NNN} format
8. NASA references include SP-6105 and NPR 7123.1 section numbers
9. OpenStack doc references are specific page URLs
</verification>

<success_criteria>
- 3 files created: neutron-procedures.md, cinder-procedures.md, swift-procedures.md
- 30 total procedures across the 3 files (10 + 10 + 10)
- Every procedure verifiable by doc-verifier agent
- NASA procedure format followed consistently
- Service-specific operational tools referenced in commands
</success_criteria>

<output>
After completion, create `.planning/phases/320-operations-manual/320-02-SUMMARY.md`
</output>
