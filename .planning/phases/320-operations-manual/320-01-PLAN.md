---
phase: 320-operations-manual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations-manual/keystone-procedures.md
  - docs/operations-manual/nova-procedures.md
  - docs/operations-manual/glance-procedures.md
autonomous: true
requirements: [DOCS-03, DOCS-04, DOCS-09]

must_haves:
  truths:
    - "Keystone procedures file contains health check, config verification, backup/restore, upgrade, troubleshooting, and security audit procedures following OPS-KEYSTONE-NNN format"
    - "Nova procedures file contains health check, config verification, backup/restore, upgrade, troubleshooting, live migration, and flavor management procedures following OPS-NOVA-NNN format"
    - "Glance procedures file contains health check, config verification, backup/restore, upgrade, troubleshooting, image format conversion, and metadata management procedures following OPS-GLANCE-NNN format"
    - "Every procedure has VERIFICATION section with specific commands an operator can run"
    - "All OpenStack documentation references point to specific pages with release context"
  artifacts:
    - path: "docs/operations-manual/keystone-procedures.md"
      provides: "All OPS-KEYSTONE procedures for identity service operations"
      contains: "OPS-KEYSTONE-001"
    - path: "docs/operations-manual/nova-procedures.md"
      provides: "All OPS-NOVA procedures for compute service operations"
      contains: "OPS-NOVA-001"
    - path: "docs/operations-manual/glance-procedures.md"
      provides: "All OPS-GLANCE procedures for image service operations"
      contains: "OPS-GLANCE-001"
  key_links:
    - from: "docs/operations-manual/keystone-procedures.md"
      to: "skills/openstack/keystone/SKILL.md"
      via: "procedure content derived from skill troubleshoot/operate sections"
      pattern: "NPR 7123.1"
    - from: "docs/operations-manual/nova-procedures.md"
      to: "skills/openstack/nova/SKILL.md"
      via: "procedure content derived from skill troubleshoot/operate sections"
      pattern: "NPR 7123.1"
    - from: "docs/operations-manual/glance-procedures.md"
      to: "skills/openstack/glance/SKILL.md"
      via: "procedure content derived from skill troubleshoot/operate sections"
      pattern: "NPR 7123.1"
---

<objective>
Create operations manual procedure files for Keystone (identity), Nova (compute), and Glance (images) -- the three foundational OpenStack services. Each file contains per-service procedures in NASA format covering daily health checks, configuration verification, backup/restore, upgrades, troubleshooting common failures, security audits, and service-specific operations.

Purpose: These are the three services every other OpenStack component depends on. Operators need verified, step-by-step procedures for day-2 operations of identity, compute, and image services.
Output: 3 procedure files in docs/operations-manual/ with OPS-{SERVICE}-{NNN} formatted procedures
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@skills/methodology/ops-manual-writer/SKILL.md
@skills/openstack/keystone/SKILL.md
@skills/openstack/nova/SKILL.md
@skills/openstack/glance/SKILL.md
@docs/filesystem-contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Keystone identity service procedures</name>
  <files>docs/operations-manual/keystone-procedures.md</files>
  <action>
Create the Keystone operations manual procedure file following the exact format from ops-manual-writer SKILL.md. The file header should include a title, description, and table of contents.

Write these procedures using the OPS-KEYSTONE-NNN format with all required sections (PURPOSE, PRECONDITIONS, SAFETY CONSIDERATIONS, PROCEDURE with numbered steps with Expected result and If unexpected branches, VERIFICATION, ROLLBACK, REFERENCES):

**Required standard categories:**
- OPS-KEYSTONE-001: Service Health Check (daily) -- verify keystone_api and keystone_fernet containers running, token issue succeeds, endpoint list returns catalog, check logs for errors
- OPS-KEYSTONE-002: Configuration Verification -- confirm globals.yml keystone settings, verify Fernet key state, check TLS certificate validity, validate RBAC policy files
- OPS-KEYSTONE-003: Backup and Restore -- backup keystone database, backup Fernet keys, backup policy files, restore procedure with verification
- OPS-KEYSTONE-004: Minor Upgrade -- pre-upgrade checks, kolla-ansible upgrade --tags keystone, post-upgrade verification
- OPS-KEYSTONE-005: Major Upgrade -- extended pre-checks, database migration, upgrade execution, extended post-verification, rollback plan
- OPS-KEYSTONE-006: Troubleshooting Common Failures -- 401 authentication errors, token validation failures, service catalog endpoint mismatch, database connection failures, Fernet key rotation issues
- OPS-KEYSTONE-007: Security Audit -- RBAC policy review, token expiration settings, TLS certificate validity, admin credential rotation, audit log review

**Service-specific additions:**
- OPS-KEYSTONE-008: Token Rotation -- Fernet key rotation procedure with zero-downtime steps
- OPS-KEYSTONE-009: Catalog Management -- add/modify/remove service endpoints in the catalog

All commands must use `openstack` CLI or `docker exec` for containerized operations. All procedures map to SE Phase E (Operations and Sustainment). References must include specific OpenStack docs URLs (docs.openstack.org/keystone/latest/) and NASA document IDs (SP-6105, NPR 7123.1). Cross-reference related procedures by OPS-KEYSTONE-NNN ID.

Use imperative mood. Include exact commands. Provide expected output examples. Do NOT use placeholder text like "adjust accordingly".
  </action>
  <verify>
Confirm file exists at docs/operations-manual/keystone-procedures.md. Verify it contains all 9 OPS-KEYSTONE procedures (001-009). Verify each procedure has PURPOSE, PRECONDITIONS, SAFETY CONSIDERATIONS, PROCEDURE, VERIFICATION, ROLLBACK, and REFERENCES sections. Verify NASA document references (SP-6105, NPR 7123.1) appear. Verify OpenStack docs URLs are present and specific (not just top-level docs.openstack.org).
  </verify>
  <done>keystone-procedures.md contains 9 complete procedures (OPS-KEYSTONE-001 through OPS-KEYSTONE-009) in NASA format with verification sections, specific OpenStack doc URLs, and NASA cross-references</done>
</task>

<task type="auto">
  <name>Task 2: Create Nova compute service procedures</name>
  <files>docs/operations-manual/nova-procedures.md</files>
  <action>
Create the Nova operations manual procedure file following the exact format from ops-manual-writer SKILL.md. The file header should include a title, description, and table of contents.

Write these procedures using the OPS-NOVA-NNN format with all required sections:

**Required standard categories:**
- OPS-NOVA-001: Service Health Check (daily) -- verify nova containers (nova_api, nova_compute, nova_scheduler, nova_conductor, nova_novncproxy), check hypervisor list, verify compute services are up, check logs
- OPS-NOVA-002: Configuration Verification -- confirm nova settings in globals.yml, verify compute driver configuration, check scheduler filter settings, validate virt_type
- OPS-NOVA-003: Backup and Restore -- backup nova database, backup instance definitions, backup nova configuration, restore procedure
- OPS-NOVA-004: Minor Upgrade -- pre-upgrade instance status check, kolla-ansible upgrade --tags nova, post-upgrade verification including instance reachability
- OPS-NOVA-005: Major Upgrade -- extended pre-checks, cell mapping verification, database migration, upgrade execution, post-verify
- OPS-NOVA-006: Troubleshooting Common Failures -- instances stuck in BUILD/ERROR, live migration failures, scheduler NoValidHost errors, console access failures, compute service down
- OPS-NOVA-007: Security Audit -- nova policy review, console proxy security, metadata service access, compute node isolation verification

**Service-specific additions:**
- OPS-NOVA-008: Live Migration -- pre-migration checks, execute live migration, verify migration success, handle failed migrations
- OPS-NOVA-009: Flavor Management -- create/modify/delete flavors, verify flavor metadata, set access restrictions
- OPS-NOVA-010: Hypervisor Maintenance -- disable scheduling, migrate instances off host, perform maintenance, re-enable

All commands must use `openstack` CLI or `docker exec`. References must include specific OpenStack docs URLs (docs.openstack.org/nova/latest/) and NASA IDs. Cross-reference OPS-KEYSTONE procedures where authentication is involved.

Use imperative mood. Include exact commands. Provide expected output examples.
  </action>
  <verify>
Confirm file exists at docs/operations-manual/nova-procedures.md. Verify it contains all 10 OPS-NOVA procedures (001-010). Verify each procedure has all required sections. Verify cross-references to OPS-KEYSTONE procedures where relevant. Verify NASA and OpenStack doc references are present and specific.
  </verify>
  <done>nova-procedures.md contains 10 complete procedures (OPS-NOVA-001 through OPS-NOVA-010) in NASA format with verification sections, cross-references to Keystone procedures, specific OpenStack doc URLs, and NASA cross-references</done>
</task>

<task type="auto">
  <name>Task 3: Create Glance image service procedures</name>
  <files>docs/operations-manual/glance-procedures.md</files>
  <action>
Create the Glance operations manual procedure file following the exact format from ops-manual-writer SKILL.md. The file header should include a title, description, and table of contents.

Write these procedures using the OPS-GLANCE-NNN format with all required sections:

**Required standard categories:**
- OPS-GLANCE-001: Service Health Check (daily) -- verify glance_api container running, image list returns results, check backend storage connectivity, check logs
- OPS-GLANCE-002: Configuration Verification -- confirm glance settings in globals.yml, verify backend type (file/swift/ceph), check store configuration, validate image size limits
- OPS-GLANCE-003: Backup and Restore -- backup glance database, backup image files (if file backend), backup configuration, restore procedure
- OPS-GLANCE-004: Minor Upgrade -- pre-upgrade image list snapshot, kolla-ansible upgrade --tags glance, post-upgrade image accessibility verification
- OPS-GLANCE-005: Major Upgrade -- extended pre-checks, database migration, upgrade execution, image integrity verification
- OPS-GLANCE-006: Troubleshooting Common Failures -- image upload failures (HTTP 500/413), download timeouts, format conversion errors, images stuck in queued/saving state, metadata corruption
- OPS-GLANCE-007: Security Audit -- image visibility settings review, public image policy, backend storage permissions, access control verification

**Service-specific additions:**
- OPS-GLANCE-008: Image Format Conversion -- convert between raw/qcow2/vmdk formats, verify converted image integrity, update metadata after conversion
- OPS-GLANCE-009: Metadata Management -- update image properties, manage custom metadata, set minimum disk/RAM requirements
- OPS-GLANCE-010: Backend Rotation -- migrate images between storage backends, verify image accessibility after migration

All commands must use `openstack` CLI or `docker exec`. References must include specific OpenStack docs URLs (docs.openstack.org/glance/latest/) and NASA IDs. Cross-reference OPS-KEYSTONE for auth and OPS-NOVA for compute integration.

Use imperative mood. Include exact commands. Provide expected output examples.
  </action>
  <verify>
Confirm file exists at docs/operations-manual/glance-procedures.md. Verify it contains all 10 OPS-GLANCE procedures (001-010). Verify each procedure has all required sections. Verify cross-references to OPS-KEYSTONE and OPS-NOVA procedures. Verify NASA and OpenStack doc references are present and specific.
  </verify>
  <done>glance-procedures.md contains 10 complete procedures (OPS-GLANCE-001 through OPS-GLANCE-010) in NASA format with verification sections, cross-references to Keystone and Nova procedures, specific OpenStack doc URLs, and NASA cross-references</done>
</task>

</tasks>

<verification>
1. All 3 procedure files exist in docs/operations-manual/
2. Each file uses OPS-{SERVICE}-{NNN} procedure ID format
3. Every procedure contains all 7 required sections (PURPOSE, PRECONDITIONS, SAFETY CONSIDERATIONS, PROCEDURE, VERIFICATION, ROLLBACK, REFERENCES)
4. Imperative mood used throughout (no "you should" or "the operator")
5. Exact commands in every procedure step with expected results
6. Cross-references use OPS-{SERVICE}-{NNN} format
7. NASA references include SP-6105 and NPR 7123.1 section numbers
8. OpenStack doc references are specific page URLs, not top-level
9. SE Phase E designation present in every procedure header
</verification>

<success_criteria>
- 3 files created: keystone-procedures.md, nova-procedures.md, glance-procedures.md
- 29 total procedures across the 3 files (9 + 10 + 10)
- Every procedure verifiable by doc-verifier agent
- NASA procedure format followed consistently
- OpenStack docs URLs are specific and include release context
</success_criteria>

<output>
After completion, create `.planning/phases/320-operations-manual/320-01-SUMMARY.md`
</output>
