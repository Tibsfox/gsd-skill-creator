---
phase: 320-operations-manual
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations-manual/heat-procedures.md
  - docs/operations-manual/horizon-procedures.md
autonomous: true
requirements: [DOCS-03, DOCS-04, DOCS-09]

must_haves:
  truths:
    - "Heat procedures file contains health check, config verification, backup/restore, upgrade, troubleshooting, security audit, stack template validation, resource dependency analysis, and orphan cleanup procedures"
    - "Horizon procedures file contains health check, config verification, backup/restore, upgrade, troubleshooting, security audit, session management, custom panel deployment, and theme configuration procedures"
    - "Every procedure has VERIFICATION section with specific commands"
    - "All OpenStack documentation references are page-specific URLs"
  artifacts:
    - path: "docs/operations-manual/heat-procedures.md"
      provides: "All OPS-HEAT procedures for orchestration service operations"
      contains: "OPS-HEAT-001"
    - path: "docs/operations-manual/horizon-procedures.md"
      provides: "All OPS-HORIZON procedures for dashboard service operations"
      contains: "OPS-HORIZON-001"
  key_links:
    - from: "docs/operations-manual/heat-procedures.md"
      to: "skills/openstack/heat/SKILL.md"
      via: "procedure content derived from skill troubleshoot/operate sections"
      pattern: "NPR 7123.1"
    - from: "docs/operations-manual/horizon-procedures.md"
      to: "skills/openstack/horizon/SKILL.md"
      via: "procedure content derived from skill troubleshoot/operate sections"
      pattern: "NPR 7123.1"
---

<objective>
Create operations manual procedure files for Heat (orchestration) and Horizon (dashboard) -- the application-layer OpenStack services. Each file contains per-service procedures in NASA format covering daily health checks, configuration verification, backup/restore, upgrades, troubleshooting common failures, security audits, and service-specific operations.

Purpose: Heat and Horizon are the user-facing application layer. Operators need verified procedures for stack management, template validation, and dashboard maintenance.
Output: 2 procedure files in docs/operations-manual/ with OPS-{SERVICE}-{NNN} formatted procedures
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@skills/methodology/ops-manual-writer/SKILL.md
@skills/openstack/heat/SKILL.md
@skills/openstack/horizon/SKILL.md
@docs/filesystem-contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Heat orchestration service procedures</name>
  <files>docs/operations-manual/heat-procedures.md</files>
  <action>
Create the Heat operations manual procedure file following the exact format from ops-manual-writer SKILL.md. The file header should include a title, description, and table of contents.

Write these procedures using the OPS-HEAT-NNN format with all required sections (PURPOSE, PRECONDITIONS, SAFETY CONSIDERATIONS, PROCEDURE with numbered steps with Expected result and If unexpected branches, VERIFICATION, ROLLBACK, REFERENCES):

**Required standard categories:**
- OPS-HEAT-001: Service Health Check (daily) -- verify heat containers (heat_api, heat_api_cfn, heat_engine), check service list, verify stack operations functional, check logs
- OPS-HEAT-002: Configuration Verification -- confirm heat settings in globals.yml, verify stack domain user configuration, check convergence engine settings, validate heat template version support
- OPS-HEAT-003: Backup and Restore -- backup heat database, backup template repository, backup stack definitions, restore procedure with stack state verification
- OPS-HEAT-004: Minor Upgrade -- pre-upgrade stack state capture, kolla-ansible upgrade --tags heat, post-upgrade stack list and template version verification
- OPS-HEAT-005: Major Upgrade -- extended pre-checks, database migration, upgrade execution, template compatibility verification
- OPS-HEAT-006: Troubleshooting Common Failures -- stack create failures (resource dependency errors), stack update rollback failures, stack delete stuck (nested stacks), heat engine timeout, template validation errors
- OPS-HEAT-007: Security Audit -- stack domain user permissions, trusts configuration review, template policy restrictions, resource type restrictions

**Service-specific additions:**
- OPS-HEAT-008: Stack Template Validation -- validate HOT templates before deployment, check resource type availability, verify parameter constraints, preview stack resources
- OPS-HEAT-009: Resource Dependency Analysis -- inspect stack resource dependencies, identify circular references, analyze resource creation order, debug dependency failures
- OPS-HEAT-010: Orphan Cleanup -- identify orphaned stacks and resources, clean up failed stack remnants, purge deleted stack records, verify resource reclamation

All commands must use `openstack` CLI or `docker exec`. Include heat template validation syntax. Cross-reference OPS-KEYSTONE for auth and OPS-NOVA/OPS-NEUTRON/OPS-CINDER where Heat orchestrates those resources.

Use imperative mood. Include exact commands. Include HOT template syntax examples where relevant.
  </action>
  <verify>
Confirm file exists at docs/operations-manual/heat-procedures.md. Verify it contains all 10 OPS-HEAT procedures (001-010). Verify heat template validation commands are present. Verify cross-references to OPS-KEYSTONE, OPS-NOVA, OPS-NEUTRON, OPS-CINDER.
  </verify>
  <done>heat-procedures.md contains 10 complete procedures (OPS-HEAT-001 through OPS-HEAT-010) in NASA format with HOT template commands, cross-references to other service procedures, specific OpenStack doc URLs, and NASA cross-references</done>
</task>

<task type="auto">
  <name>Task 2: Create Horizon dashboard service procedures</name>
  <files>docs/operations-manual/horizon-procedures.md</files>
  <action>
Create the Horizon operations manual procedure file following the exact format from ops-manual-writer SKILL.md. The file header should include a title, description, and table of contents.

Write these procedures using the OPS-HORIZON-NNN format with all required sections:

**Required standard categories:**
- OPS-HORIZON-001: Service Health Check (daily) -- verify horizon container running (horizon), check web server response (HTTP 200 at dashboard URL), verify Keystone authentication through dashboard, check Apache/Nginx logs
- OPS-HORIZON-002: Configuration Verification -- confirm horizon settings in globals.yml, verify local_settings.py configuration (OPENSTACK_API_VERSIONS, SESSION_ENGINE, CACHES), check allowed hosts, validate SSL certificate
- OPS-HORIZON-003: Backup and Restore -- backup horizon configuration, backup custom panels/themes, backup local_settings.py customizations, restore procedure with login verification
- OPS-HORIZON-004: Minor Upgrade -- pre-upgrade config backup, kolla-ansible upgrade --tags horizon, post-upgrade login and panel accessibility verification
- OPS-HORIZON-005: Major Upgrade -- extended pre-checks, custom panel compatibility verification, upgrade execution, full dashboard functionality test
- OPS-HORIZON-006: Troubleshooting Common Failures -- 500 Internal Server Error (memcached connection, Keystone unreachable), blank dashboard after login, session timeout issues, static asset loading failures, CORS errors
- OPS-HORIZON-007: Security Audit -- session configuration review (cookie flags, timeout), CSP header verification, HTTPS enforcement check, password complexity settings, two-factor authentication status

**Service-specific additions:**
- OPS-HORIZON-008: Session Management -- configure session backend (memcached/database), set session timeout, clear stale sessions, monitor active sessions
- OPS-HORIZON-009: Custom Panel Deployment -- install custom dashboard panels, configure panel group ordering, enable/disable default panels, verify custom panel rendering
- OPS-HORIZON-010: Theme Configuration -- apply custom branding, modify color scheme and logos, configure help URLs, verify theme rendering across browsers

All commands must use `docker exec`, web requests via `curl`, or kolla-ansible commands. Horizon is a web application -- include HTTP-level verification commands (curl with status codes). Cross-reference OPS-KEYSTONE for authentication integration.

Use imperative mood. Include exact commands. Include curl-based verification for web endpoints.
  </action>
  <verify>
Confirm file exists at docs/operations-manual/horizon-procedures.md. Verify it contains all 10 OPS-HORIZON procedures (001-010). Verify curl-based HTTP verification commands are present. Verify cross-references to OPS-KEYSTONE.
  </verify>
  <done>horizon-procedures.md contains 10 complete procedures (OPS-HORIZON-001 through OPS-HORIZON-010) in NASA format with HTTP/curl verification commands, cross-references to Keystone, specific OpenStack doc URLs, and NASA cross-references</done>
</task>

</tasks>

<verification>
1. Both procedure files exist in docs/operations-manual/
2. Each file uses OPS-{SERVICE}-{NNN} procedure ID format
3. Every procedure contains all 7 required sections
4. Imperative mood used throughout
5. Exact commands in every procedure step with expected results
6. Service-specific tools included (heat template validation for Heat, curl for Horizon)
7. Cross-references use OPS-{SERVICE}-{NNN} format
8. NASA references include SP-6105 and NPR 7123.1 section numbers
9. OpenStack doc references are specific page URLs
</verification>

<success_criteria>
- 2 files created: heat-procedures.md, horizon-procedures.md
- 20 total procedures across the 2 files (10 + 10)
- Every procedure verifiable by doc-verifier agent
- NASA procedure format followed consistently
- Combined with Plans 01 and 02, all 8 OpenStack services have operations procedures
</success_criteria>

<output>
After completion, create `.planning/phases/320-operations-manual/320-03-SUMMARY.md`
</output>
