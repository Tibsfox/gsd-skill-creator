---
phase: 313-core-openstack-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/openstack/keystone/SKILL.md
  - skills/openstack/nova/SKILL.md
  - skills/openstack/glance/SKILL.md
autonomous: true
requirements:
  - SKILL-01
  - SKILL-06
  - SKILL-07

must_haves:
  truths:
    - "Keystone skill loads through the 6-stage pipeline and provides deploy/configure/operate/troubleshoot guidance for identity services"
    - "Nova skill loads through the 6-stage pipeline and provides deploy/configure/operate/troubleshoot guidance for compute services"
    - "Glance skill loads through the 6-stage pipeline and provides deploy/configure/operate/troubleshoot guidance for image services"
    - "Each skill contains troubleshooting sections for common failure modes"
    - "No individual skill exceeds 8K tokens"
  artifacts:
    - path: "skills/openstack/keystone/SKILL.md"
      provides: "Keystone identity service skill"
      contains: "name: openstack-keystone"
    - path: "skills/openstack/nova/SKILL.md"
      provides: "Nova compute service skill"
      contains: "name: openstack-nova"
    - path: "skills/openstack/glance/SKILL.md"
      provides: "Glance image service skill"
      contains: "name: openstack-glance"
  key_links:
    - from: "skills/openstack/keystone/SKILL.md"
      to: "skills/methodology/nasa-se/SKILL.md"
      via: "SE phase cross-references in deploy/verify sections"
      pattern: "SP-6105|NPR 7123"
    - from: "skills/openstack/nova/SKILL.md"
      to: "skills/openstack/keystone/SKILL.md"
      via: "Dependency reference: Nova depends on Keystone for auth"
      pattern: "keystone|identity|authentication"
    - from: "skills/openstack/glance/SKILL.md"
      to: "skills/openstack/keystone/SKILL.md"
      via: "Dependency reference: Glance depends on Keystone for auth"
      pattern: "keystone|identity|authentication"
---

<objective>
Create the first 3 core OpenStack service skills: Keystone (identity), Nova (compute), and Glance (images). These form the deployment foundation chain -- Keystone authenticates everything, Glance stores images, Nova launches instances.

Purpose: These are the most fundamental OpenStack services. Keystone is a dependency for every other service. Nova and Glance are the minimum viable compute path. Getting these right establishes the skill authoring pattern for all remaining skills.

Output: 3 SKILL.md files in skills/openstack/{keystone,nova,glance}/ following the established frontmatter pattern from the nasa-se methodology skill.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/methodology/nasa-se/SKILL.md
@docs/filesystem-contracts.md
@src/types/openstack.ts
@.planning/research/gsd-openstack-nasa-vision.md
@.planning/research/gsd-openstack-nasa-reference.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Keystone identity service skill</name>
  <files>skills/openstack/keystone/SKILL.md</files>
  <action>
Create `skills/openstack/keystone/SKILL.md` following the exact frontmatter pattern from `skills/methodology/nasa-se/SKILL.md`. Keystone is the identity service -- everything depends on it.

**Frontmatter requirements:**
- `name: openstack-keystone` (must pass validateSkillName)
- `description:` Max 1024 chars. Must mention: identity, authentication, service catalog, RBAC, token management. Must be useful for auto-triggering.
- `user-invocable: true`
- `allowed-tools: Read Grep Glob`
- `metadata.extensions.gsd-skill-creator:` with version, createdAt, triggers (intents + contexts)
- Trigger intents: "keystone", "identity", "authentication", "service catalog", "RBAC", "token", "federation", "policy.json", "endpoint"
- Trigger contexts: "deploying openstack", "configuring identity", "troubleshooting authentication", "managing users and projects"

**Body sections (all required):**

1. **Introduction** - What Keystone is, why it matters (everything authenticates through it), OpenStack dependency position (first service deployed, last decommissioned)

2. **Deploy** - Kolla-Ansible deployment specifics: globals.yml settings for keystone (`keystone_admin_password`, `keystone_database_password`, TLS settings), container verification (`docker ps | grep keystone`), service catalog registration, initial admin project/user creation, endpoint verification (`openstack endpoint list`)

3. **Configure** - Key configuration areas: Fernet token provider (rotation procedure), domain/project/user hierarchy, RBAC policies (policy.yaml customization), service catalog management, federation (SAML/OIDC basics for future reference), credential encryption, token expiration tuning

4. **Operate** - Day-2 operations: user/project/domain management, role assignment, quota management, token debugging, service catalog maintenance, credential rotation, Fernet key rotation procedure (with `kolla-ansible -i inventory keystone_fernet_rotate`), backup/restore of Keystone database

5. **Troubleshoot** - Common failure modes with symptoms and resolutions:
   - "401 Unauthorized" on all services (expired Fernet keys, misconfigured endpoints, clock skew)
   - Service catalog endpoint mismatch (internal vs public vs admin URLs)
   - Token validation failures (Fernet key rotation issues, memcached problems)
   - Database connection failures (MariaDB connectivity, credentials)
   - Federation/SSO issues (certificate problems, metadata sync)
   - RBAC policy denials (policy.yaml syntax, rule evaluation order)

6. **Integration Points** - How Keystone connects: every service authenticates via Keystone, middleware configuration (keystonemiddleware), service users and passwords, endpoint discovery

7. **NASA SE Cross-References** - Map to SE phases: Phase C (configuration), Phase D (verification with `openstack token issue`), Phase E (operations/token rotation)

**Token budget:** Target 6-7K tokens. Keystone is the most complex identity service -- use the full budget but do not exceed 8K. Be dense and practical, not verbose. Every line should be actionable.
  </action>
  <verify>
1. File exists: `ls skills/openstack/keystone/SKILL.md`
2. Frontmatter valid: YAML parses, name is "openstack-keystone", description under 1024 chars
3. All 7 sections present (Introduction, Deploy, Configure, Operate, Troubleshoot, Integration Points, NASA SE Cross-References)
4. Troubleshoot section has at least 5 failure mode entries
5. Token count estimate: `wc -c skills/openstack/keystone/SKILL.md` should be under 32000 bytes (rough 8K token proxy)
  </verify>
  <done>Keystone SKILL.md exists with valid frontmatter, all 7 sections, 5+ troubleshooting entries, under 8K token budget</done>
</task>

<task type="auto">
  <name>Task 2: Create Nova compute service skill</name>
  <files>skills/openstack/nova/SKILL.md</files>
  <action>
Create `skills/openstack/nova/SKILL.md` following the same pattern established in Task 1. Nova is the compute service -- instance lifecycle management.

**Frontmatter:**
- `name: openstack-nova`
- `description:` Mention: compute scheduling, instance lifecycle, flavors, live migration, hypervisor management, placement service
- Trigger intents: "nova", "compute", "instance", "server", "flavor", "live migration", "hypervisor", "placement", "scheduler"
- Trigger contexts: "deploying openstack compute", "managing instances", "troubleshooting compute", "capacity planning"

**Body sections:**

1. **Introduction** - Nova's role as the compute scheduler, relationship to libvirt/KVM, the placement service for resource tracking

2. **Deploy** - Kolla-Ansible specifics: globals.yml for nova (`nova_compute_virt_type: kvm` or `qemu` for nested, `enable_nova_libvirt_container: yes`), CPU allocation ratios, RAM allocation ratios, container verification, compute service registration (`openstack compute service list`), cell mapping (`nova-manage cell_v2 list_cells`)

3. **Configure** - Key areas: flavor management (standard set: m1.tiny through m1.xlarge), CPU pinning, NUMA topology, huge pages, live migration configuration, scheduler filters and weights, placement service, metadata service, VNC/SPICE console configuration

4. **Operate** - Day-2: instance lifecycle (create/stop/start/reboot/resize/migrate/evacuate), live migration procedures, compute node maintenance (disable/enable), orphan instance cleanup, quota management, aggregate and availability zone management, image caching

5. **Troubleshoot** - Common failure modes:
   - Instance stuck in BUILD/ERROR (scheduler failures, placement resource exhausted, image download failure)
   - Live migration failures (shared storage requirements, libvirt version mismatch, network config)
   - "No valid host" errors (scheduler filter analysis, placement resource inventory)
   - Console access failures (VNC proxy misconfiguration, firewall rules)
   - Compute service down (libvirt connectivity, hypervisor resource exhaustion)
   - Cell mapping issues (cell0 vs cell1, missing mappings)

6. **Integration Points** - Keystone auth, Glance image retrieval, Neutron port creation, Cinder volume attachment, Placement API, metadata service

7. **NASA SE Cross-References** - Phase C (compute configuration), Phase D (instance launch verification), Phase E (capacity management)

**Token budget:** Target 6-7K tokens. Dense, practical, actionable.
  </action>
  <verify>
1. File exists: `ls skills/openstack/nova/SKILL.md`
2. Frontmatter valid: name is "openstack-nova", description under 1024 chars
3. All 7 sections present
4. Troubleshoot section has at least 5 failure mode entries
5. `wc -c skills/openstack/nova/SKILL.md` under 32000 bytes
  </verify>
  <done>Nova SKILL.md exists with valid frontmatter, all 7 sections, 5+ troubleshooting entries, under 8K token budget</done>
</task>

<task type="auto">
  <name>Task 3: Create Glance image service skill</name>
  <files>skills/openstack/glance/SKILL.md</files>
  <action>
Create `skills/openstack/glance/SKILL.md` following the same pattern. Glance is the image registry -- stores and serves VM images.

**Frontmatter:**
- `name: openstack-glance`
- `description:` Mention: image registry, format conversion, metadata management, backend storage, image import
- Trigger intents: "glance", "image", "snapshot", "image format", "qcow2", "raw", "image import", "image registry"
- Trigger contexts: "deploying openstack images", "managing vm images", "troubleshooting image service", "converting image formats"

**Body sections:**

1. **Introduction** - Glance's role as image catalog and delivery service, supported formats (qcow2, raw, vmdk, vhd, iso), backend options (file, swift, ceph)

2. **Deploy** - Kolla-Ansible specifics: globals.yml for glance (`glance_backend_file: yes` for single-node, `glance_file_datadir`), container verification, backend configuration, image import task setup

3. **Configure** - Key areas: backend storage selection (file vs swift vs ceph), image format policies (which formats to accept), image size limits, image caching (glance-cache), metadata definitions, property protections, image import methods (web-download, glance-direct, copy-image)

4. **Operate** - Day-2: image upload/download, format conversion (qemu-img convert), image sharing between projects, snapshot management, image metadata management, storage usage monitoring, image cleanup (soft-deleted images)

5. **Troubleshoot** - Common failure modes:
   - Image upload failures (storage full, permission denied on data directory, size limits)
   - Image download slow/fails (backend connectivity, cache misses, network)
   - Format conversion errors (unsupported format, corrupted source)
   - Image stuck in "saving" state (backend write failure, timeout)
   - Metadata service issues (property protection blocking updates)

6. **Integration Points** - Keystone auth, Nova image retrieval for instance boot, Cinder volume-from-image, Swift as backend storage option

7. **NASA SE Cross-References** - Phase C (image backend configuration), Phase D (image upload/download verification), Phase E (image lifecycle management)

**Token budget:** Target 5-6K tokens. Glance is simpler than Keystone/Nova -- do not pad to fill budget. Be precise.
  </action>
  <verify>
1. File exists: `ls skills/openstack/glance/SKILL.md`
2. Frontmatter valid: name is "openstack-glance", description under 1024 chars
3. All 7 sections present
4. Troubleshoot section has at least 4 failure mode entries
5. `wc -c skills/openstack/glance/SKILL.md` under 32000 bytes
  </verify>
  <done>Glance SKILL.md exists with valid frontmatter, all 7 sections, 4+ troubleshooting entries, under 8K token budget</done>
</task>

</tasks>

<verification>
1. All 3 SKILL.md files exist in correct directories under skills/openstack/
2. Each file has valid YAML frontmatter that would pass skill metadata validation
3. Each skill has all 7 required sections (Introduction, Deploy, Configure, Operate, Troubleshoot, Integration Points, NASA SE Cross-References)
4. Each skill's troubleshoot section covers common failure modes specific to that service
5. No individual skill exceeds 8K tokens (proxy: under 32KB file size)
6. Skills cross-reference each other where appropriate (Nova references Keystone auth, Glance references Keystone auth)
</verification>

<success_criteria>
- 3 SKILL.md files created at skills/openstack/{keystone,nova,glance}/SKILL.md
- Each passes SKILL.md frontmatter validation (name, description, triggers)
- Each contains deploy/configure/operate/troubleshoot sections with actionable content
- Each contains troubleshooting sections for common failure modes
- No skill exceeds 8K token budget
- Skills are loadable by the 6-stage pipeline (valid structure and metadata)
</success_criteria>

<output>
After completion, create `.planning/phases/313-core-openstack-skills/313-01-SUMMARY.md`
</output>
