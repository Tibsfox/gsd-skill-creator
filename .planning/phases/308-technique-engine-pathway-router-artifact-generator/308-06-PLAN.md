---
phase: 308-technique-engine-pathway-router-artifact-generator
plan: "06"
type: tdd
wave: 3
depends_on: ["308-01", "308-02", "308-03", "308-04", "308-05"]
files_modified:
  - src/brainstorm/techniques/engine.test.ts
  - src/brainstorm/pathways/router.test.ts
  - src/brainstorm/artifacts/generator.test.ts
autonomous: true
requirements: [TECH-07, PATH-02, PATH-03, PATH-04]

must_haves:
  truths:
    - "All 15 (16) techniques load via loadTechnique() — no throws"
    - "Each technique's isComplete() implementation returns true under correct completion condition"
    - "Brainwriting Round 6 ideas have parent_id references to Round 5 ideas (verifiable chain)"
    - "SCAMPER generateRound() produces ideas with scamper_lens field for each of the 7 lenses"
    - "Six Thinking Hats generateRound() with phase='diverge' skips Black Hat (returns empty or next hat)"
    - "Five Whys chain reaches depth=5 with parent_id forming causal chain"
    - "Lotus Blossom isComplete() is false until exactly 64 ideas generated"
    - "PathwayRouter.recommendPathway() test cases match documented signal words"
    - "ArtifactGenerator.generateTranscript() contains phase headers and technique labels"
    - "ArtifactGenerator.generateActionPlan() includes source_idea_ids traced to content"
  artifacts:
    - path: "src/brainstorm/techniques/engine.test.ts"
      provides: "Engine integration tests + per-technique isComplete() + fidelity checks"
      contains: "describe('TechniqueEngine')"
    - path: "src/brainstorm/pathways/router.test.ts"
      provides: "Pathway routing signal word tests + adaptation signal handling tests"
      contains: "describe('PathwayRouter')"
    - path: "src/brainstorm/artifacts/generator.test.ts"
      provides: "Artifact generation tests for all 4 output formats"
      contains: "describe('ArtifactGenerator')"
  key_links:
    - from: "src/brainstorm/techniques/engine.test.ts"
      to: "src/brainstorm/techniques/engine.ts"
      via: "TechniqueEngine instantiation and loadTechnique() calls"
      pattern: "new TechniqueEngine\\(\\)"
    - from: "src/brainstorm/pathways/router.test.ts"
      to: "src/brainstorm/pathways/router.ts"
      via: "PathwayRouter.recommendPathway() test assertions"
      pattern: "recommendPathway"
---

<objective>
TDD verification suite for Technique Engine, Pathway Router, and Artifact Generator.

Purpose: The RED→GREEN→REFACTOR cycle validates all 15 technique isComplete() implementations, critical fidelity requirements (Brainwriting parent_id chains, Six Thinking Hats Black Hat phase skip, Five Whys causal depth, Lotus Blossom 64-idea threshold), pathway signal word routing, and artifact output correctness.
Output: 3 test files covering all TECH-07, PATH-02/03/04 requirements with semantic fidelity checks beyond structural validation.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/brainstorm/shared/types.ts
@src/brainstorm/shared/constants.ts
@.planning/input/v1.32/07-technique-engine-spec.md
@.planning/research/PITFALLS.md
@.planning/phases/308-technique-engine-pathway-router-artifact-generator/308-01-SUMMARY.md
@.planning/phases/308-technique-engine-pathway-router-artifact-generator/308-03-SUMMARY.md
@.planning/phases/308-technique-engine-pathway-router-artifact-generator/308-05-SUMMARY.md
</context>

<feature>
  <name>Technique Engine + Pathway Router + Artifact Generator Test Suite</name>
  <files>
    src/brainstorm/techniques/engine.test.ts,
    src/brainstorm/pathways/router.test.ts,
    src/brainstorm/artifacts/generator.test.ts
  </files>
  <behavior>
**engine.test.ts — RED phase test list:**

1. `loadTechnique(id) - loads all 15 techniques without throwing`
   - Input: call loadTechnique() for each TechniqueId
   - Expected: returns TechniqueInstance with id, config, initialize, generateRound, getState, isComplete

2. `listByCategory - returns correct counts per category`
   - Input: listByCategory('individual'), listByCategory('collaborative'), listByCategory('analytical'), listByCategory('visual')
   - Expected: 4, 5, 4, 3 (totals 16 due to lotus-blossom being included)

3. `freewriting.isComplete() - returns true at stream target`
   - Input: initialize, generate enough rounds to exceed min_ideas_per_minute × duration threshold OR exceed default_duration_ms
   - Expected: isComplete() returns true

4. `rapid-ideation.isComplete() - returns true at 10 ideas`
   - Input: initialize, call generateRound() until total ideas ≥ 10
   - Expected: isComplete() returns true after ≥ 10 ideas

5. `question-brainstorming.isComplete() - returns true at 15 questions`
   - Input: initialize, generate rounds until total questions ≥ 15
   - Expected: isComplete() returns true

6. `brainwriting-635 - Round 6 ideas have parent_id from Round 5`
   - Input: run all 6 rounds with generateRound()
   - Expected: round 6 ideas ALL have parent_id set to an id from round 5 ideas
   - SEMANTIC CHECK: round 6 ideas are not identical in content to round 1 ideas

7. `rolestorming - perspective field populated on all ideas`
   - Input: initialize, run 2 rounds
   - Expected: ALL returned ideas have non-empty perspective field

8. `scamper - all 7 lenses produce ideas with scamper_lens field`
   - Input: run enough rounds to cover all 7 lenses
   - Expected: ideas present for each ScamperLens value ('substitute', 'combine', 'adapt', 'modify', 'put-to-another-use', 'eliminate', 'reverse')

9. `six-thinking-hats Black Hat - skips during diverge phase`
   - Input: initialize with session.phase = 'diverge', run rounds until Black Hat would be reached
   - Expected: NO ideas have hat_color: 'black' in the output when phase is 'diverge'

10. `five-whys - chain reaches depth 5 with parent_id`
    - Input: initialize with problem statement, run enough rounds to complete chain
    - Expected: at least one question has depth: 5; chain from depth 1 to 5 is traceable via parent_id
    - CAUSAL CHECK: each question in chain has parent_id pointing to depth-1 lower question

11. `starbursting - covers all 6 W-categories`
    - Input: run enough rounds to cover all categories
    - Expected: questions present for all of: 'who', 'what', 'where', 'when', 'why', 'how'

12. `lotus-blossom - isComplete() false before 64 ideas`
    - Input: run 5 rounds (partial completion)
    - Expected: isComplete() returns false; idea count < 64

13. `affinity-mapping - clusters empty previous_output gracefully`
    - Input: generateRound(1, []) — empty idea array
    - Expected: no throw; prompts field contains message about needing ideas first

14. `affinity-mapping - cluster count between 2 and 8`
    - Input: generateRound(1, 20 mock ideas)
    - Expected: cluster count between 2 and 8 inclusive

**router.test.ts — RED phase test list:**

1. `recommendPathway - routes "how might we reduce customer churn" to problem-solving`
   - Input: `router.recommendPathway("how might we reduce customer churn")`
   - Expected: `'problem-solving'`

2. `recommendPathway - routes "imagine new possibilities for our product" to creative-exploration`
   - Input: `router.recommendPathway("imagine new possibilities for our product")`
   - Expected: `'creative-exploration'`

3. `recommendPathway - returns free-form for unrecognized input`
   - Input: `router.recommendPathway("xyz abc 123")`
   - Expected: `'free-form'`

4. `getPathway('free-form') - returns empty technique_sequence`
   - Input: `router.getPathway('free-form')`
   - Expected: `pathway.technique_sequence.length === 0`

5. `getTechniqueQueue - returns ordered technique IDs for problem-solving`
   - Input: `router.getTechniqueQueue('problem-solving')`
   - Expected: first technique is 'question-brainstorming'

6. `adaptTechniqueQueue - low_energy signal inserts rapid-ideation`
   - Input: queue = ['mind-mapping', 'affinity-mapping'], signal = {type: 'low_energy', completed: []}
   - Expected: 'rapid-ideation' or 'rolestorming' appears early in returned queue

7. `adaptTechniqueQueue - user_request moves requested technique to front`
   - Input: queue = ['freewriting', 'mind-mapping'], signal = {type: 'user_request', requested_technique: 'scamper'}
   - Expected: returned queue[0] === 'scamper'

8. `adaptTechniqueQueue - does not modify completed techniques`
   - Input: session with techniques_used = ['freewriting'], queue = ['mind-mapping']
   - Expected: adapted queue does not contain 'freewriting' again

**generator.test.ts — RED phase test list:**

1. `generateTranscript - contains phase headers`
   - Input: mock SessionState with phase_history containing 'diverge' and 'organize'
   - Expected: output contains '## Phase: Diverge' and '## Phase: Organize'

2. `generateTranscript - contains technique labels`
   - Input: mock SessionState with ideas from 'freewriting' and 'scamper' techniques
   - Expected: output contains 'Technique: Freewriting' and 'Technique: SCAMPER'

3. `generateTranscript - contains idea content`
   - Input: mock SessionState with 3 ideas
   - Expected: all 3 idea contents appear in transcript

4. `generateActionPlan - includes source_idea_ids traced to content`
   - Input: mock SessionState with action_items referencing source_idea_ids, ideas matching those IDs
   - Expected: each action item row includes the original source idea text

5. `generateActionPlan - handles missing source idea gracefully`
   - Input: action_item with source_idea_ids pointing to non-existent idea
   - Expected: 'No source idea linked' appears, no throw

6. `generateJsonExport - produces parseable JSON`
   - Input: mock SessionState
   - Expected: JSON.parse(result) does not throw; parsed object has 'id', 'phase', 'ideas' keys

7. `generateClusterMap - groups ideas by cluster with unassigned section`
   - Input: mock SessionState with 2 clusters and 1 unassigned idea
   - Expected: output contains both cluster labels and 'Unassigned Ideas' section
  </behavior>
  <implementation>
Follow RED→GREEN→REFACTOR cycle:

**RED phase:**
- Write all test cases in test files (they will fail because implementations exist but may have edge cases)
- Run: `npx vitest run src/brainstorm/techniques/engine.test.ts src/brainstorm/pathways/router.test.ts src/brainstorm/artifacts/generator.test.ts`
- Expected: some tests fail (particularly the semantic fidelity tests and edge cases)
- Commit: `test(308-06): add failing technique engine, pathway, and artifact tests`

**GREEN phase:**
- Fix any implementation gaps revealed by failing tests
- Common fixes needed:
  - Six Thinking Hats Black Hat skip during diverge (if not implemented correctly)
  - Affinity mapping empty input handling
  - Action plan source idea lookup
  - Pathway signal word matching edge cases
- Run tests until all pass
- Commit: `feat(308-06): fix technique implementations to pass fidelity tests`

**REFACTOR phase (if needed):**
- Remove any test code duplication with shared helpers
- Ensure mock SessionState helper is reusable across test files
- Run: `npx vitest run` — all tests must still pass
- Commit: `refactor(308-06): extract shared test helpers for session state mocks`

**Mock SessionState helper** — create inline in test files (not a shared module — no shared test infrastructure needed at this phase):
```typescript
function mockSessionState(overrides: Partial<SessionState> = {}): SessionState {
  return {
    id: crypto.randomUUID(),
    status: 'active',
    phase: 'diverge',
    problem_statement: 'How might we reduce customer churn?',
    active_technique: null,
    active_pathway: null,
    technique_queue: [],
    active_agents: ['facilitator', 'ideator', 'scribe'],
    ideas: [],
    questions: [],
    clusters: [],
    evaluations: [],
    action_items: [],
    timer: { technique_timer: null, session_timer: { elapsed_ms: 0 }, is_paused: false },
    energy_level: 'high',
    rules_active: ['quantity', 'no-criticism', 'wild-ideas', 'build-combine'],
    metadata: {
      created_at: Date.now(),
      updated_at: Date.now(),
      total_ideas: 0,
      total_questions: 0,
      techniques_used: [],
      phase_history: [{ phase: 'explore', entered_at: Date.now() }],
    },
    ...overrides,
  };
}
```

Use `import { randomUUID } from 'node:crypto'` for UUIDs in test helpers.
  </implementation>
</feature>

<verification>
Run: `npx vitest run src/brainstorm/techniques/engine.test.ts src/brainstorm/pathways/router.test.ts src/brainstorm/artifacts/generator.test.ts --reporter=verbose 2>&1 | tail -30`
Expected: All tests pass. Output shows test counts for each file.

Run: `npx vitest run src/brainstorm/ --reporter=verbose 2>&1 | grep -E "PASS|FAIL|Tests" | tail -10`
Expected: All brainstorm tests pass including Phase 305 tests.
</verification>

<success_criteria>
- All 14 engine tests pass (including Brainwriting parent_id chain, Six Thinking Hats Black Hat skip, Five Whys depth 5, Lotus Blossom 64 threshold)
- All 8 pathway router tests pass (including signal word routing)
- All 7 artifact generator tests pass (including source_idea_ids tracing)
- No regressions in Phase 305 tests (src/brainstorm/shared/*.test.ts)
- Total brainstorm test count increases by ≥29 tests
</success_criteria>

<output>
After completion, create `.planning/phases/308-technique-engine-pathway-router-artifact-generator/308-06-SUMMARY.md`
</output>
