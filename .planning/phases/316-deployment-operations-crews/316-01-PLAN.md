---
phase: 316-deployment-operations-crews
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - configs/crews/deployment-crew.yaml
autonomous: true
requirements: [CREW-01, CREW-04, CREW-05, CREW-06, CREW-07]

must_haves:
  truths:
    - "Deployment crew YAML defines all 12 roles (FLIGHT, PLAN, EXEC x3, CRAFT-network, CRAFT-security, CRAFT-storage, VERIFY, INTEG, SCOUT, CAPCOM, BUDGET, TOPO) at Squadron profile"
    - "Each EXEC agent has domain-specific skill loadout (exec-keystone gets keystone+kolla-ansible; exec-compute gets nova+glance+kolla-ansible; exec-network-storage gets neutron+cinder+kolla-ansible)"
    - "CRAFT agents have keyword triggers (neutron/network/SDN for craft-network; RBAC/policy/certificate for craft-security; volume/object/backup for craft-storage)"
    - "CAPCOM is marked as sole human interface with no other agent having human_interface: true"
    - "Scout (3 roles), Patrol (7 roles), and Squadron (all 12 roles) activation profiles are defined with correct role subsets"
  artifacts:
    - path: "configs/crews/deployment-crew.yaml"
      provides: "Complete deployment crew configuration with 12 roles, 3 activation profiles, skill loadouts, and trigger definitions"
      contains: "deployment-crew"
  key_links:
    - from: "configs/crews/deployment-crew.yaml"
      to: "skills/openstack/*/SKILL.md"
      via: "skill name references in agent skill loadouts"
      pattern: "skills:.*openstack-(keystone|nova|neutron|cinder|glance|kolla-ansible)"
    - from: "configs/crews/deployment-crew.yaml"
      to: ".chipset/chipset.yaml"
      via: "activation profile pattern matching chipset agents section"
      pattern: "activation_profiles:"
---

<objective>
Create the deployment crew configuration YAML defining all 12 roles with correct skill loadouts, CRAFT trigger keywords, CAPCOM as sole human interface, and Scout/Patrol/Squadron activation profiles.

Purpose: The deployment crew is the primary agent team for the OpenStack cloud deployment mission. It exercises the full Squadron activation profile from the mission crew manifest, with specialized EXEC agents for parallel service deployment and CRAFT agents for domain-specific expertise.

Output: `configs/crews/deployment-crew.yaml` -- complete crew configuration ready for chipset integration in Phase 318.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/gsd-openstack-nasa-vision.md
@docs/filesystem-contracts.md
@.chipset/chipset.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deployment crew configuration with 12 roles and skill loadouts</name>
  <files>configs/crews/deployment-crew.yaml</files>
  <action>
Create `configs/crews/deployment-crew.yaml` defining the deployment crew configuration. First create the `configs/crews/` directory if it does not exist.

The YAML structure must follow the chipset pattern established in `.chipset/chipset.yaml` (positions array with id, role, context, lifecycle, activationTrigger, skillRequirements) but adapted for crew-specific fields.

**Top-level metadata:**
```yaml
name: deployment-crew
version: 1.0.0
description: "OpenStack deployment crew -- 12 roles for full Squadron-class cloud deployment through NASA SE methodology"
mission_phase: "Formulation through Integration"
```

**Define all 12 agent positions:**

1. `flight` -- role: FLIGHT, context: main, lifecycle: persistent, skills: [nasa-se-methodology], tier: 1 (mission control). Orchestrates full deployment sequence via command loop.

2. `plan` -- role: PLAN, context: fork, lifecycle: task, skills: [nasa-se-methodology], tier: 2. Decomposes OpenStack deployment into phases.

3. `exec-keystone` -- role: EXEC, context: fork, lifecycle: task, domain: identity, skills: [openstack-keystone, openstack-kolla-ansible], tier: 2. Deploys Keystone identity service.

4. `exec-compute` -- role: EXEC, context: fork, lifecycle: task, domain: compute, skills: [openstack-nova, openstack-glance, openstack-kolla-ansible], tier: 2. Deploys Nova compute and Glance image services.

5. `exec-network-storage` -- role: EXEC, context: fork, lifecycle: task, domain: network-storage, skills: [openstack-neutron, openstack-cinder, openstack-kolla-ansible], tier: 2. Deploys Neutron networking and Cinder block storage.

6. `craft-network` -- role: CRAFT, context: fork, lifecycle: on-demand, domain: sdn, skills: [openstack-neutron, openstack-networking-debug], tier: 3. Triggers: ["neutron", "network", "SDN", "floating IP", "security group", "OVS", "OVN", "DHCP"].

7. `craft-security` -- role: CRAFT, context: fork, lifecycle: on-demand, domain: security, skills: [openstack-keystone, openstack-security], tier: 3. Triggers: ["RBAC", "policy", "certificate", "hardening", "TLS", "CVE", "audit"].

8. `craft-storage` -- role: CRAFT, context: fork, lifecycle: on-demand, domain: storage, skills: [openstack-cinder, openstack-swift, openstack-backup], tier: 3. Triggers: ["volume", "object", "backup", "snapshot", "LVM", "NFS"].

9. `verify` -- role: VERIFY, context: fork, lifecycle: task, skills: [doc-verifier], tier: 2. Runs service health checks, API validation, integration tests. Must be independent from EXEC (cannot access EXEC implementation context).

10. `integ` -- role: INTEG, context: fork, lifecycle: task, skills: [], tier: 2. Monitors cross-service interfaces (Nova-Neutron, Nova-Cinder, etc.).

11. `scout` -- role: SCOUT, context: fork, lifecycle: task, skills: [openstack-kolla-ansible], tier: 3. Pre-deployment hardware survey and dependency assessment.

12. `capcom` -- role: CAPCOM, context: main, lifecycle: persistent, skills: [], tier: 2, human_interface: true. The SOLE channel crossing the human-machine boundary. No other agent has human_interface: true.

13. `budget` -- role: BUDGET, context: main, lifecycle: persistent, skills: [], tier: 3. Token and resource consumption tracking.

14. `topo` -- role: TOPO, context: main, lifecycle: persistent, skills: [], tier: 2. Manages team topology changes as deployment progresses.

Note: That is 14 entries but the spec says 12 roles. BUDGET and TOPO are included per the vision document's deployment crew table. Count FLIGHT, PLAN, EXEC(x3), CRAFT(x3), VERIFY, INTEG, SCOUT, CAPCOM, BUDGET, TOPO = 14 positions but 12 distinct role types (EXEC x3 = 1 role type, CRAFT x3 = 1 role type). List all 14 positions.

**Communication loop assignments:** Add a `communication_loop` field to each position:
- FLIGHT: command
- PLAN: execution
- EXEC-*: execution
- CRAFT-*: specialist
- VERIFY: execution
- INTEG: execution
- SCOUT: specialist
- CAPCOM: user
- BUDGET: budget
- TOPO: command

**Activation profiles section:**

```yaml
activation_profiles:
  scout:
    description: "Quick OpenStack config change or single-service fix"
    roles: [FLIGHT, EXEC, VERIFY]
    positions: [flight, exec-keystone, verify]

  patrol:
    description: "Standard service deployment or upgrade"
    roles: [FLIGHT, PLAN, EXEC, VERIFY, CAPCOM, BUDGET, LOG]
    positions: [flight, plan, exec-keystone, exec-compute, exec-network-storage, verify, capcom, budget]

  squadron:
    description: "Full OpenStack deployment with documentation"
    roles: all
    positions: all
```

Note for patrol: Use 7 distinct role types as specified in CREW-04. The LOG role is not in deployment crew; substitute with TOPO to keep 7: [FLIGHT, PLAN, EXEC, VERIFY, CAPCOM, BUDGET, TOPO]. Positions count will be 9 (EXEC x3 expands).

**CAPCOM isolation rule:** Add a top-level field:
```yaml
human_interface:
  sole_agent: capcom
  rule: "No agent other than CAPCOM may send messages directly to the human operator"
```

Add YAML comments throughout explaining the role hierarchy (Tier 1: FLIGHT, Tier 2: PLAN/EXEC/VERIFY/INTEG/CAPCOM/TOPO, Tier 3: CRAFT/SCOUT/BUDGET) and skill loadout rationale.
  </action>
  <verify>
1. File exists at `configs/crews/deployment-crew.yaml`
2. YAML parses without errors: `python3 -c "import yaml; yaml.safe_load(open('configs/crews/deployment-crew.yaml'))" && echo "VALID"`
3. Grep confirms all 14 positions defined: grep for each of flight, plan, exec-keystone, exec-compute, exec-network-storage, craft-network, craft-security, craft-storage, verify, integ, scout, capcom, budget, topo
4. Grep confirms 3 activation profiles (scout, patrol, squadron)
5. Grep confirms CAPCOM has human_interface: true
6. Grep confirms no other position has human_interface: true
7. Grep confirms skill references: openstack-keystone, openstack-nova, openstack-neutron, openstack-cinder, openstack-glance, openstack-kolla-ansible, nasa-se-methodology
  </verify>
  <done>
- Deployment crew YAML defines all 14 positions (12 role types) with correct skill loadouts per vision document
- 3 EXEC agents each have domain-specific skills (identity, compute, network-storage)
- 3 CRAFT agents each have domain keyword triggers
- CAPCOM is sole human interface
- Scout (3 roles), Patrol (7 roles), Squadron (all) activation profiles defined
- YAML validates without parse errors
  </done>
</task>

</tasks>

<verification>
- `configs/crews/deployment-crew.yaml` exists and parses as valid YAML
- All 14 positions present with correct role assignments
- Skill loadouts match vision document Section 1.2 Deployment Crew table
- CRAFT triggers match vision document Section 1.4 Chipset Configuration
- Activation profiles correctly subset roles per CREW-04
- CAPCOM isolation rule enforced per CREW-07
</verification>

<success_criteria>
1. Deployment crew YAML is valid, complete, and defines all roles from the vision document's Deployment Crew table
2. Each EXEC agent has the correct domain-specific skill loadout (CREW-05)
3. Each CRAFT agent triggers on correct domain keywords (CREW-06)
4. CAPCOM is sole human interface (CREW-07)
5. Three activation profiles (Scout/Patrol/Squadron) correctly subset roles (CREW-04)
</success_criteria>

<output>
After completion, create `.planning/phases/316-deployment-operations-crews/316-01-SUMMARY.md`
</output>
