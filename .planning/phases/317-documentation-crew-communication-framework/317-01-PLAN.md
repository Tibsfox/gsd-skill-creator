---
phase: 317-documentation-crew-communication-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - configs/crews/documentation-crew.yaml
autonomous: true
requirements: [CREW-03]

must_haves:
  truths:
    - "Documentation crew YAML defines all 8 roles (FLIGHT, PLAN, EXEC x2, CRAFT-techwriter, VERIFY, ANALYST, RETRO, PAO) for parallel documentation production"
    - "CRAFT-techwriter receives nasa-se-methodology skill for NASA document standards"
    - "EXEC agents have documentation-specific skill loadouts (ops-manual-writer, runbook-generator)"
    - "Documentation crew can operate in parallel with deployment and operations crews (no position conflicts)"
    - "CAPCOM is not in documentation crew (documentation crew receives directives from FLIGHT, not directly from human)"
    - "Scout/Patrol/Squadron activation profiles correctly subset documentation crew roles"
  artifacts:
    - path: "configs/crews/documentation-crew.yaml"
      provides: "Complete documentation crew configuration with 8 roles, 3 activation profiles, skill loadouts, and parallel operation capability"
      contains: "documentation-crew"
  key_links:
    - from: "configs/crews/documentation-crew.yaml"
      to: "skills/methodology/*/SKILL.md"
      via: "skill name references in agent skill loadouts"
      pattern: "skills:.*nasa-se-methodology|ops-manual-writer|runbook-generator|doc-verifier"
    - from: "configs/crews/documentation-crew.yaml"
      to: ".chipset/chipset.yaml"
      via: "activation profile pattern matching chipset agents section"
      pattern: "activation_profiles:"
---

<objective>
Create the documentation crew configuration YAML defining all 8 roles with correct skill loadouts for parallel documentation production, including CRAFT-techwriter with NASA SE skill and EXEC agents with documentation skills.

Purpose: The documentation crew operates continuously across all mission phases, producing and verifying NASA SE-structured documentation in parallel with deployment and operations activities. Unlike the other crews, it has no CAPCOM (it receives work through FLIGHT directives) and its EXEC agents produce documentation artifacts rather than infrastructure changes.

Output: `configs/crews/documentation-crew.yaml` -- complete crew configuration ready for chipset integration in Phase 318.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/gsd-openstack-nasa-vision.md
@docs/filesystem-contracts.md
@.planning/phases/316-deployment-operations-crews/316-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create documentation crew configuration with 8 roles and documentation skill loadouts</name>
  <files>configs/crews/documentation-crew.yaml</files>
  <action>
Create `configs/crews/documentation-crew.yaml` defining the documentation crew. Create the `configs/crews/` directory if it does not exist (Phase 316 may have already created it; check first with `ls`).

Follow the SAME YAML structure established by Phase 316's crew files (`configs/crews/deployment-crew.yaml` pattern). Read that file first if it exists; if Phase 316 has not yet executed, use the structure from the 316-01-PLAN.md action section as the template.

**Top-level metadata:**
```yaml
name: documentation-crew
version: 1.0.0
description: "OpenStack documentation crew -- 8 roles for continuous NASA SE-structured documentation production in parallel with other crews"
mission_phase: "Continuous across all phases (Formulation through Closeout)"
parallel_operation: true
```

The `parallel_operation: true` field distinguishes this crew from deployment/operations crews -- documentation crew runs alongside them, not sequentially.

**Define all 8 agent positions per the vision document Documentation Crew table:**

1. `flight` -- role: FLIGHT, context: main, lifecycle: persistent, skills: [nasa-se-methodology], tier: 1, communication_loop: command. Coordinates documentation production across all NASA SE phases. NOTE: This is a separate FLIGHT instance from the deployment/operations FLIGHT -- documentation FLIGHT coordinates doc work only.

2. `plan` -- role: PLAN, context: fork, lifecycle: task, skills: [nasa-se-methodology], tier: 2, communication_loop: execution. Maps NASA SE phases to documentation deliverables. Determines which documents are due at each phase gate.

3. `exec-procedures` -- role: EXEC, context: fork, lifecycle: task, domain: procedures, skills: [ops-manual-writer, openstack-kolla-ansible], tier: 2, communication_loop: execution. Writes operations manual procedures following NASA procedure format (OPS-{SERVICE}-{NUMBER}).

4. `exec-runbooks` -- role: EXEC, context: fork, lifecycle: task, domain: runbooks, skills: [runbook-generator, openstack-kolla-ansible], tier: 2, communication_loop: execution. Writes runbook entries following standard format (RB-{SERVICE}-{NNN}) with dual task/symptom indexing.

5. `craft-techwriter` -- role: CRAFT, context: fork, lifecycle: on-demand, domain: technical-writing, skills: [nasa-se-methodology, ops-manual-writer, runbook-generator], tier: 3, communication_loop: specialist. NASA document standards specialist. Triggers: ["document standard", "procedure format", "technical writing", "NASA format", "ConOps", "SEMP", "cross-reference", "SE phase"].

6. `verify` -- role: VERIFY, context: fork, lifecycle: task, skills: [doc-verifier], tier: 2, communication_loop: execution. Cross-references documentation against running system configuration. Independent from EXEC -- cannot access EXEC's draft context. Uses doc-verifier skill's 4 drift detection methods (configuration, endpoint, procedure, version).

7. `analyst` -- role: ANALYST, context: fork, lifecycle: task, skills: [], tier: 3, communication_loop: observation. Studies deployment and operations patterns for curriculum content. Feeds observations into the skill-creator pipeline.

8. `retro` -- role: RETRO, context: fork, lifecycle: task, skills: [nasa-se-methodology], tier: 3, communication_loop: observation. Post-phase analysis and lessons learned capture following NASA LLIS format.

9. `pao` -- role: PAO, context: fork, lifecycle: task, skills: [ops-manual-writer], tier: 3, communication_loop: command. Generates human-readable mission narrative and changelog. Public Affairs Officer for mission communication.

Note: That is 9 entries. The vision spec says "8 roles" but lists 9 positions (FLIGHT, PLAN, EXEC x2, CRAFT-techwriter, VERIFY, ANALYST, RETRO, PAO). Count distinct role TYPES: FLIGHT, PLAN, EXEC, CRAFT, VERIFY, ANALYST, RETRO, PAO = 8 role types (EXEC x2 = 1 type). List all 9 positions.

**No CAPCOM in documentation crew.** Add a comment explaining:
```yaml
# Documentation crew has no CAPCOM. Human interaction flows through the
# deployment or operations crew's CAPCOM. Documentation FLIGHT receives
# directives from the mission-level FLIGHT director, not directly from
# the human operator. This enables the documentation crew to operate
# asynchronously and in parallel with the active operational crew.
```

**Communication loop assignments:**
- FLIGHT: command
- PLAN: execution
- EXEC-procedures: execution
- EXEC-runbooks: execution
- CRAFT-techwriter: specialist
- VERIFY: execution
- ANALYST: observation
- RETRO: observation
- PAO: command

**Activation profiles section:**
```yaml
activation_profiles:
  scout:
    description: "Quick documentation update or single procedure revision"
    roles: [FLIGHT, EXEC, VERIFY]
    positions: [flight, exec-procedures, verify]

  patrol:
    description: "Standard documentation production for a phase gate deliverable"
    roles: [FLIGHT, PLAN, EXEC, VERIFY, CRAFT, PAO]
    positions: [flight, plan, exec-procedures, exec-runbooks, craft-techwriter, verify, pao]

  squadron:
    description: "Full documentation production -- complete phase documentation set"
    roles: all
    positions: all
```

**Parallel operation rules section:**
```yaml
parallel_operation_rules:
  - rule: "Documentation crew FLIGHT coordinates with mission FLIGHT via command loop"
    mechanism: "command loop priority 1 messages"
  - rule: "Documentation crew VERIFY reads system state but never modifies it"
    mechanism: "read-only access to OpenStack API endpoints and config files"
  - rule: "Documentation crew does not block deployment or operations activities"
    mechanism: "observation loop (priority 6) for non-critical pattern data"
  - rule: "Doc-sync loop alerts documentation crew when system configuration changes"
    mechanism: "doc-sync loop (priority 4) drift detection triggers"
```

Add YAML comments throughout explaining the role hierarchy (Tier 1: FLIGHT, Tier 2: PLAN/EXEC/VERIFY, Tier 3: CRAFT-techwriter/ANALYST/RETRO/PAO) and how documentation crew differs from deployment and operations crews (parallel operation, no CAPCOM, EXEC agents write docs not infrastructure, ANALYST and RETRO for learning/retrospective).
  </action>
  <verify>
1. File exists at `configs/crews/documentation-crew.yaml`
2. YAML parses without errors: `python3 -c "import yaml; yaml.safe_load(open('configs/crews/documentation-crew.yaml'))" && echo "VALID"`
3. Grep confirms all 9 positions: flight, plan, exec-procedures, exec-runbooks, craft-techwriter, verify, analyst, retro, pao
4. Grep confirms 3 activation profiles (scout, patrol, squadron)
5. Grep confirms NO capcom or human_interface in file
6. Grep confirms parallel_operation: true
7. Grep confirms skill references: nasa-se-methodology, ops-manual-writer, runbook-generator, doc-verifier
8. Grep confirms craft-techwriter has triggers including "NASA format" and "technical writing"
  </verify>
  <done>
- Documentation crew YAML defines all 9 positions (8 role types) with correct skill loadouts per vision document
- CRAFT-techwriter receives nasa-se-methodology skill for NASA document standards (CREW-03)
- 2 EXEC agents have documentation-specific skills (ops-manual-writer for procedures, runbook-generator for runbooks)
- No CAPCOM present -- documentation crew receives directives through FLIGHT
- Parallel operation rules defined for concurrent operation with deployment/operations crews
- Scout (3 roles), Patrol (7 roles), Squadron (all) activation profiles defined
- YAML validates without parse errors
  </done>
</task>

</tasks>

<verification>
- `configs/crews/documentation-crew.yaml` exists and parses as valid YAML
- All 9 positions present with correct role assignments matching vision document Documentation Crew table
- Skill loadouts include nasa-se-methodology on CRAFT-techwriter, ops-manual-writer and runbook-generator on EXEC agents
- No CAPCOM or human_interface in documentation crew
- Parallel operation capability declared and rules defined
- Activation profiles correctly subset roles per established CREW-04 pattern
</verification>

<success_criteria>
1. Documentation crew YAML is valid, complete, and defines all 8 role types (9 positions) from the vision document's Documentation Crew table (CREW-03)
2. CRAFT-techwriter receives NASA SE skill and has domain-specific triggers
3. EXEC agents have documentation-specific skill loadouts (ops-manual-writer, runbook-generator)
4. Documentation crew can operate in parallel with deployment and operations crews
5. Three activation profiles (Scout/Patrol/Squadron) correctly subset roles
</success_criteria>

<output>
After completion, create `.planning/phases/317-documentation-crew-communication-framework/317-01-SUMMARY.md`
</output>
