---
phase: 290-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vtm/index.ts
  - src/vtm/pipeline.ts
  - src/vtm/vtm-pipeline.ts
  - src/vtm/chipset.yaml
autonomous: true
requirements: [INTG-01, INTG-02, INTG-03]

must_haves:
  truths:
    - "Importing from src/vtm/index.ts provides every public function, type, and constant exported by all 14 VTM source modules"
    - "A chipset.yaml file describes the VTM module's skills, agents with pipeline topology, and evaluation gates"
    - "VTMPipeline class wraps runPipeline and other functional API calls with a stateful OOP interface, following the createXxx factory pattern used by Den modules"
    - "The var keyword at pipeline.ts:447 is replaced with let and generateReadme is wired into buildFileManifest"
  artifacts:
    - path: "src/vtm/index.ts"
      provides: "Complete barrel export for all VTM public API"
      min_lines: 50
    - path: "src/vtm/chipset.yaml"
      provides: "Chipset configuration for VTM module"
      contains: "topology"
    - path: "src/vtm/vtm-pipeline.ts"
      provides: "VTMPipeline class wrapper with createVTMPipeline factory"
      exports: ["VTMPipeline", "createVTMPipeline"]
    - path: "src/vtm/pipeline.ts"
      provides: "Tech debt fixes: var->let, generateReadme wired"
  key_links:
    - from: "src/vtm/index.ts"
      to: "src/vtm/vtm-pipeline.ts"
      via: "export * from './vtm-pipeline.js'"
      pattern: "export.*vtm-pipeline"
    - from: "src/vtm/vtm-pipeline.ts"
      to: "src/vtm/pipeline.ts"
      via: "import { runPipeline, selectPipelineSpeed } from './pipeline.js'"
      pattern: "import.*runPipeline.*pipeline"
    - from: "src/vtm/pipeline.ts"
      to: "src/vtm/mission-assembly.ts"
      via: "generateReadme called in buildFileManifest or mission stage"
      pattern: "generateReadme"
---

<objective>
Verify and complete barrel exports, create chipset YAML for the VTM module, add the functional API + class wrapper pattern, and fix known tech debt.

Purpose: Satisfy INTG-01 (barrel exports), INTG-02 (chipset YAML), and INTG-03 (class wrapper pattern) while cleaning up pipeline.ts tech debt from the milestone audit.
Output: Updated index.ts with verified complete exports, new chipset.yaml, new vtm-pipeline.ts class wrapper, and pipeline.ts fixes.
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/vtm/index.ts
@src/vtm/pipeline.ts
@src/vtm/types.ts
@src/vtm/vision-parser.ts
@src/vtm/vision-validator.ts
@src/vtm/research-compiler.ts
@src/vtm/research-utils.ts
@src/vtm/mission-assembly.ts
@src/vtm/mission-assembler.ts
@src/vtm/wave-planner.ts
@src/vtm/wave-analysis.ts
@src/vtm/model-assignment.ts
@src/vtm/model-budget.ts
@src/vtm/cache-optimizer.ts
@src/vtm/test-plan-generator.ts
@src/vtm/template-system.ts
@src/den/index.ts
@src/den/dispatcher.ts
@src/electronics-pack/chipset.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Barrel export audit, tech debt fixes, and chipset YAML</name>
  <files>src/vtm/index.ts, src/vtm/pipeline.ts, src/vtm/chipset.yaml</files>
  <action>
**Barrel export audit (INTG-01):**

1. Read every `src/vtm/*.ts` source file (excluding test files and index.ts itself) and collect all `export` declarations (functions, types, interfaces, constants).
2. Compare against what `src/vtm/index.ts` currently re-exports via `export * from './X.js'` statements.
3. Verify all 14 modules are covered: types, vision-parser, vision-validator, research-compiler, research-utils, mission-assembly, mission-assembler, wave-planner, wave-analysis, model-assignment, model-budget, cache-optimizer, test-plan-generator, template-system, pipeline.
4. After adding the new vtm-pipeline.ts (Task 2), add `export * from './vtm-pipeline.js'` to index.ts.
5. Update the module JSDoc comment to mention the class wrapper.

**Tech debt fixes in pipeline.ts:**

1. Change `var visionStage` at line 447 to `let visionStage` (code style fix from audit).
2. Wire `generateReadme` into the pipeline: In the mission stage (around line 517-544), after `assembleMissionPackage` completes, call `generateReadme(missionPackage)` and store the result. Then update `buildFileManifest` to use the actual README token size from generateReadme's output instead of the hardcoded `'~500 tokens'` string. The generateReadme function is already imported at line 40 but never called.

**Chipset YAML (INTG-02):**

Create `src/vtm/chipset.yaml` following the pattern established by `src/electronics-pack/chipset.yaml`. The VTM chipset should define:

```yaml
name: vtm
version: 0.1.0
description: "Vision-to-Mission Pipeline — transforms vision documents into GSD-ready mission packages"

skills:
  - id: vision-parsing
    name: Vision Document Processing
    description: "Parse, validate, classify, and extract dependencies from vision documents"
    triggers: [vision, parse, validate, archetype, classify, quality check]

  - id: research-compilation
    name: Research Reference Compilation
    description: "Compile structured research references with knowledge chunking and safety extraction"
    triggers: [research, compile, safety, knowledge tier, chunk, source quality]

  - id: mission-assembly
    name: Mission Package Assembly
    description: "Assemble complete mission packages with milestone specs, component specs, and self-containment validation"
    triggers: [mission, assemble, component spec, milestone, self-containment]

  - id: wave-planning
    name: Wave Planning & Analysis
    description: "Decompose missions into dependency-ordered waves with parallel tracks and risk analysis"
    triggers: [wave, plan, parallel track, dependency, critical path, risk]

  - id: model-assignment
    name: Model Assignment & Budget
    description: "Classify tasks into Opus/Sonnet/Haiku tiers with budget validation and auto-rebalance"
    triggers: [model, assign, opus, sonnet, haiku, budget, signal, rebalance]

  - id: cache-optimization
    name: Cache Optimization
    description: "Analyze shared loads, schema reuse, TTL compliance, and token savings"
    triggers: [cache, TTL, token savings, schema reuse, shared load]

  - id: test-plan-generation
    name: Test Plan Generation
    description: "Generate categorized test plans with safety classification and verification matrices"
    triggers: [test plan, safety, verification matrix, density, categorize]

  - id: template-rendering
    name: Template System
    description: "Load, render, validate, and register mustache-style markdown templates"
    triggers: [template, render, mustache, validate template, registry]

agents:
  - id: VTM-1
    name: vision-processor
    role: "Parses and validates vision documents, classifies archetypes"
    skills: [vision-parsing]

  - id: VTM-2
    name: research-compiler
    role: "Compiles research references from vision documents"
    skills: [research-compilation]

  - id: VTM-3
    name: mission-assembler
    role: "Assembles complete mission packages from vision and research"
    skills: [mission-assembly, wave-planning, model-assignment, cache-optimization, test-plan-generation, template-rendering]

topology:
  type: pipeline
  description: "Three-stage pipeline: VTM-1 (vision) -> VTM-2 (research, optional) -> VTM-3 (mission assembly)"
  flow:
    - from: VTM-1
      to: [VTM-2, VTM-3]
    - from: VTM-2
      to: [VTM-3]

evaluation:
  gates:
    preDeploy:
      - check: barrel_export_completeness
        description: "All public API symbols are re-exported from index.ts"
        action: block
      - check: self_containment
        description: "Component specs pass self-containment validation"
        action: block
      - check: budget_compliance
        description: "Model assignments satisfy 60/40 principle"
        action: warn
      - check: test_coverage
        threshold: 90
        action: block
```

Adjust the YAML to be valid and consistent with the electronics-pack chipset pattern (note: electronics chipset doesn't have `evaluation` at top level, it just has topology — check the pattern and follow it, but also add evaluation gates since that's specifically required by INTG-02).
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no TypeScript errors from barrel export changes. Verify pipeline.ts no longer has `var` keyword (`grep -n 'var ' src/vtm/pipeline.ts` should return no matches). Verify `generateReadme` appears in a call expression in pipeline.ts (not just an import). Verify chipset.yaml is valid YAML with `node -e "const yaml = require('yaml'); const fs = require('fs'); yaml.parse(fs.readFileSync('src/vtm/chipset.yaml', 'utf8'))"`.
  </verify>
  <done>Barrel exports cover all 14+1 source modules. pipeline.ts uses `let` instead of `var` and calls `generateReadme`. chipset.yaml defines 8 skills, 3 agents, pipeline topology, and evaluation gates.</done>
</task>

<task type="auto">
  <name>Task 2: VTMPipeline class wrapper with createVTMPipeline factory</name>
  <files>src/vtm/vtm-pipeline.ts</files>
  <action>
Create `src/vtm/vtm-pipeline.ts` implementing the functional API primary + class wrapper pattern used throughout the project (see `src/den/dispatcher.ts`, `src/den/planner.ts`, etc.).

The pattern is:
1. Pure functions are the primary API (already exist: `runPipeline`, `selectPipelineSpeed`, `parseVisionDocument`, `validateVisionDocument`, `classifyArchetype`, `compileResearch`, `assembleMissionPackage`, etc.)
2. A class wrapper holds configuration state and delegates to those pure functions
3. A `createXxx()` factory function instantiates the class

Create:

```typescript
/**
 * VTMPipeline class wrapper — stateful OOP interface around the VTM
 * functional API following the project's "functional API primary +
 * class wrapper" pattern (same as Den Dispatcher, Planner, etc.).
 *
 * The class holds a PipelineConfig and provides methods that delegate
 * to the pure functional API. Use `createVTMPipeline()` factory for
 * convenient construction.
 *
 * @module vtm/vtm-pipeline
 */

import type { VisionDocument } from './types.js';
import type { PipelineConfig, PipelineResult, PipelineSpeed, VisionStageResult, ResearchStageResult, MissionStageResult } from './pipeline.js';
import { runPipeline, selectPipelineSpeed } from './pipeline.js';
import { parseVisionDocument } from './vision-parser.js';
import type { ParseResult } from './vision-parser.js';
import { validateVisionDocument, checkQuality, classifyArchetype } from './vision-validator.js';
import type { VisionDiagnostic, Archetype } from './vision-validator.js';
import { compileResearch, checkSourceQuality } from './research-compiler.js';
import type { SourceDiagnostic } from './research-compiler.js';
import { assembleMissionPackage } from './mission-assembler.js';

export class VTMPipeline {
  private readonly config: PipelineConfig;
  private lastResult: PipelineResult | null = null;

  constructor(config?: PipelineConfig) {
    this.config = config ?? {};
  }

  /** Run the full pipeline (delegates to runPipeline). */
  run(input: string | VisionDocument, configOverride?: PipelineConfig): PipelineResult {
    const mergedConfig = { ...this.config, ...configOverride };
    this.lastResult = runPipeline(input, mergedConfig);
    return this.lastResult;
  }

  /** Parse a vision document markdown string. */
  parse(markdown: string): ParseResult<VisionDocument> {
    return parseVisionDocument(markdown);
  }

  /** Validate a vision document (structural). */
  validate(doc: VisionDocument): VisionDiagnostic[] {
    return validateVisionDocument(doc);
  }

  /** Run quality checks on a vision document. */
  checkQuality(doc: VisionDocument): VisionDiagnostic[] {
    return checkQuality(doc);
  }

  /** Classify a vision document's archetype. */
  classify(doc: VisionDocument): Archetype {
    return classifyArchetype(doc);
  }

  /** Select pipeline speed for a vision document. */
  selectSpeed(doc: VisionDocument): PipelineSpeed {
    return selectPipelineSpeed(doc, this.config);
  }

  /** Get the last pipeline result (null if run() never called). */
  getLastResult(): PipelineResult | null {
    return this.lastResult;
  }

  /** Get the current pipeline config. */
  getConfig(): PipelineConfig {
    return { ...this.config };
  }
}

/** Factory function to create a VTMPipeline instance. */
export function createVTMPipeline(config?: PipelineConfig): VTMPipeline {
  return new VTMPipeline(config);
}
```

Key points:
- Config is immutable after construction (consistent with Den pattern where Zod validates config in constructor). VTM doesn't need Zod validation since PipelineConfig is all optional fields.
- `run()` delegates to the pure `runPipeline` function and stores the last result.
- Each method is a thin wrapper around the corresponding functional API.
- `createVTMPipeline()` factory follows the `createDispatcher()`, `createPlanner()`, etc. naming convention.
- Export both the class and the factory from the module.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm the new file compiles without errors. Run `node -e "import('./dist/vtm/vtm-pipeline.js').then(m => console.log(typeof m.VTMPipeline, typeof m.createVTMPipeline))"` (after build) to verify exports. Or simply verify that `grep -c 'export' src/vtm/vtm-pipeline.ts` shows at least 2 exports (class + factory).</verify>
  <done>VTMPipeline class wrapper and createVTMPipeline factory exist, delegate to functional API, and are re-exported from index.ts.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript compilation errors
2. `grep 'var ' src/vtm/pipeline.ts` — no matches (var keyword eliminated)
3. `grep 'generateReadme(' src/vtm/pipeline.ts` — at least one call expression match
4. `node -e "const y=require('yaml');y.parse(require('fs').readFileSync('src/vtm/chipset.yaml','utf8'))"` — valid YAML
5. Count of `export * from` lines in index.ts is >= 15 (14 existing + 1 new vtm-pipeline)
6. `VTMPipeline` and `createVTMPipeline` are importable from `src/vtm/index.ts`
</verification>

<success_criteria>
- All 15+ VTM source modules are barrel-exported from index.ts
- chipset.yaml defines skills (8), agents (3), pipeline topology, and evaluation gates
- VTMPipeline class wraps functional API with createVTMPipeline factory
- pipeline.ts tech debt resolved (var->let, generateReadme wired)
- Zero TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/290-integration-testing/290-01-SUMMARY.md`
</output>
