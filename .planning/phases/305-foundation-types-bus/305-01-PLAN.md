---
phase: 305-foundation-types-bus
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/brainstorm/shared/types.ts
  - src/brainstorm/shared/constants.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-05

must_haves:
  truths:
    - "All 9 enum types (AgentRole, SessionPhase, TechniqueId, TechniqueCategory, PathwayId, HatColor, ScamperLens, OsbornRule, EnergyLevel, SessionStatus) are defined as Zod schemas with inferred TypeScript types and compile without error"
    - "All 12 object schemas (Idea, Question, Cluster, Evaluation, ActionItem, TimerState, SessionState, BrainstormMessage, TechniqueConfig, PathwayConfig, AgentActivation, SessionConfig) are Zod schemas with z.infer<> derived types"
    - "TECHNIQUE_DEFAULTS covers all 16 TechniqueId values with duration_ms and agents fields; TypeScript enforces exhaustive coverage via Record<TechniqueId, ...>"
    - "AGENT_PHASE_RULES covers all 8 AgentRole values; critic.active contains only ['converge']"
    - "No import from src/den/, src/vtm/, src/knowledge/, or any module outside zod and node:*"
  artifacts:
    - path: "src/brainstorm/shared/types.ts"
      provides: "All Zod schemas and TypeScript types for the brainstorm system"
      exports:
        - AgentRoleSchema
        - SessionPhaseSchema
        - TechniqueIdSchema
        - TechniqueCategorySchema
        - PathwayIdSchema
        - HatColorSchema
        - ScamperLensSchema
        - OsbornRuleSchema
        - EnergyLevelSchema
        - SessionStatusSchema
        - MessageTypeSchema
        - IdeaSchema
        - QuestionSchema
        - ClusterSchema
        - EvaluationSchema
        - ActionItemSchema
        - TimerStateSchema
        - SessionStateSchema
        - BrainstormMessageSchema
        - TechniqueConfigSchema
        - PathwayConfigSchema
        - AgentActivationSchema
        - SessionConfigSchema
      min_lines: 150
    - path: "src/brainstorm/shared/constants.ts"
      provides: "All runtime constants for technique defaults, agent rules, message priorities"
      exports:
        - PHASE_ORDER
        - TECHNIQUE_DEFAULTS
        - HAT_DESCRIPTIONS
        - SCAMPER_PROMPTS
        - AGENT_PHASE_RULES
        - MESSAGE_PRIORITIES
      min_lines: 60
  key_links:
    - from: "src/brainstorm/shared/types.ts"
      to: "zod"
      via: "import { z } from 'zod'"
      pattern: "import.*zod"
    - from: "src/brainstorm/shared/constants.ts"
      to: "src/brainstorm/shared/types.ts"
      via: "type imports for Record keys"
      pattern: "from.*types\\.js"
---

<objective>
Create the complete shared type layer and constants for the brainstorm system.

Purpose: Every downstream brainstorm component (Rules Engine, Session Manager, agents, technique engine) imports its types from this foundation. Nothing else can be built until these types compile.
Output: src/brainstorm/shared/types.ts and src/brainstorm/shared/constants.ts
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/305-foundation-types-bus/305-RESEARCH.md
@.planning/input/v1.32/03-shared-types-spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/brainstorm/shared/types.ts with all Zod schemas</name>
  <files>src/brainstorm/shared/types.ts</files>
  <action>
Create `src/brainstorm/shared/types.ts`. Use Zod schema-first pattern throughout — identical to src/den/types.ts. NEVER write TypeScript interfaces or type aliases directly; always define the Zod schema first then derive the type with z.infer<>.

Only allowed import: `import { z } from 'zod';` — no imports from src/den/, src/vtm/, src/knowledge/, or any project module.

Create these schemas in order (each immediately followed by its z.infer<> type export):

**Enum schemas (z.enum([...])):**
1. `AgentRoleSchema` — values: 'facilitator', 'ideator', 'questioner', 'analyst', 'mapper', 'critic', 'persona', 'scribe'
2. `SessionPhaseSchema` — values: 'explore', 'diverge', 'organize', 'converge', 'act'
3. `TechniqueCategorySchema` — values: 'individual', 'collaborative', 'analytical', 'visual'
4. `TechniqueIdSchema` — 16 values (4 individual, 5 collaborative, 4 analytical, 3 visual): 'freewriting', 'mind-mapping', 'rapid-ideation', 'question-brainstorming', 'brainwriting-635', 'round-robin', 'brain-netting', 'rolestorming', 'figure-storming', 'scamper', 'six-thinking-hats', 'starbursting', 'five-whys', 'storyboarding', 'affinity-mapping', 'lotus-blossom'
5. `PathwayIdSchema` — values: 'creative-exploration', 'problem-solving', 'product-innovation', 'decision-making', 'free-form'
6. `HatColorSchema` — values: 'white', 'red', 'black', 'yellow', 'green', 'blue'
7. `ScamperLensSchema` — values: 'substitute', 'combine', 'adapt', 'modify', 'put-to-another-use', 'eliminate', 'reverse'
8. `OsbornRuleSchema` — values: 'quantity', 'no-criticism', 'wild-ideas', 'build-combine'
9. `EnergyLevelSchema` — values: 'high', 'medium', 'low', 'flagging'
10. `SessionStatusSchema` — values: 'created', 'active', 'paused', 'completed', 'abandoned'
11. `MessageTypeSchema` — 14 values: 'idea', 'question', 'cluster', 'evaluation', 'phase_transition', 'technique_switch', 'timer_event', 'hat_color', 'energy_signal', 'rule_violation', 'user_input', 'artifact_ready', 'agent_activated', 'agent_deactivated'

**Object schemas (z.object({...})):**
12. `IdeaSchema` — fields: id (z.string().uuid()), content (z.string().min(1)), source_agent (AgentRoleSchema), source_technique (TechniqueIdSchema), phase (SessionPhaseSchema), parent_id (z.string().uuid().optional()), perspective (z.string().optional()), cluster_id (z.string().uuid().optional()), priority (z.number().int().min(0).max(10).optional()), scamper_lens (ScamperLensSchema.optional()), hat_color (HatColorSchema.optional()), tags (z.array(z.string())), timestamp (z.number().int().positive())
13. `QuestionSchema` — fields: id (z.string().uuid()), content (z.string().min(1)), category (z.enum(['who','what','where','when','why','how','root-cause'])), source_technique (TechniqueIdSchema), depth (z.number().int().min(0).optional()), parent_id (z.string().uuid().optional()), timestamp (z.number().int().positive())
14. `ClusterSchema` — fields: id (z.string().uuid()), label (z.string().min(1)), idea_ids (z.array(z.string().uuid())), theme (z.string().optional()), priority (z.number().int().min(0).max(10).optional())
15. `EvaluationSchema` — fields: idea_id (z.string().uuid()), feasibility (z.number().int().min(1).max(5)), impact (z.number().int().min(1).max(5)), alignment (z.number().int().min(1).max(5)), risk (z.number().int().min(1).max(5)), notes (z.string().optional()), evaluator (AgentRoleSchema), timestamp (z.number().int().positive())
16. `ActionItemSchema` — fields: id (z.string().uuid()), description (z.string().min(1)), source_idea_ids (z.array(z.string().uuid())), owner (z.string().optional()), deadline (z.string().optional()), priority (z.enum(['high','medium','low'])), status (z.enum(['proposed','accepted','in-progress','complete']))
17. `TimerStateSchema` — fields: technique_timer (z.object({ remaining_ms: z.number().int().min(0), total_ms: z.number().int().positive() }).nullable()), session_timer (z.object({ elapsed_ms: z.number().int().min(0), max_ms: z.number().int().positive().optional() })), is_paused (z.boolean())
18. `SessionStateSchema` — fields: id (z.string().uuid()), status (SessionStatusSchema), phase (SessionPhaseSchema), problem_statement (z.string().min(1)), active_technique (TechniqueIdSchema.nullable()), active_pathway (PathwayIdSchema.nullable()), technique_queue (z.array(TechniqueIdSchema)), active_agents (z.array(AgentRoleSchema)), ideas (z.array(IdeaSchema)), questions (z.array(QuestionSchema)), clusters (z.array(ClusterSchema)), evaluations (z.array(EvaluationSchema)), action_items (z.array(ActionItemSchema)), timer (TimerStateSchema), energy_level (EnergyLevelSchema), rules_active (z.array(OsbornRuleSchema)), hat_color (HatColorSchema.optional()), metadata (z.object({ created_at: z.number().int().positive(), updated_at: z.number().int().positive(), total_ideas: z.number().int().min(0), total_questions: z.number().int().min(0), techniques_used: z.array(TechniqueIdSchema), phase_history: z.array(z.object({ phase: SessionPhaseSchema, entered_at: z.number().int().positive(), exited_at: z.number().int().positive().optional() })) }))
19. `BrainstormMessageSchema` — fields: id (z.string().uuid()), from (z.union([AgentRoleSchema, z.literal('system')])), to (z.union([AgentRoleSchema, z.literal('broadcast')])), type (MessageTypeSchema), phase (SessionPhaseSchema), payload (z.record(z.string(), z.unknown())), timestamp (z.number().int().positive()), session_id (z.string().uuid()), priority (z.number().int().min(0).max(7))
20. `TechniqueConfigSchema` — fields: id (TechniqueIdSchema), name (z.string()), category (TechniqueCategorySchema), description (z.string()), default_duration_ms (z.number().int().positive()), min_duration_ms (z.number().int().positive()), max_duration_ms (z.number().int().positive()), required_agents (z.array(AgentRoleSchema)), optional_agents (z.array(AgentRoleSchema)), valid_phases (z.array(SessionPhaseSchema)), osborn_rules (z.array(OsbornRuleSchema)), parameters (z.record(z.string(), z.unknown()))
21. `PathwayConfigSchema` — fields: id (PathwayIdSchema), name (z.string()), description (z.string()), situation (z.string()), technique_sequence (z.array(z.object({ technique: TechniqueIdSchema, duration_ms: z.number().int().positive(), notes: z.string() }))), required_agents (z.array(AgentRoleSchema)), recommended_for (z.array(z.string()))
22. `AgentActivationSchema` — fields: role (AgentRoleSchema), active_phases (z.array(SessionPhaseSchema)), behavioral_constraints (z.array(z.string())), skills_required (z.array(z.string()))
23. `SessionConfigSchema` — fields: brainstormDir (z.string().min(1)), sessionId (z.string().uuid())

Use NodeNext module resolution conventions: when this file imports from other project files, use .js extensions. But this file only imports from 'zod' so no .js extension issues arise.
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>Exit code 0 from tsc --noEmit. File src/brainstorm/shared/types.ts exists with all 23 schemas exported. No imports from den/, vtm/, knowledge/ or other src/ modules.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/brainstorm/shared/constants.ts with all lookup tables</name>
  <files>src/brainstorm/shared/constants.ts</files>
  <action>
Create `src/brainstorm/shared/constants.ts`. Import types from `./types.js` (NodeNext requires .js extension on local imports).

Use `as const` pattern for all lookup objects — NEVER TypeScript enums. Use `Record<TechniqueId, ...>` so TypeScript enforces exhaustive coverage at compile time (missing key = compile error).

Create these exports:

**PHASE_ORDER:**
```typescript
export const PHASE_ORDER: SessionPhase[] = ['explore', 'diverge', 'organize', 'converge', 'act'];
```

**TECHNIQUE_DEFAULTS** (Record<TechniqueId, {duration_ms: number, agents: AgentRole[]}>) — must include all 16 TechniqueId values:
- 'freewriting': { duration_ms: 600_000, agents: ['ideator'] }
- 'mind-mapping': { duration_ms: 900_000, agents: ['mapper'] }
- 'rapid-ideation': { duration_ms: 60_000, agents: ['ideator'] }
- 'question-brainstorming': { duration_ms: 600_000, agents: ['questioner'] }
- 'brainwriting-635': { duration_ms: 1_800_000, agents: ['ideator'] }
- 'round-robin': { duration_ms: 900_000, agents: ['ideator'] }
- 'brain-netting': { duration_ms: 1_200_000, agents: ['ideator'] }
- 'rolestorming': { duration_ms: 900_000, agents: ['persona', 'ideator'] }
- 'figure-storming': { duration_ms: 900_000, agents: ['persona', 'ideator'] }
- 'scamper': { duration_ms: 1_800_000, agents: ['analyst', 'ideator'] }
- 'six-thinking-hats': { duration_ms: 2_100_000, agents: ['analyst'] }
- 'starbursting': { duration_ms: 900_000, agents: ['questioner'] }
- 'five-whys': { duration_ms: 600_000, agents: ['questioner'] }
- 'storyboarding': { duration_ms: 900_000, agents: ['mapper'] }
- 'affinity-mapping': { duration_ms: 900_000, agents: ['mapper'] }
- 'lotus-blossom': { duration_ms: 900_000, agents: ['mapper', 'ideator'] }
Add `as const` at the end.

**HAT_DESCRIPTIONS** (Record<HatColor, {mode: string, focus: string}>):
- 'white': { mode: 'Facts and Data', focus: 'Objective information analysis' }
- 'red': { mode: 'Emotions and Intuition', focus: 'Gut reactions without justification' }
- 'black': { mode: 'Critical Judgment', focus: 'Risks, weaknesses, problems' }
- 'yellow': { mode: 'Optimistic Thinking', focus: 'Benefits, value, opportunities' }
- 'green': { mode: 'Creative Thinking', focus: 'New ideas, alternatives, possibilities' }
- 'blue': { mode: 'Process Control', focus: 'Managing the discussion itself' }
Add `as const`.

**SCAMPER_PROMPTS** (Record<ScamperLens, string[]>):
- 'substitute': ['What materials, components, or people could be swapped?']
- 'combine': ['What features, functions, or ideas could be merged?']
- 'adapt': ['What could be borrowed or adapted from other domains?']
- 'modify': ['What could be made bigger, smaller, or changed in form?']
- 'put-to-another-use': ['How else could this be used? Who else could use it?']
- 'eliminate': ['What could be removed or simplified?']
- 'reverse': ['What would happen if you reversed the order or process?']
Add `as const`.

**AGENT_PHASE_RULES** (Record<AgentRole, {active: SessionPhase[], constraints: string[]}>):
- 'facilitator': { active: ['explore','diverge','organize','converge','act'], constraints: ['Never evaluate ideas directly'] }
- 'ideator': { active: ['diverge'], constraints: ['Never evaluate own output', 'Never filter ideas'] }
- 'questioner': { active: ['explore','diverge'], constraints: ['Generate questions only, never answers'] }
- 'analyst': { active: ['diverge','organize'], constraints: ['Follow framework structure strictly'] }
- 'mapper': { active: ['diverge','organize'], constraints: ['Organize without evaluating quality'] }
- 'critic': { active: ['converge'], constraints: ['NEVER activate during diverge phase'] }
- 'persona': { active: ['diverge'], constraints: ['Maintain perspective consistently', 'Constructive personas only'] }
- 'scribe': { active: ['explore','diverge','organize','converge','act'], constraints: ['Capture only, never generate ideas'] }
Add `as const`.

**MESSAGE_PRIORITIES:**
```typescript
export const MESSAGE_PRIORITIES = {
  RULES_VIOLATION: 0,
  USER_INPUT: 0,
  PHASE_TRANSITION: 1,
  HAT_COLOR_CHANGE: 1,
  TECHNIQUE_SWITCH: 2,
  IDEA_CAPTURE: 3,
  ENERGY_SIGNAL: 4,
  TIMER_EVENT: 5,
  AGENT_LIFECYCLE: 6,
  HEARTBEAT: 7,
} as const;
```

Import only from './types.js'. Do not import from any other project module.
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>Exit code 0. TECHNIQUE_DEFAULTS type-checks as exhaustive Record over all 16 TechniqueId values. AGENT_PHASE_RULES has 8 entries with critic.active === ['converge']. All constants use as const, no TypeScript enums anywhere.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:
1. `npx tsc --noEmit` — must exit 0
2. `grep -r "from '../../den\|from '../../vtm\|from '../../knowledge" src/brainstorm/` — must produce no output
3. `grep -c "export const.*Schema" src/brainstorm/shared/types.ts` — must be >= 23
4. `grep -c "export const" src/brainstorm/shared/constants.ts` — must be >= 6
</verification>

<success_criteria>
- src/brainstorm/shared/types.ts exists with all 23 Zod schemas + z.infer<> types
- src/brainstorm/shared/constants.ts exists with PHASE_ORDER, TECHNIQUE_DEFAULTS (16 entries), HAT_DESCRIPTIONS (6), SCAMPER_PROMPTS (7), AGENT_PHASE_RULES (8 entries, critic active only in converge), MESSAGE_PRIORITIES (9 keys)
- `npx tsc --noEmit` exits 0
- Zero imports from any existing src/ module (only zod and ./types.js)
</success_criteria>

<output>
After completion, create `.planning/phases/305-foundation-types-bus/305-01-SUMMARY.md`
</output>
