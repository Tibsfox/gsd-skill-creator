---
phase: 305-foundation-types-bus
plan: "03"
type: tdd
wave: 3
depends_on:
  - "305-01"
  - "305-02"
files_modified:
  - src/brainstorm/shared/types.test.ts
  - src/brainstorm/shared/schemas.test.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05

must_haves:
  truths:
    - "All 9 enum schemas accept valid values and reject invalid values — verified by Vitest unit tests"
    - "IdeaSchema and BrainstormMessageSchema reject malformed input (missing required fields, wrong enum values)"
    - "brainstormMessageFilename() produces 4 distinct filenames when called 4 times in the same millisecond"
    - "Filenames from brainstormMessageFilename() sort lexicographically in write order"
    - "TECHNIQUE_DEFAULTS has exactly 16 keys matching all TechniqueId values"
    - "AGENT_PHASE_RULES.critic.active === ['converge'] only"
    - "MESSAGE_PRIORITIES has 9 named entries covering priority levels 0-7"
    - "initBrainstormSession creates 4 bus subdirectories in a temp directory"
  artifacts:
    - path: "src/brainstorm/shared/types.test.ts"
      provides: "Unit tests for all enum schemas, object schemas, FOUND-01 and FOUND-02 coverage"
      min_lines: 80
    - path: "src/brainstorm/shared/schemas.test.ts"
      provides: "Unit tests for bus filename generation, JSONL parsing, session init, constants — FOUND-03, FOUND-04, FOUND-05 coverage"
      min_lines: 60
  key_links:
    - from: "src/brainstorm/shared/types.test.ts"
      to: "src/brainstorm/shared/types.ts"
      via: "named imports of all schemas under test"
      pattern: "from.*types\\.js"
    - from: "src/brainstorm/shared/schemas.test.ts"
      to: "src/brainstorm/shared/schemas.ts"
      via: "imports of brainstormMessageFilename, resetBrainstormCounter, initBrainstormSession"
      pattern: "from.*schemas\\.js"
---

<objective>
Write and run Vitest unit tests verifying all 5 Phase 305 requirements.

Purpose: Tests prove the foundation is correct before downstream phases (Rules Engine, Session Manager) build on it. TDD here means writing tests that verify observable behaviors specified in ROADMAP.md success criteria — not implementation details.
Output: Two test files with green tests; `npx vitest run src/brainstorm/` exits 0
</objective>

<execution_context>
@/home/foxy/.claude/get-shit-done/workflows/execute-plan.md
@/home/foxy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/305-foundation-types-bus/305-RESEARCH.md
@.planning/phases/305-foundation-types-bus/305-01-SUMMARY.md
@.planning/phases/305-foundation-types-bus/305-02-SUMMARY.md
</context>

<feature>
  <name>Phase 305 Foundation Verification Tests</name>
  <files>src/brainstorm/shared/types.test.ts, src/brainstorm/shared/schemas.test.ts</files>

  <behavior>
    Types test file verifies FOUND-01 and FOUND-02:
    - AgentRoleSchema.parse('facilitator') succeeds; parse('coordinator') throws ZodError
    - SessionPhaseSchema.parse('diverge') succeeds; parse('planning') throws ZodError
    - TechniqueIdSchema — all 16 values parse; 'unknown-technique' throws
    - IdeaSchema.parse(validIdea) succeeds; missing 'id' field throws; invalid UUID in 'id' throws
    - BrainstormMessageSchema.parse(msg) with missing session_id throws ZodError
    - SessionStateSchema round-trip: JSON.stringify then JSON.parse then parse() succeeds
    - All optional fields on IdeaSchema (parent_id, perspective, cluster_id, priority, scamper_lens, hat_color) are truly optional — omitting all of them does not throw

    Schemas test file verifies FOUND-03, FOUND-04, FOUND-05:
    - FOUND-03: Call brainstormMessageFilename 4 times synchronously → Set has size 4 (no collisions)
    - FOUND-03: Filenames are lexicographically sortable in write order (counter suffix ensures this)
    - FOUND-03: resetBrainstormCounter() resets counter to 0 (filename after reset starts with _000001)
    - FOUND-04: initBrainstormSession creates {tmpdir}/sessions/{id}/bus/{session,capture,user,energy}/ — use os.tmpdir() + randomUUID() for isolation
    - FOUND-04: initBrainstormSession is idempotent (calling twice does not throw)
    - FOUND-05: Object.keys(TECHNIQUE_DEFAULTS).length === 16
    - FOUND-05: TechniqueIdSchema.options.every(id => id in TECHNIQUE_DEFAULTS) — no missing key
    - FOUND-05: Object.keys(AGENT_PHASE_RULES).length === 8
    - FOUND-05: AGENT_PHASE_RULES.critic.active deep equals ['converge']
    - FOUND-05: Object.keys(MESSAGE_PRIORITIES).length === 9
  </behavior>

  <implementation>
Follow project test conventions (Vitest, not Jest). Use `import { describe, it, expect, beforeEach, afterEach } from 'vitest'`. Use `import { tmpdir } from 'node:os'` and `import { rm } from 'node:fs/promises'` for temp directory cleanup in session init tests.

RED phase:
1. Write types.test.ts — initially all tests will fail since types.ts may not exist yet in this executor's context. Run `npx vitest run src/brainstorm/shared/types.test.ts 2>&1` — verify RED (fail or pass depending on whether 305-01 ran first).
2. Write schemas.test.ts — run `npx vitest run src/brainstorm/shared/schemas.test.ts 2>&1` — verify tests are structured correctly.

GREEN phase:
- If 305-01 and 305-02 completed (which they must, since wave: 3 depends_on both), all tests should pass.
- Run `npx vitest run src/brainstorm/shared/ 2>&1`
- If any test fails, diagnose the failure. If it's a schema mismatch, fix the test to match the actual exported type. If it's an implementation bug (e.g., wrong AGENT_PHASE_RULES entry), fix the implementation file.

REFACTOR phase:
- Ensure test descriptions are clear and map to requirement IDs in comments.
- No test should use `any` type casting.
  </implementation>
</feature>

<verification>
1. `npx vitest run src/brainstorm/shared/ 2>&1` — all tests pass, exit code 0
2. `npx tsc --noEmit 2>&1` — still passes (test files don't break compilation)
3. Test count: `npx vitest run src/brainstorm/shared/ --reporter=verbose 2>&1 | grep -c "✓"` — should be >= 18
</verification>

<success_criteria>
- types.test.ts exists with FOUND-01 and FOUND-02 coverage
- schemas.test.ts exists with FOUND-03, FOUND-04, FOUND-05 coverage
- All tests pass: `npx vitest run src/brainstorm/shared/` exits 0
- At least 18 individual test cases (as specified in RESEARCH.md test inventory)
- No test uses dynamic `any` casts to bypass type safety
</success_criteria>

<output>
After completion, create `.planning/phases/305-foundation-types-bus/305-03-SUMMARY.md`
</output>
