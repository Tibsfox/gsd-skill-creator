# Hardware Profile (sanitized -- safe for version control)
# Generated by discover-hardware.sh on 2026-01-01T00:00:00Z
# Contains NO identifying information (no MACs, serials, hostnames)
#
# This is an EXAMPLE file showing the expected YAML structure.
# Run `infra/scripts/discover-hardware.sh` to generate the actual
# hardware-profile.yaml for your machine.
#
# Downstream consumers (provisioning, VM creation, kickstart templating)
# should parse hardware-profile.yaml -- NOT this example file.

cpu:
  architecture: x86_64
  family: "Intel Core i7 13th Gen"
  cores_physical: 8
  cores_logical: 16
  virtualization: true
  virtualization_type: vmx

memory:
  total_gb: 64
  swap_gb: 8

storage:
  devices:
    - type: ssd
      size_gb: 1000
    - type: hdd
      size_gb: 2000
  total_gb: 3000

gpu:
  present: true
  vendor: nvidia
  family: "GeForce RTX 40 Series"
  vram_gb: 8
  driver: nvidia
  iommu_available: true

hypervisor:
  kvm: true
  vmware: false
  virtualbox: false
  podman: true
  docker: false
  nested_virtualization: true

capabilities:
  can_run_vms: true
  can_passthrough_gpu: true
  meets_minimum: true
